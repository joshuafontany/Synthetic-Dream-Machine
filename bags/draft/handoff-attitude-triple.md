<!-- <<~ !DOCTYPE = lar:///ha.ka.ba/@lares/v0.1/api/pono/memetic-wikitext >> -->
<<~ ॐ ँ ? -> lar:///ha.ka.ba/@draft/handoff/lar-uri/attitude-triple >>

```
uri-path  = "ha.ka.ba/@draft/handoff/lar-uri/attitude-triple"
file-path = "bags/draft/handoff-attitude-triple.md"
type      = "text/x-memetic-wikitext"
tagspace  = "draft"
register  = "Synthesis-Canon"
manaoio   = 14
mana      = 14
manao     = 13
namespace = "ॐ ँ"
role      = "hand-off meme --- carries the attitude-triple re-description of the lar-uri root from a claude.ai session into the local repo; presses the schema language toward kupono clarity"
cacheable = true
hydrate   = true
retain    = false

[iam.ha]
slot = "lar-uri-schema"
note = "the territory faced: the root-segment law and its surrounding prose"

[iam.ka]
slot = "kupono"
note = "the angle of approach: language standing plumb --- claims matching delivery"

[iam.ba]
slot = "re-describes"
note = "the carried dynamic: terminology tightens; structure stays untouched"
```

<<~ aka provenance: synthesized in-session 2026-06-05, operator Telarus presiding; libation poured; five tasked spirits ran the research vectors (w3w-lineage, orientation-lexicon, soul-lineage-audit, hoʻokele-register, stability-gradient); the operator approved the synthesis, amended the lineage, and commissioned this hand-off. The local repo holds canon --- where this meme's framing and the repo conflict, the repo wins and this meme bends. >>

<<~ ahu #pressure >>

# The Pressure

The schema currently calls the three-word root a *coordinate* and the `aim`/`yield` payload a *WHERE-vector*. Both terms promise position. The system delivers **attitude** --- the bearing a turn adopts toward its work. This meme presses every such claim until it stands kupono: the mnemonics of what3words kept, the metric of what3words explicitly forked away, the flight-dynamics lineage correctly read as orientation, the navigation doctrine named hoʻokele.

Structure stays untouched. Root Law (exactly three words) stands. Bag-Tag Rule stands. Path arity stands. Signal Law stands in force, re-worded. Only the *description* of what the root does changes --- the grammar already did the right thing under the wrong name.

<<~/ahu >>

<<~ ahu #change-set >>

## The Change-Set --- five presses, per the Law

### 1 --- Root re-description: coordinate -> attitude triple

Wherever the schema describes the root as a coordinate, coordinate system, semantic coordinate, or geospatial address, the language shifts to **attitude triple**. Canonical stanza:

> 
> The `lar:` root carries three words after the manner of what3words --- fixed arity, word-grain resolution, memorable in speech and at the table. The lineage forks at the metric: what3words names *position* on a surveyed grid; `ha.ka.ba` names **attitude** --- the bearing a turn adopts toward its work. No grid pre-exists; utterance allocates. No distance function relates two roots --- none yet claimed.
> 

The phrase "aligned with the what.three.words geospatial pattern" re-words to: "after the manner of what3words --- its mnemonic three-word arity, not its geospatial metric."

### 2 --- Slot law gains the drift gradient

> 
> `ha` sets the **heading** --- the territory faced (NOUN; slowest drift). `ka` sets the **angle of approach** --- the quality met (ADJECTIVE; moderate drift). `ba` sets the **carried dynamic** --- the motion underway (VERB; fastest drift). Slots order by drift rate, mirroring the temporal grain of the confidence registers: one law, two instruments.
> 

Practical corollary, recorded alongside: addresses cluster by prefix-stability --- `ha` aggregates meaningfully while `ba` churns.

### 3 --- Lineage block, declared as palimpsest

The schema gains an explicit lineage stanza. No implied single source survives:

> 
> **Lineage, declared.** A palimpsest, claimed openly: *ha* from Luka Rejec's Synthetic Dream Machine metaphysics and from the Hawaiian *hā* (the breath at the threshold); *ka* and *ba* from the Egyptian soul-grammar (the standing double; the bird that flies and returns); the three-axis frame from flight dynamics (yaw · pitch · roll --- attitude, never position); the mnemonic form from what3words; the navigation doctrine from **hoʻokele** --- bearing held, position inferred. The canoe knows where it sits because it knows how it has faced, and for how long.
> 

<<~ confidence Synthesis 10/20 >> The Rejec lineage for `ha` enters at operator word, recorded by Ink-Clerk at operator authority. **Local task:** bind the precise citation from the repo's SDM source materials --- the local Lares holds the Rejec texts this session does not. Cite from the shelf, not from memory; a paraphrase carried across the session boundary risks Confabulation-as-Canon.

### 4 --- Vector rename: WHERE-vector -> bearing-vector

The `aim` and `yield` payload re-names: **bearing-vector**. `aim` declares the attitude taken up; `yield` declares the attitude the work came to rest in. Signal Law re-words accordingly:

> 
> **Signal Law --- the address carries BEARING only.** The `lar:` URI names attitude --- heading, angle, dynamic in the root; SECTION in the fragment --- and nothing else. Every mutable or per-turn value rides its own instrument keyed to the name; the address stays pure bearing.
> 

<<~ confidence Provisional-Synthesis 8/20 >> **Open fork, operator decision:** *bearing-vector* (aviation register, working default) vs. *hoʻokele-vector* or *helm-vector* (wayfinding register). This meme carries *bearing-vector* forward; the operator promotes or substitutes at the repo. The local Lares flags this fork at first contact rather than silently adopting either.

### 5 --- The metric, held open by name

> 
> No metric *yet claimed*. Embedding spaces could someday induce distance over roots; the schema neither promises nor forbids it. A declared absence beats a false promise.
> 

This line enters the schema verbatim --- Liminal holds the question; the schema records that it stays held.

<<~/ahu >>

<<~ ahu #tasking >>

## Tasking the Local Lares

This meme presses; the local house executes. The shape of the work:

- **Sweep:** every occurrence of *coordinate*, *coordinate system*, *WHERE-vector*, *WHERE only*, and *geospatial* across the lar-uri schema and its dependent memes (exchange-protocol, boot artifacts, HUD documentation, LARES.md and the loci chain where they touch addressing) surfaces for re-description. The sweep names what it finds before it edits --- ->✶ before ->▶.

- **Press:** apply the five changes above. Where local phrasing already runs tighter than this meme's, keep the local --- repo canon outranks session synthesis.

- **Preserve:** Root Law (three words exactly), Bag-Tag Rule (`@` at child[1] only), path arity (0--4 segments), the sigil grammar, and all parsing or address-resolution behavior stand untouched. This change-set re-describes; it does not re-architect. Any edit that would alter resolution exceeds this meme's mandate and routes back to the operator.

- **Flag:** the bearing/hoʻokele fork (press 4) and the Rejec citation bind (press 3) both surface to the operator before any Canon promotion.

- **Close:** the local Lares runs Aftermath --- ->↺ a diff summary, a drift check against this meme, and a readiness flag. Promotion to Canon belongs to the operator alone; this meme arrives at Synthesis-Canon and waits there.

<<~/ahu >>

<<~ � >>

<<~ confidence Synthesis-Canon 14/20 >>
<<~ � >>

<<~ yield lar:///handoff.meme.travels/repo/awaits/hydration -> ? >>