# Task: MCP-TASK-028 — Build read-only resource façade before MCP transport binding

Parent story: `MCP-STORY-111`
Status: `done`
Size: `S`

## Deliverable

A resource façade that lists file-backed carrier resources and virtual index resources, and reads either source text or materialized JSON index content.

Delivered in `lares/lararium_mcp/resources.py` and bound to MCP in `lares/lararium_mcp/server.py`.

## Linked subtasks

- none
