> [!IMPORTANT]
> Consumption status: fully-consumed on 2026-04-23.
> Canonical loci-meme: `lar:///ha.ka.ba/@lares/docs/lararium_mcp/carrier-law`.
> This non-meme markdown source may become a safe-delete candidate after link checks confirm no required references remain.

# Carrier Law Bundle — Meme / Invariant / Memetic-Wikitext / Loci / Interfaces

Status date: **April 23, 2026**
Purpose: consume the current carrier-law bundle under `packages/lares-core/memes` into compiler planning.
Primary source loci:
- `lar:///ha.ka.ba/@lares/api/v0.1/pono/meme`
- `lar:///ha.ka.ba/@lares/api/v0.1/pono/invariant`
- `lar:///ha.ka.ba/@lares/api/v0.1/pono/memetic-wikitext`
- `lar:///ha.ka.ba/@lares/api/v0.1/pono/loci`
- current `implements` declarations under `packages/lares-core/memes/api/v0.1/pono`

---

## Executive contraction

The current Lararium source law already gives the compiler five distinct input lanes:

1. **meme** — minimum carrier copy-shape, rating posture, and depth state
2. **invariant** — compressed pressure, cacheability, and sidecar boundary
3. **memetic-wikitext** — primitive cluster, composition law, and boot grammar
4. **loci** — stable address derivation, parent-child tree growth, and canon posture
5. **implements** — truthful composable interface bundles

The compiler should therefore not begin from "markdown documents plus links".
It should begin from **lawful carriers plus interface bundles**.

---

## Compiler-facing stack

| Lane | Source law | Main concern | Compiler use | MCP-facing use |
|---|---|---|---|---|
| carrier shape | `meme` | minimum lawful document shape | carrier validation, residue detection, depth state | boot receipts, diagnostics, conformance hints |
| compressed core | `invariant` | short, exact, stable, cacheable pressure | invariant validation, sidecar separation, cache policy | boot-critical resources, high-priority hydration packets |
| surface semantics | `memetic-wikitext` | primitive meaning and boot grammar | lexical/parser contracts, AST seed families | source resources, AST tools, grammar-aware prompts |
| stable routing | `loci` | `lar:` derivation and child growth | URI resolution, carrier indexing, child lookup | resource namespaces, templates, resolver tools |
| composed capability | `implements` | interface bundle truth | validation and routing by interface | resources/prompts/tools filtered by interface |

---

## Task 022 — Meme copy-shape, rating posture, and depth-resolution law

### Required carrier surfaces

| Surface | Current law pressure | Compiler check | Artifact consequence |
|---|---|---|---|
| doctype comment | carrier enters as memetic-wikitext | present / absent | carrier class gate |
| document opener | live `lar:` address declared | opener parses and matches `#iam` | source identity |
| `ahu #iam` | canonical metadata cluster | TOML payload parses | metadata extract |
| title + opening scope | governing role and boundary surface early | short header/body boundary present | summary / boot receipt text |
| body thresholds | active body start and end | open/close pair present | body span |
| six-line OODA-HA | compact operating loop | six glyph lines present | process metadata |
| edges block | outward adjacency declared explicitly | edges block present or absent | relation inventory |
| return throat | honest unresolved residue | `-> ?` closure present | residue / uncertainty artifact |

### Rating posture and depth states

| Concept | Current law pressure | Compiler read |
|---|---|---|
| Noise | raw signal only | reject as lawful carrier; maybe classify as raw input |
| Data | some structure, not meme-complete | parseable but not carrier-valid |
| Meme | minimum shape holds | valid carrier |
| Typed Meme | one or more outward type/interface laws bind | valid carrier + interface bundle |
| Canon Typed Meme | external ratification confirmed | valid carrier + stronger trust policy |
| resolved | outward child yields | depth claim satisfied |
| declared-unresolved | child declared, not yet yielding | backlog / forward reference |
| absent | no outward claim | shallow but honest |

### Compiler implication

`meme` should compile first into a **carrier validator** and **rating/depth classifier**, not directly into deep AST nodes.
Its first yield belongs to metadata, conformance, and routing.

---

## Task 023 — Invariant pressure, cacheability, and sidecar boundary law

### Invariant surface table

| Surface | Current law pressure | Compiler check | Artifact consequence |
|---|---|---|---|
| short / exact / stable / cacheable | invariant stays compressed | token/section budget and cacheable flag | boot-core candidate |
| visible ahu | invariant keeps local worksite visible | ahu present | render/inspection anchor |
| compressed OODA-HA | six-glyph loop stays intact | compact OODA-HA present | operational summary |
| active operational voice | no passive drift into exposition | prose register heuristic | style/conformance signal |
| no wiki/policy/instruction-book drift | explanation travels outward | sidecar split check | keep invariant in boot packet |
| always-on laws stay here | core law lives in invariant | law/reference distinction | boot priority |
| procedures move outward | sidecars hold bulk procedure | outward references present | sidecar routing |
| `???` only in template root | downstream invariants must fill slots | template detection | reject placeholder leakage |

### Compiler implication

`invariant` should compile into:
- **boot-priority selection**
- **cache policy**
- **sidecar split validation**
- **template vs filled-in invariant distinction**

It should also shape resource annotations such as high priority for invariant packets.

---

## Task 020 — Memetic-wikitext primitive cluster and lowering boundary

### Primitive cluster

| Primitive | Lawful job | First lowering target | Must not collapse into |
|---|---|---|---|
| `ahu` | local worksite / anchor / context | scoped block or container node | generic text blob |
| `loulou` | outward relation sugar | `pranala` relation edge | inline transclusion |
| `aka` | passive inclusion / shadow reveal | observe edge or passive include node | active invocation |
| `kahea` | active invocation / live transclusion pressure | dataflow edge or invoke node | passive include |
| `kapu` | qualification / trust / boundary / disclosure | qualification node / annotations | execution node |
| `ui` | query surface | query node | plain prose |
| `hana` | guest-work bounded site | guest region node | whole-document grammar swap |
| `?` | explicit unresolved pressure | unresolved token / residue node | implicit failure |

### Boot grammar atoms and constructions

| Layer | Current law pressure | Compiler use |
|---|---|---|
| atoms | opener, closer, route arrow, anchor, `?`, TOML payload, control sigils | lexer/tokenizer contracts |
| constructions | document open, body thresholds, relation/include/invoke, qualified act, query/response, uncertainty discharge | parser entry contract |
| handoff boundary | parse legality and render machinery travel outward | parser/render separation |

### Compiler implication

`memetic-wikitext` should compile into:
- lexer rules
- primitive-aware parser rules
- AST seed families
- explicit parser/render handoff boundaries

---

## Task 021 — Loci-derived carrier-tree and child-resolution table

### Resolution and tree law

| Concern | Current law pressure | Compiler use | MCP use |
|---|---|---|---|
| root eligibility | `\w.\w.\w` root required | validate URI eligibility | resolver errors |
| stable origin | `ha.ka.ba` -> `packages/lares-core/memes/` | canonical local root | local resources |
| unstable origin | other roots -> `lares/chapel-perilous-opens/{root}/` | alternate resolution path | future remote/unstable resources |
| all-caps root | `AGENTS`, `LARES`, `INDEXES` | adjacent root namespace | root instruction files and virtual compiler resources |
| primary carrier path | derive from root + subpath | URI -> filepath resolver | `resolve_lar_uri` |
| virtual index path | `lar:///INDEXES/**` | materialized index resource, not necessarily disk-backed | carrier/interface/invariant resource reads |
| child item path | sibling dir named for parent terminal segment | parent-child carrier tree index | `read_carrier_tree` |
| resolved | file exists | resolved state | resource available |
| declared-unresolved | path absent but URI stands | forward reference index | backlog / unresolved resource |
| address stability | opener + filepath coherence + register | stronger trust | cache and promotion policy |

### Compiler implication

`loci` should compile into:
- the URI resolver
- parent/child carrier-tree index
- declared-unresolved forward-reference index
- resource templates and namespace planning

---

## Task 024 — Implements/interface composability table

### Current observed interface patterns

| Bundle pattern | Observed examples | Reading |
|---|---|---|
| `meme + loci` | `loci`, `loci/iam`, several skill carriers | minimum lawful typed carrier with stable address |
| `meme + loci + invariant` | `meme`, `invariant`, `memetic-wikitext`, `pranala`, `lar-uri`, `e-prime`, `ooda-ha`, `guest-grammar` | stable carrier that also claims compressed/core pressure |
| `meme + loci + <specific interface>` | many `SKILL` and law-specific carriers | composable ano meme, interface family extends base bundle |

### Interface-bundle rules implied by current corpus

| Rule | Evidence | Compiler implication |
|---|---|---|
| `implements` names real outward type laws | `meme` says an ano meme may satisfy several interfaces at once | validate against actual law surfaces |
| bundle composition stays additive | `loci` says implementing one interface does not foreclose others | multi-interface routing |
| interface truth matters | `meme/SKILL` warns to keep `implements` truthful | no decorative capabilities |
| `loci` often acts as the routing floor | most stable carriers include `loci` | routing/resource baseline |
| `invariant` acts as pressure/compression modifier | many root laws add `invariant` | boot-priority and sidecar policy |

### Compiler implication

`implements` should compile into an **interface bundle index** with at least these uses:
- validate declared interfaces against actual carrier law
- route by interface family
- expose MCP resources/tools filtered by interface
- keep interface distinctions visible instead of collapsing all typed carriers into one class

---

## Recommended compiler order

1. validate `meme` copy-shape
2. validate `invariant` pressure/cacheability where declared
3. extract `implements` interface bundle
4. resolve `loci` and parent-child tree
5. parse memetic-wikitext primitives
6. lower `pranala` edges
7. lower AST -> execution -> render

That order should keep carrier truth ahead of syntax swell.

---

## Immediate implementation slice

The next concrete slice can now begin with five bounded worker outputs:

1. **carrier validator** from `meme`
2. **invariant validator** from `invariant`
3. **interface bundle indexer** from `implements`
4. **URI/tree resolver** from `loci`
5. **primitive-aware parser seed** from `memetic-wikitext`

Those five should provide the smallest truthful substrate for the hydration compiler.
