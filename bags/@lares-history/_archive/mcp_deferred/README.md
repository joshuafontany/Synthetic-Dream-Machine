> [!IMPORTANT]
> Consumption status: fully-consumed on 2026-04-23.
> Canonical loci-meme: `lar:///ha.ka.ba/@lares/docs/lararium_mcp/spine`.
> This non-meme markdown source may become a safe-delete candidate after link checks confirm no required references remain.

<!-- <<~ !DOCTYPE = lar:///ha.ka.ba/@lares/api/v0.1/pono/memetic-wikitext >> -->

# MCP — Lararium Delivery Shelf

Status date: **April 23, 2026**
Backlog prefix: `MCP-*`
Current pressure: **contraction / crucible pass**
Primary target: a fully hydrated Lararium MCP layer with the altar fire lit, built from the repo's current graph, current meme/invariant/memetic-wikitext carrier trees, `loci` law, composable `implements` interface bundles, and current git submodules.

## Crucible rule-set

This shelf now runs under a tighter constraint set than the first expansion pass.
Only the following program lines survive into the near-term core:

1. **All current git submodules enter the MCP program as core pieces.**
   None remain ornamental, deferred, or concept-only.
2. **The Lararium graph remains canonical.**
   MCP compiles from `lar:///...` truth, meme copy-shape law, invariant pressure law, memetic-wikitext carrier trees, `loci` derivation law, and truthful `implements` bundles rather than replacing them.
3. **TiddlyWiki Filter Language enters directly.**
   We do **not** import TiddlyWiki as the runtime center; we import the Filter Language as guest grammar and as the primary self-describing / self-booting graph example.
4. **Read-only v1 stays the gate.**
   Write actions wait until read paths, safety, and closure traces settle.
5. **Tools remain the portability floor.**
   Resources and prompts stay first-class where clients support them.

## Current program frame

The MCP layer now points toward five hard deliverables:

- a hydration compiler over the Lararium graph
- a carrier-tree, meme/invariant-shape, interface-bundle, and loci index over `packages/lares-core/memes` source surfaces
- a submodule integration layer across all current submodules
- a local stdio MCP server for IDE surfaces
- a remote HTTP/SSE MCP server for remote surfaces

Resolver law now includes all-caps roots adjacent to tuple roots:
`lar:///AGENTS` and `lar:///LARES` resolve as file-backed root carriers,
while `lar:///INDEXES/**` names virtual compiler resources whose child
paths may use any case.

## Submodule core set

- `mempalace` — storage / retrieval / memory substrate
- `kowloon` — event, social, and feed backend substrate
- `kowloon-client` — client bridge for Kowloon operations
- `kowloon-frontend` — operator-facing workflow and UI reference surface
- `tldraw` — canvas / visual graph / MCP app and render target lane
- `tiddlywiki5` — direct Filter Language source and self-booting graph exemplar

See `SUBMODULE_INTEGRATION_MATRIX.md`.

## Talk Story / Refine-Consume / High-Manaoio protocol

This shelf now prefers three working moves:

1. **Talk Story first**  
   Early planning may remain conversational, comparative, and example-rich while the shape still searches for its right boundary.

2. **Refine-Consume second**  
   Later passes should consume local repo truth into tighter carriers, tables, matrices, and contracts rather than expanding fresh speculative prose.

3. **Promote high-manaoio actions**  
   Work should rise in priority when local resolution can repeat reliably, operator crossings stay bounded, and the gap between enacted and imagined work stays visible.

See `TALK_STORY_PROTOCOLS.md`.

## Shelf map

```text
mcp/
  README.md
  ROADMAP.md
  BACKLOG.md
  WORK_TRACKING.md
  ARCHITECTURE.md
  TALK_STORY_PROTOCOLS.md
  SUBMODULE_INTEGRATION_MATRIX.md
  research/
  epics/
  sprints/
  stories/
  tasks/
  subtasks/
  templates/
```

## Reading order

1. `ROADMAP.md`
2. `ARCHITECTURE.md`
3. `TALK_STORY_PROTOCOLS.md`
4. `SUBMODULE_INTEGRATION_MATRIX.md`
5. `research/RESEARCH_BRIEF_2026-04-23.md`
6. `BACKLOG.md`
7. active sprint file in `sprints/`

## Working agreements

- contraction beats expansion
- talk story may open; refine-consume should close
- repo truth outranks platform fashion
- high-manaoio actions outrank broad speculative connector language
- every submodule must contribute a named runtime, protocol, schema, UI, or test role
- TiddlyWiki Filter Language import stays direct; full TiddlyWiki runtime import stays out of v1
- no hidden TODO stays only in chat
