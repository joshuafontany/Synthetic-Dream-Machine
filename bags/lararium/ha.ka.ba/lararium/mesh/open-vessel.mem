<<!DOCTYPE memetic-wikitext+tiddlywiki lar:///ha.ka.ba/lares/api/pono/memetic-wikitext >>

<<^ code:"&#x0001;" ? -> lar:///ha.ka.ba/lararium/mesh/open-vessel >>
```toml iam
cacheable   = true
file-path   = "bags/lararium/ha.ka.ba/lararium/mesh/open-vessel.mem"
hydrate     = true
l-space     = "lararium"
mana        = 17
manao       = 16
manaoio     = 14
register    = "Synthesis"
retain      = true
role        = "The Vessel — one clean composable web3-protocol design: a vessel composes from sovereign protocol participations named by its KEEL (VesselKeel); openVesselCore owns the sequence, the keel supplies the substrate atoms; role rides as a capability bundle, never a platform. `recipe` names the pinned wiki's composition and nothing else."
source-file = "packages/lararium-mesh/src/open-vessel-core.ts"
type        = "text/x-memetic-wikitext+tiddlywiki"
uri-path    = "ha.ka.ba/lararium/mesh/open-vessel"
```

<<^ code:"&#x0002;" >>

! The Vessel — One Composable Web3 Design

The authority contract lives in [[operator-peer|lar:///ha.ka.ba/lararium/mesh/operator-peer]] (two-lane law, ea, actor-parity).
This meme owns the ''compositional structure'': what a vessel composes FROM, and the
ONE factory shape both platforms instance (see #one-derivation).

<<~ ahu #the-law >>

!! The Law

A vessel composes from ''sovereign web3-protocol participations'' — it never reads as
an application with platform branches. The factory derives by SUBTRACTION: the lines
that differ between the working hosts name the shore, and nothing else earns one
(Rule-of-Three). The core holds zero platform code; no `if (platform)` enters it.

!!! Same structure, differing capabilities (Ink & Switch)

Node and browser ''differ ONLY in capabilities, never in structure or''
''pattern-integrity'' (Ink & Switch). The composition — the seven participations, their
order, the protocol sequence — stands IDENTICAL on every substrate. What differs reads
as which ''capability pieces a vessel currently holds'', plus the few genuinely
substrate-bound resolutions inside each piece (storage backend, worker kind, crypto
provider, transport). A substrate constrains how a piece resolves; it never removes the
piece from the structure.

''Hold the convergence open (MUST).'' The capability set GROWS. Many capabilities wired
on node today — founding/init, PersonaGroup ceremonies, genesis authoring, corpus
loading, residency — SHALL become composable in the browser. The design therefore
treats no capability as structurally node-bound: each rides as a piece any vessel MAY
compose once it holds the capability, never an `isNode`/`isBrowser` fork and never a
permanently-absent slot. A capability absent today reads as //not-yet-held//, never
//cannot-hold//. The shore for every such piece stays open on both substrates.

<<~/ahu >>

<<~ ahu #the-seven-participations >>

!! The Seven Participations (what a vessel composes)

Each names a sovereign protocol already standing in the tree — the vessel COMPOSES
them, it does not re-implement them.

| # | Participation | Protocol | Source |
|---|---|---|---|
| 1 | ''Identity'' | keyhive / did:key — keypair + self-certifying ContactCard | [[operator-peer|lar:///ha.ka.ba/lararium/mesh/operator-peer#actor-parity]] (light leaf identity) |
| 2 | ''Replica'' | Automerge Repo = own log + own storage scope; no global now | [[causal-island|lar:///ha.ka.ba/lararium/mesh/causal-island#substrate-not-sovereignty]] |
| 3 | ''Peering'' | NetworkAdapter + inbound-access policy as a ''capability'' | V3 peer-auth (auth-wire); sharePolicy |
| 4 | ''Recipe + residency'' | CompositeStore bag-cascade + residency tiers | [[causal-island|lar:///ha.ka.ba/lararium/mesh/causal-island]], residency-tiers, disk-projection |
| 5 | ''Sovereign islands'' | TW5-in-Worker VM pool; syncPort per island | [[project_no_vm_on_main_thread]], island-protocol |
| 6 | ''Admin island'' | authn/z home; keyhive-in-worker; verb dispatch + binding | verify-then-delegate; admin-vm-core |
| 7 | ''Verb plane'' | verb-tiddler invocation → outcome (UCAN-invocation shape) | verb-tiddler; [[project_asymmetric_peer_handoff]] |

<<~/ahu >>

<<~ ahu #the-shape >>

!! The Shape — A Vessel Composes Its Pieces

A vessel does not read as a core that an injected host-port drives. A vessel ''IS a''
''composition'' — a ''KEEL'' (`VesselKeel`) that names which sovereign pieces it carries
and the order they assemble. ''The word `recipe` belongs to the pinned wiki'' (operator
ruling): a wiki's recipe names the bag cascade it composes from, and letting the vessel
borrow it put one word across two layers that answer to different hands. What an operator
ASKS a vessel to stand as rides its own name — `AskedStanding` — and `standAs` decides
what it actually stands as from whether the archive opens. The open phases (`boot → repo-open → catalog-ready → island-ready →
wiki-ready → vessel-ready → corpus-ready → tw5-booted → live`) mark the composition
milestones, gating on the admin island's `ea` before any wiki mounts (S11.5
admin-first). The same shape composes one level down (admin-vm, sovereign-kernel,
island-pool already compose this way; automerge-repo composes its Repo from pieces
too) — this composes one level up.

''The substrate resolves native-first INSIDE each piece, not in a host tower.'' The
platform difference lives within the sovereign piece that owns it — the Replica piece
resolves NodeFS-or-IndexedDB; the Identity piece resolves disk-or-IDB; the Islands
piece resolves worker_threads-or-Web-Worker. No fat `Host` interface sits between a
core and the platform. What the composition receives reads as a handful of plain
VALUES + closures, each held by its own piece, and that set derives by SUBTRACTION —
only the lines that actually differ between two living vessels earn a place, nothing
speculative (Rule-of-Three). Conformance proves BEHAVIORAL (both vessels walk the
phase sequence), never type-conformance to a port.

<<~/ahu >>

<<~ ahu #role >>

!! Role Names Which Pieces the Keel Carries (the keystone reframe)

''Role names the capability pieces a vessel CURRENTLY HOLDS'' — a snapshot of a growing
set, decoupled from the runtime it runs on ([[feedback_isomorphism_by_composition]]). A
role adds or omits a PIECE; it never toggles a flag or fills an optional port method,
and a substrate never stands in for a role: node-means-relay reads as a category error.
The substrate NEVER gates which pieces a vessel MAY hold (Ink & Switch, #the-law): a
piece absent today reads as not-yet-held, never cannot-hold.

* The ''relay'' keel carries, in addition to the seven base pieces: the inbound-peer
  gate (V3), the disk-mirror grant, the full resident-verb plane, the residency
  sweeper.
* The ''leaf / keeper'' keel simply OMITS those pieces and carries the outbound
  peer-handshake instead. An omitted piece reads as absent from the cascade — no
  no-op stub, no `if (platform)`, nothing for the composition to branch on.

A node MAY compose a thin leaf keel; a future heavy browser MAY compose relay
pieces — because role lives in the keel, not the substrate. ''The access piece is''
''always present, never a default'': the relay keel carries the V3 gate piece; the
browser keel carries the `admit same-origin / in-process` piece (its only peers ride
the MessageChannel to its own island + the same-origin BroadcastChannel — trusted by
origin, a LEGITIMATE asymmetry, not an open hole). No keel omits the access piece, so
an accidental open-to-all reads as unreachable by construction.

<<~/ahu >>

<<~ ahu #one-derivation >>

!! One Derivation ~ what a platform MAY decide, and what it MAY NOT

Both factories reduce to thin ''KEELS'' — each naming its pieces (the shared seven plus
its role pieces) and the few substrate values each piece resolves native-first. The
protocol ''SEQUENCE'' holds invariant across them, and the node + browser suites boot real
vessels to guard it.

''A FACT ABOUT THE OPERATOR IS NEVER A PLATFORM DECISION.'' The bags a vessel registers
with its capability ring derive from what that vessel HOLDS — its own ground, its wiki
slot, and the union over the PersonaGroups it stands in, each contributing its plane and
every bag its catalog names (`register-bags`, `persona-scope`). A keel MAY carry a
platform's own pieces; it MUST NOT carry its own answer to a question about the operator.

''DEIXIS RESOLVES ONCE, AT THE BOOT PATH.'' A vessel reads its own sentinels, derives the
absolute name of the PersonaGroup plane it stands in, and carries that name onward —
into the mount, the registration, the registry lookup and the daemon VM. No layer beneath
re-derives it and none receives a gesture; the bootstrap carries the name beside the url
(`open-vessel-core`, and canon at persona-circle#the-plane-name).

''Why the rule bites here in particular.'' Keyhive's bag→doc map lives in process memory,
so a bag absent from that set can never satisfy a cap check — no throw, no warning, an
`act LOAD` that refuses forever. Two hand-written lists of one fact drift, and the one
carrying authority drifts ''silently''; a derivation cannot.

<<~/ahu >>

<<~ confidence 14/20 >> The design reads pono (composition, not interface;
role-as-capability; native-first by subtraction), and the node + browser suites boot real
vessels through the phase sequence, so the invariant carries a live guard rather than an
argument. What stays open sits one level out: a live-daemon exercise of the full sequence.

<<~/ahu >>

<<~ loulou lar:///ha.ka.ba/lararium/mesh/operator-peer >>
<<~ loulou lar:///ha.ka.ba/lararium/mesh/causal-island >>
<<~ loulou lar:///ha.ka.ba/lararium/mesh/island-protocol >>
<<~ loulou lar:///ha.ka.ba/lararium/mesh/lar-vessel >>

<<^ code:"&#x0003;" >>ni:///sha-256;oJxJlcoewe2o5kr2cKP_KMekkdcM9WACCpGejOYDzHM

<<^ code:"&#x0004;" -> ? >>
