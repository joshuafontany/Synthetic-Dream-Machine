<<!DOCTYPE memetic-wikitext+tiddlywiki lar:///ha.ka.ba/lares/api/pono/memetic-wikitext>>

<<^ code="&#x0001;" namespace="⚕" from=? -> to=lar:///ha.ka.ba/lararium/mesh/founding-runbook>>
```toml meta
cacheable  = true
file-path  = "bags/lararium/ha.ka.ba/lararium/mesh/founding-runbook.mem"
l-space    = "lararium"
mana       = 15
manao      = 15
manaoio    = 14
namespace  = "&#x2695;"
retain     = true
revised-on = "2026-08-09"
role       = "the go-live founding rite, in TWO BRANCHES: a REHEARSAL founding (containers, disposable, repeated until it bores) and a KEEPER founding (the real hearth, once, irreversible) — they share every movement but the clear. The exact CLI sequence that rehearses, clears a vessel fresh-from-void, stands the device + three founding kahu (SELF-STOOD or CONTRACTED, per the True Name Model), forges + custodies the pre-rotation reserve (compromise-resistant Shamir 2-of-3 cards), seals + backs up the vault, regenesizes, goes LIVE, stands a Herm crossroads where a family needs one, and grows the Nexus. Carries the reserve-custody independence rule + the recovery-cost frame + the identity-class split + the founding-mode ⊥ carriage-contract disambiguation, and serves as the OPERATOR-INSTRUCTION SOURCE a reader (human or agent) resolves the next command from. Operator-ruled 2026-07-20; branched + True-Name-aligned 2026-07-27; THIRD PASS 2026-08-06/07 re-points the seat join from the PRIVATE pet-name to the DECLARED HANDLE (three acts per founding command: --name labels privately, --handle declares outward, --seat stands for a chair), gathers the names onto the fleet via `persona sync`, RETIRES the built-in founding names (the roster forms from what stood; the threshold derives majority), re-homes the seal per-operator at <lares>/nexus, and lands the @nexus LIFECYCLE (born by act · lives by ley-line traffic · quiet by absence — no admin capability anywhere; succession rides ROTATE under the standing pre-commitment) plus the bag-HOME axis (repository | hearth | ley, fail-closed to hearth). FOURTH PASS 2026-08-08: ① names what the clear MUST NOT touch (the acquired library and the repo registry survive every founding — the library rebuilds from NOTHING); a founder meets two new surfaces (#/bags-and-shelf) — every bag declares itself (cap-tier ⊥ home, DISK-OWNED so no wiki edit can re-home a bag) and the acquired shelf holds books outside every tracked tree, referenced by collection name rather than path. FIFTH PASS 2026-08-09: the ground CLEARS — deps brought current (build green, 1682 specs + 3318 tests, zero failures), the AI-instruction pointers outside the repo repointed off a dead path generation, ⓪′ preflight gains the playwright-binary leg and reports a WALK, ④ gains the two passphrase env vars with the shell-history hygiene that keeps a seal worth sealing, and ③′ gains the three questions that decide a guardian name plus the ruling that guardians MAY stand unassigned at founding. Carries one RULED rename: `hoʻāla` fails its lexicon check — Māmaka Kaiao already coined it for `to format, initialize, as a computer disk`, the inverse of the preserving waking-floor it named here, and it sat a few lines from the irreversible clear; the concept and its grounding stand and the pet-name falls — it now reads THE WAKING FLOOR, plain English, after `ho'omaka` (spent on `start a computer program`), `kahua` (spent on `database field`) and `paepae` (free, but already carrying that exact metaphor for a Native Hawaiian nonprofit) each failed the same check. WALKED 2026-08-09: every movement stood across two cycles and this meme needed NO amendment, which the rite sets as its own bar — a silent document rather than a green report; four of the five failures that got there were INSTRUMENTS (an impatient wait, a seed carrying another founding's address book, a gate inspecting where it had to connect, a probe that rebuilt what it measured) plus one silent misroute a green suite reported as fine. Also lands the BOOTSTRAP RE-HOME (operator-ruled): the social bootstrap — one vessel's address book — leaves the corpus root for <lares>/vessel, beside the docs it addresses and inside the wipe zone, so it dies with them rather than by a wipe-list naming it; genesis/ carries the baked island ALONE and a seed becomes safe to copy. And the HOME SPLIT (operator-ruled): THREE WORDS, NO SYNONYMS — lares names the SPIRITS, lararium names the HOUSE, lar names the NAMESPACE — and every siting answers WHOSE IS THIS. LARES PASS, THE LARARIUM ABIDES: the acquired library and the sensoriums are the house's and stand at <lararium> (~/.local/share/lararium), while identity, seal, repo registry, hearth bags and the vessel substrate are the spirits' and stand at <lares> (a Lar's keys ARE that Lar), and watermarks alone stand at <state> (delete at will). Back up <lararium> and <lares>; the wipe zone standing STRUCTURAL is the consequence, since a tier held by its own house leaves no list to widen by a path segment."
status     = "rite"
status-why = "the founding rite, in two branches — rehearsal and keeper; WALKED end-to-end 2026-08-09, two cycles, no amendment"
tags       = ["mesh/carry-contract", "api/pono/persona-policy", "mesh/siege-resilience", "mesh/charter-chain"]
type       = "text/memetic-wikitext+tiddlywiki"
uri-path   = "ha.ka.ba/lararium/mesh/founding-runbook"
written    = "2026-07-20"
```

<<~ aka lar:///ha.ka.ba/lares/api/pono/RFC-2119#/normative-language>>

<<^ code="&#x0002;">>

! The Founding Runbook ~ the go-live rite

> A woken operator runs this rite to stand a node fresh-from-void, found the three-kahu
> quorum, arm the charter's pre-rotation, and go live. Each step reads as its own verb; the
> irreversible one wears a warning. The rite runs top to bottom once — a re-run re-founds.

<<~ ahu #the-rite>>

!! The rite ~ rehearsed first, then the six movements, then the growing

<<~loops ⓪ rehearse -> ✶ clear -> ⏿ found -> ◇ reserve -> ▶ seal-and-back-up -> ↺ seed -> LIVE -> ⚲ crossroads -> ⊕ grow -> ⊛ gather -> ⊙ feed -> ⊘ dwell>>

''Rehearse'' runs the whole rite in containers, disposably, until it bores (#/rehearse) · ''clear''
pares the vessel to void · ''found'' stands the device + the three kahu · ''reserve'' forges the
pre-rotation's future-key custody · ''seal-and-back-up'' locks the secrets at rest + writes a
recoverable copy · ''seed'' plants the content from `bags/` · ''LIVE'' serves ·
''crossroads'' stands a Herm where a family needs one (#/crossroads) · ''grow'' opens the Nexus,
contracts fellow operators, and welcomes users at the anon floor (#/grow-the-nexus) · ''gather'' brings the
operator's own vessels in as contracted leaves (#/gather-the-fleet) · ''feed'' lays the first offering, which
IS the realm's founding (#/feed-the-realm) · ''dwell'' seats a second operator IN the realm rather than
merely carrying for it (#/dwell).

''The last three movements run AFTER LIVE and never gate it'' — and they carry different readiness. ⊛ Gather
runs today on built verbs, ⊙ Feed among them (its two doors landed 2026-08-07). ⊘ Dwell holds a
built vouch and a crossing that REFUSES until the operator seats the admission dials — and its conversion act
stays un-modelled by ruling, so no command there may name a quorum, a waiting period, or a test.

''THE RITE RUNS IN TWO BRANCHES, and they want opposite virtues'' (operator ruling, 2026-07-27).
A ''REHEARSAL'' founding runs in containers, disposably, as many times as it takes — cheapness IS
the point and nothing stands worth backing up. A ''KEEPER'' founding runs once, on the real hearth,
irreversibly. ''Movements ②–⑨ read the same in both''; the branches part at ① and at what a failure
COSTS. Every rehearsal therefore doubles as a dress rehearsal of the keeper — same commands, same
order, disposable stakes — which stops the keeper from ever being a first take.

''THE RITE STANDS ONE VESSEL; THE ARC RUNS WIDER.'' What happens across several vessels, several operators and however long the months take — a realm FED after LIVE, a fleet gathered as contracted leaves, a second operator founding independently and then DWELLING IN the first realm — rides at <<~ loulou lar:///ha.ka.ba/lararium/mesh/first-realm-arc>>. It names three stretches this rite does not walk: the fleet join (② names the CONTRACTED mode and walks only SELF-STOOD), the realm feeding (no movement names the first offering, and `feedCabalRealm` stands built and unwalked), and the dwelling join (⊕ Grow contracts CARRIAGE and seats no DWELLER — carriage and dwelling ride orthogonal axes).

''Read this rite as the operator-instruction source.'' Each movement names its branch, its exact
commands, and its precondition, so a reader (human or agent) can answer //what do I run next// from
the vessel's current state rather than from memory (#/agent-seed).

<<~/ahu>>

<<~ ahu #rehearse>>

!! ⓪ Rehearse ~ run it in a box until it bores (the REHEARSAL branch opens here)

''A ceremony nobody has performed carries no evidence, only intent.'' This rite stood `proposed` and
unrun while the code beneath it moved for a week — so the first movement stands one that costs
nothing to repeat. The harness already stands: `docker-compose.mesh.yml` founds ''real'' identities
in containers (`tools/lararium-container-boot.sh` — //founds its OWN identity, then composeHerm//)
with two Herm relays on a shared network.

```
docker compose -f docker-compose.mesh.yml up --abort-on-container-exit   # found, in a box
tools/herm-mesh-witness.sh                                               # witness the crossroads carry
docker compose -f docker-compose.mesh.yml down -v                        # THE CLEAR — no backup, nothing kept
```

''The burn IS the clear, and that inverts the danger.'' Where the keeper branch opens on the most
destructive command in the house, the rehearsal branch opens on one whose whole virtue reads
//repeatable//. Run it until the sequence bores; ''boredom names the readiness the keeper needs''.

!!! The KEEPER rehearsal ~ the sequence this branch never ran

''The container harness stands the MESH branch'' — it founds vessels with `lares vessel found` and witnesses Herm
carry + partition. ''Movements ②–⑥ were never run end-to-end by anything'': persona → nexus seal → vault →
regenesis → live had no green baseline to diff a failure against. `tools/rehearse-keeper.sh` gives them one.

```
tools/rehearse-keeper.sh                             # 2 cycles, throwaway tree, burns on exit
tools/rehearse-keeper.sh --cycles 5 --keep           # run until it bores; keep the tree to inspect
```

''THE REHEARSAL NOW WALKS ⑦ — the door a second operator comes through.'' Movements ②–⑥ prove a hearth
standing ALONE, and a Nexus that cannot admit a second operator is a private vessel wearing a charter. ⑦
runs both halves of the handshake with held personas (`accept-carriage` mints the joiner's contract-in,
the quorum counter-signs it onto the members board, `revoke` supersedes) ''and the refusal'': a nym with
no contract-in must fail closed and write nothing. A gate only ever shown saying yes has not been shown
to be a gate.

''AND THE CUSTODY CROSSING HAS ITS OWN WITNESS.'' ⑦ signs with a persona the same vessel holds, so the
one property the ceremony exists for — that a Nexus admits a key it has ''NEVER HELD'' — goes unmeasured
there. `contract-witness` stands TWO roots that share nothing and crosses it for real:

```
tools/contract-witness.sh                            # two independent foundings, one handshake
npm run witness                                      # every witness, never short-circuiting
```

''It also surfaces a step one vessel hides.'' `accept-carriage` reads the roster from the JOINER'S OWN
seal home, because a contract-in binds to the charter epoch it consents under — so ''B cannot consent to
a charter it has never seen'', and A's `founding-roster.mem` (public material: seated keys, threshold,
epoch lineage) must travel FIRST. Holding it lets B name the epoch; it grants B no quorum, because the
public charter is not a key.

''AND THE JOINER KEEPS WHAT SHE SIGNED.'' A relation has two sides and ''each side holds its own
evidence'': the founder's is the admit on her members board, the joiner's is her own contract-in.
`accept-carriage` writes that consent beside the charter it binds to, so a vessel reads the relation
it stands in ''without holding any partner's document''. The record is EPOCH-BOUND — a consent rooted
behind the standing epoch consented to terms that have since moved, and grants nothing.

''AND THE RECORD PROVES ITSELF.'' Disk holds no trust: `LAR_ROOT` names the whole seal home, so a
reading that trusted the file's PRESENCE would report a Nexus a vessel never joined. Three things
carry it — ''the epoch stands'', ''the seal verifies'' (it binds nym and epoch together, and only the
holder of that nym's seed can make it), and ''the nym is a root THIS vessel holds''. The third is the
one that hides: another operator's consent is genuine evidence that //she// joined, and copying it
would claim a relation somebody else entered. Trust rides the signature, never the location.

''The members board records, and each vessel decides.'' A stamp is a ''shared contract into the Cabal'' —
a public, quorum-countersigned fact about who stamped in — so the board belongs on the per-Nexus
shared side beside the KEL, the antigen, who-face and crossroads. ''Whether THIS vessel carries for a
given member stays this vessel's own reading OVER that record.'' Sharing a record conscripts nobody;
fusing the record with the decision would, because an admit only ever WIDENS whom a vessel carries
for and an operator consents at ONE epoch, never to every admit made after it.

⚠ ''What an artifact that TRAVELS may carry.'' A charter travels before a partner can consent, and a
shared board carries the stamps that contracted in — so whichever key class they name is ''published,
and a key published once cannot be unpublished''. Three key classes stand and they do not
interchange: a ''vessel key'' is device-minted and must never co-surface; a ''PersonaGroup root'' binds
one human's own devices into an overlay; a ''veiled-user Handle'' is the sovereign pseudonym presented
through the veil, and it is the only one of the three meant to be seen.

''The Handle carries the same verification and none of the exposure.'' A PersonaGroup root on a
travelling artifact names that human's device-group — and several drawn from one vault correlate to
each other, which is what a founding seat does when it draws every chair from one identity home. An
unrecognised key reads UNKNOWN rather than safe: a vessel cannot classify a key it did not mint, and a
travelling artifact is the wrong place to guess.

⚠ ''AND PUBLISHABLE IS NOT THE WHOLE QUESTION.'' A Handle is MEANT to be seen — a HandleCard announces
it, and recognition needs it ''stable''. That same stability ''correlates a human across every circle
it stands in''. The persona tree already carries the floor one level down: `m / handle' / context' /
circle-scope'`, the scope index derived from the circle's own doc id, so ''the same persona presents a
different key to each circle it joins'' and rejoining the same circle returns the same key.

''An artifact scoped to ONE circle wants the SCOPED leaf.'' A stamp re-keyed to a bare Handle would
drop the device-overlay and keep the cross-circle link — a fix shaped like one.

''An ISLAND takes its own domain, never the circle's.'' Canon sets the shape — //the name DERIVES from
the compartment's own material; each plane answers to a domain-separated MAC over that PersonaGroup's
own doc id// — and a circle names a human's own compartment where a Nexus names an island several
operators share. One MAC domain across both would let a compartment leaf collide with an island leaf,
which the levels stay apart to forbid. An island answers to its ''genesis epoch AID'', content-addressed
over the seated key-set, so the leaf derives from the island's own material and from nothing a vessel
owns.


''ONE ENV VAR ISOLATES EVERYTHING.'' `LAR_ROOT` re-roots data, state, the seal, the acquired library, bags
and genesis — eleven by re-siting, and the UDS socket ''by DERIVATION'': the rendezvous stands at
`/tmp/lares-<uid>/<digest-of-root>.sock`, OUTSIDE every root and every login session, because a lararium
serves as civic infrastructure and its daemon outlives the operator logging out (operator-ruled
2026-08-23) — while the digest keeps two roots off one socket. ''TWELVE of twelve'', re-measured after
the rendezvous re-siting: a clean `vessel stop` unlinks its socket; a hard kill leaves a dead name the
next contact reaps. So a throwaway costs no containers and
destroys nothing real; the container harness stays for what containers actually buy (rude sockets, real
disks, a drop that drops). ''The count gets RE-MEASURED whenever a resolver moves, never carried forward'' —
an isolation claim that ages is an isolation claim that lies, and the tenth resource (the TCP port) was
found exactly that way. The harness ''REFUSES'' any root under the operator's home, any root naming the
repo, and any root already carrying an identity — ''a rehearsal harness that can eat the hearth is not one''.

''THE SEED SET'' (operator-approved 2026-08-08): `lares` · `lararium` · `crossroads` — the two system
bags plus the public floor, re-copied EACH cycle so cycle 2 tests the ''rite's'' idempotency rather than a
warm tree's. `LAR_ROOT` isolates AND empties `bags/`, so a run that skipped this would prove less than it
appears to.

''IT RUNS THE RITE AS WRITTEN, because founding reaches nothing outside the vessel.'' A standup that also
pip-installed the mempalace library and stood the sensorium organs would write OUTSIDE `LAR_ROOT` and make
an isolated founding impossible. ''The mempalace is a SIDECAR'' (operator ruling, 2026-08-08) and its own
header always said so. Both legs carry their own doors:

```
lares mempalace install                              # the library deps (submodule + pip)
lares sense setup                                    # the sovereign sensorium organs
```

''A vessel founds and serves carrying neither.'' A stand still REPORTS what stands and counsels the verbs,
because naming a missing tool serves the operator and installing one behind them does not.

!!! ⚠ THE UNATTENDED-REBOOT TENSION ~ named at the gate, unruled

''A sealed vessel that reboots with nobody awake REFUSES TO BOOT today.'' For one operator's laptop that
reads fine; for a crossroads a family depends on, ''the at-rest seal converts a power cut into an outage''
— and the seal exists to protect a stolen disk, never to gate an ordinary restart.

The option space and its costs live beside the reading itself (`archive-passphrase#archiveOpens`), so a
later hand meets it where the decision happens rather than in a document beside it. ''BUILT NOW'': a hearth
whose archive holds shut STANDS at the waking floor as a Herm — faceless by class, carrying, serving the
public shelf, every sovereign act closed — rather than refusing to boot (`standAs`, and the boot says so in
plain words). ''What remains UNBUILT is the unattended raise'', and one correction rides with it: the raise
is a ①-VESSEL act held by whoever holds that vessel's opening secret, ''never a kahu quorum'' — a cabal is
seated from Handles standing on an already-raised lararium, so that dependency closes in a circle
(waking-floor#the-raise-is-a-vessel-layer-act). The shape that reads most pono: a vessel that cannot open its archive has lost its ''caps'', never its ''floor'' — so it
should come up ''anon at the floor'' (serving the public shelf, which needs no secret), hold every sovereign
act closed, say loudly what it cannot do, and let an ACT unseal it after. ''And that act already has a shape
the mesh runs'': a kahu quorum signing against the pre-rotated epoch chain, rather than a second authority
invented beside it.

''RULED'': passphrase-primary; no random key beside the ciphertext; never a silent degrade to weak keys.

''RULED and ENACTED'': an unattended boot ''stands at the floor''. `standAs(asked, archiveOpens)` returns the
asked class only where the identity archive opens and `"herm"` otherwise, so a vessel whose seal stays shut
meshes, carries, routes, and says loudly what it cannot do — it seats no persona
(`personaSlotCeiling("herm") === 0`). Nothing degrades silently, because the floor is a NAMED standing rather
than a weakened one: the vessel that cannot open its archive holds no key to misuse. An operator ''lights the
hearth fire'' after, and a recognised operator may RAISE caps onto it that ride their own keys and always fall
back (waking-floor). An outage recovers; a silently unsealed identity does not — and neither happens here.

A crossroads may now stand where nobody sleeps: it wakes at the floor and waits for a hand.

''TWO LEGS STILL GO UN-EXERCISED, named so a green run never reads as more than it proves'':
* `lares vessel stand --init` fans out to the AI-surface wires (`~/.claude` · `~/.codex` · `~/.copilot`). The harness
  passes `--install`, never `--init`, so ''the harness wiring goes untested here'' — a machine-setup concern
  rather than a founding one.
* the vault seals from `LARES_ARCHIVE_PASSPHRASE`, so ''the no-echo TTY prompt leg goes un-exercised''.

''What a rehearsal proves that a test cannot.'' The suites prove every property in-process, authored
by the same hand that wrote the code — the field has never disagreed. A container founding lets
sockets be rude, disks be real, and a drop actually drop: it stands the cheapest ''outside island''
available (#/the-frame). Deliberately in scope here: remote CAS transit to a holder outside this
process, bilateral genesis across two sovereign hearths, the Herm's re-key/re-share/heal teeth, and
reconnection after a real drop.

''FIRST RUN — 2026-07-27, and the rite amends itself as promised.'' Three cycles ran. ''2 green, 1 red.''
What the field said, recorded here because the run outranks the document:

# ''THE RITE'S OWN INSTRUMENTS OVERCLAIMED — twice, and neither failure reaches a test suite.'' The
  cycle runner declared `cycles green: 1/1` while BOTH witnesses failed (a bash `if` without `else`
  yields 0, so the status read the branch and never the command). The carry witness printed
  //verified at each hop// while a hop read UNVERIFIED (its gate checks the LAST hop alone). Both cured;
  the runner now ''proves its own detector'' before trusting a cycle, and refuses to proceed when a
  forced failure fails to register. ''A verdict MUST state its own scope, or a green measures the
  instrument's silence.''
# ''AN INTERMITTENT FAIL-CLOSED BINDING GATE, ~1 cold founding in 3.'' `herm-relay-2` exits 1 with
  //persona-KEL chain for the pinned identifier … absent from the local board replica — the Binding
  Gate cannot reach a head (fail-closed)//. A second identical run stands all four vessels clean. ''A
  RACE, never a structural break'' — precisely the class one test run cannot see and a repeated
  rehearsal does. Surfaced, deliberately undiagnosed: the Binding Gate sits in live work, and
  #found already names this failure shape as the cost of founding without a carried chain.
# ''WHAT PASSED, and it matters.'' On a healthy mesh the announced dial crosses source → relay-1 →
  relay-2 with the pointer signed, the snapshot hash-matched and the dial DECODED; and the partition
  witness confirms relay-2 keeps advancing after the witness CUTS relay-1 — federation riding carried dials,
  never a hardcoded peer list.

''The rite therefore does NOT yet stand WALKED.'' One movement stands amended (this one), and a red cycle
stands unexplained. ''Boredom has not arrived.''

''The rehearsal amends this meme; this meme does not predict the rehearsal.'' Where a run contradicts
a movement below, ''the run wins'' and the movement gets rewritten — the rite stands WALKED only after a
rehearsal completes end-to-end ''without amending it''.

''The bar reads as a silent document, never a green report.'' A run that
passes while teaching the rite something has not walked it — it has repaired it. So the test asks whether
this meme needed to change, which no amount of green output can fake.

!!! SECOND PASS — 2026-08-04, and the rite amends again without a rehearsal

''A reading against live code found what a run would have found, so the same law applies.'' Three
amendments landed and one hypothesis got recorded:

# ''⓪′ PREFLIGHT ADDED, ahead of the irreversible clear.'' The toolchain went unchecked before a movement
  that cannot be undone. Measured: the entry point died at import on a stale build.
# ''⊛ GATHER'S COMMANDS CONTRADICTED ITS OWN PROSE.'' The text described a joining vessel minting its own
  key and receiving a signed edge — correct. The commands signed a payload before any joinee key existed
  — impossible: the signing call refuses without the joinee's public verifying key. ''The description
  tracked the design and the sequence tracked the past'', which reads as the most dangerous shape a
  runbook carries, because the prose reassures the reader that the commands were checked.
# ''THE ADMIT PAYLOAD'S CONTENTS NAMED.'' It carries one signed edge on a lineage, never a
  capability-event bundle (#/gather).
# ''A CANDIDATE FOR THE STANDING RED, unproven.'' The unexplained fail-closed Binding Gate reports a
  persona-KEL chain absent from the local board replica. An admit payload measured 2026-08-04 carries a
  chain of ''length 1''. A one-event chain against an unsynced replica reads as a plausible
  cannot-reach-a-head shape. ''NOT reproduced'' — recorded so the next
  rehearsal watches the chain length, never as a diagnosis.

''The corroboration, with its limit named.'' The founding-then-admit path stands green end-to-end in an
automated witness (9 of 9, 2026-08-04). ''That witness is not this rite'' — it stands no containers, its
sockets stay polite, and no drop actually drops (#/rehearse). It corroborates the SHAPE of movements ②
and ⊛; it substitutes for no cycle.

!!! THIRD PASS — 2026-08-06, the seat stops reading a private label

''A reading of the rite against the identity plane found the movement-② join reaching for the wrong
name.'' The seal matched each chair against the operator's ''PRIVATE pet-name'', which welded two
registers the plane holds apart: an operator could seat only under the string they call the compartment
at home, so every private label became a public commitment by construction, and a compartment called
a compartment labelled `the-burner` could never stand under any other name.

''Nothing had leaked'' — the published string always came from the seal's own seed — so the finding names
a ''forced coupling'', never a disclosure. The correction gives the outward intent its own store and its
own act (`--handle`), and the chair claim a third (`--seat`), and re-points the join at the declared
Handle. Movement ② carries the new sequence; ⊛ gather gains `persona sync`, because both names now ride
the human's own fleet.

''What a rehearsal watches here.'' That the two registers stay ''visibly different'' — set a private
label that reads nothing like the chair name and confirm the seat still lands. A rehearsal that types the
same string into both flags proves nothing, because a label-matching join would pass it too.

''AND THE SAME READING FOUND TWO MORE, both un-pono for the same reason'' (operator, 2026-08-07):

# ''THE FOUNDING NAMES SHIPPED IN THE BUILD.'' A `FOUNDING_KAHU` constant carried three display names, so
  the SOURCE decided who the founding kahu were and the operator merely confirmed it. ''Retired.'' The
  scaffold now names nobody, and the roster forms at the seat from the personas that declared and stood.
  The threshold followed the same cut: a `2` written into a file would seat one Nexus's fairness call inside
  every Nexus that founds, so the seat derives majority and takes `--threshold` for any realm wanting another.
# ''THE PRIOR SEAL SURVIVES A CLEAR.'' `lares vessel clear --force` preserves the identity home and the `rm -rf` cuts
  it; ''neither touches `bags/`'', where the charter doc lives. A re-founding that skipped it stranded itself
  on a fail-closed lineage. ① now names the cut (#/clear).

# ''THE SEAL SAT IN THE REPOSITORY.'' Sited in the corpus bags tree, it inherited that tree's home — which on
  a development install sits inside the working tree, so a founding seal could land in a git history. ''The
  seal now homes per-operator'' at `<lares>/nexus`, beside the sovereign identity and behind the same
  reset boundary. ① cuts both (#/clear).

''The shared shape.'' Each named ''a thing decided somewhere the operator does not stand'' — a private label
deciding a public chair, a build deciding a roster, a stale artifact deciding a lineage, a directory default
deciding where a Nexus lives. ''A founding an operator merely confirms reads as somebody else's founding.''

!!! FIFTH PASS — 2026-08-09, the ground clears and a coined name fails its check

''The deps came current and the field said one thing worth keeping.'' Every runtime pin the operator approved
already stood at latest; the only live bump ran `automerge-repo` alpha.2 → alpha.3 (a `uuid` and an `xstate`
patch range, nothing else), plus the dev tooling. ''Build green, 1682 specs + 3318 tests, zero failures.''

''ONE TEST FELL, AND IT NAMED AN OLDER MISTAKE THAN THE BUMP.'' A case asserting the `lar:` root's arity
measured it by ''counting slash-segments''. Re-homing a meme one directory shallower dropped the count, so a
passing assertion turned false without the law it guarded moving at all. It now reads the arity the law
actually governs — three dot-terms — and no future re-homing can make it lie. ''A test that measures a proxy
for its law reports on the proxy'', and the day the two part company it accuses the innocent change.

''THE POINTERS OUTSIDE THE REPO CARRIED A DEAD GENERATION.'' A sweep of the machine's AI-instruction surfaces
found every `lar:` URI in `~/.claude/skills` naming `@lares/v0.1/...` — a path family that stopped existing at
the bags move and had ''dangled silently ever since''. Eighteen URIs, all repointed and each verified against
a real `.mem` before writing. Six SKILL memes in the corpus carried the same rot in `skill-package-root`.
''Transcripts and editor history stay untouched by law'' — rewriting a record to match the present falsifies it.

''A dangling pointer in an instruction file fails silently and reads
as fine'', because nothing resolves it until a reader tries — and a reader who cannot resolve it invents.

!!!! ⚠ `hoʻāla` FAILS ITS LEXICON CHECK — the concept stands, the pet-name does not

''The Hawaiian Lexicon Committee already coined `hoʻāla` for computing, and it means the opposite of ours.''
//Māmaka Kaiao// assigns it ''to format, initialize, as a computer disk'' (`hoʻāla hou` = //to reformat//).
Our sense named the ''waking floor'' — a vessel coming up at its minimum cap-stack, ''preserving everything''.

''In this rite the collision turns dangerous rather than merely awkward'': a founder meets `hoʻāla` a few
lines from the irreversible clear (#/clear), and a ʻŌlelo Hawaiʻi reader would take the two for one family.
''A name that inverts its own meaning beside a destructive command is a trap, not a label.''

''RULED'': the pet-name goes, and the concept keeps its grounding (Direct Boot BFU/AFU · SPIRE · kubelet
TLS bootstrap · Tang/Clevis) and its meme. ''It now reads THE WAKING FLOOR'' — plain English, operator-ruled,
and the phrase the prose carries throughout, so handle and body now say one thing.

''THE CHECK RAN WIDER THAN THE ONE WORD, and every near candidate failed too.'' Read against the primary
dictionaries rather than a summary: `hoʻomaka` carries ''to begin, start, as a computer program'' — spent,
and pointed at //launch// where this names a //floor//, so it fails twice over; `kahua` carries ''field, as
in a database'', a collision a Hawaiian-speaking developer would meet daily; `paepae` stands FREE in the
compute domain and reads almost perfectly (//support · prop · threshold · the plate the rafters rest on//),
but ''Paepae o Heʻeia'' already carries that exact metaphor for a Native Hawaiian nonprofit stewarding an
800-year-old fishpond, so taking it would repeat the pattern rather than mend it.

''The lesson generalizes past this name.'' Kōmike Huaʻōlelo entered
the computing domain DELIBERATELY and keeps coining in it, so a term minted from outside either collides
with a decision they made or pre-empts one they have not made yet. ''Plain English costs nothing here'' —
the house carries registers of its own (Discordian, Elyncian) where the fiction stays ours to move.
<<~ loulou lar:///ha.ka.ba/lares/api/pono/waking-floor>>

!!! ★ WALKED — 2026-08-09, and the rite changed nothing ★

''Every movement stood, across two cycles, and this meme needed no amendment.'' By the bar set above, the
rite stands ''WALKED'': not a green report, but a ''silent document''. Cycle 2 re-ran from the top on a
cleared tree, so the idempotency claim reads tested rather than asserted. 2m36s against the prior run's
4m28s, because nothing rebuilt mid-cycle.

''FOUR OF THE FIVE FAILURES THAT GOT HERE WERE INSTRUMENTS, NOT THE RITE'' — and each one had reported
green, or reported red against something innocent:

```
\procedure ~Found(Type:"" Params:"") ~Found <<Type>> holds [{{{ [<Params>] }}}]
```

<<~Found Impatience "read/no socket after 60s truth/the CLI budgets 120 for a cold boot ~ the harness out-waited what it measured">>
<<~Found Poisoned-seed "read/hearth-private doc unavailable truth/a `cp -r` carried one founding's address book into another ~ the vessel refused correctly">>
<<~Found Port-liveness "read/status LIVE truth/a fataled node keeps its port bound ~ the gate inspected where it had to connect">>
<<~Found Probe-as-act "read/the node died truth/the probe rebuilt the workspace and cleaned dist ~ a reading disturbed what it read">>
<<~Found Silent-misroute "read/1682 specs green truth/554 of 555 framed memes stopped reading as carriers ~ nothing tested detection against a swept meme">>

''The rehearsal earned its keep on the instruments alone.'' Movements ②–⑥
were sound early; what stood between them and a green run was a harness that measured its own patience, a
seed that carried somebody else's identity, a gate that inspected where it had to connect, and a probe that
acted. ''A rite proves itself only through instruments that do not lie about it'', and every one of those
lies passed a suite first.

<<~/ahu>>

<<~ ahu #nexus-lifecycle>>

!! The nexus-doc lifecycle ~ born by act, lives by traffic, quiet by absence

''THE MYTH HOLDS THE MODEL'' (operator, 2026-08-07). A Nexus names several vessels syncing across a ''ley-line
network'' — so ''no network traffic, no live Nexus''. Liveness reads off the lines themselves; nothing
declares it, and nobody grants it.

''WHICH ANSWERS DISSOLUTION WITHOUT AN ADMIN CAPABILITY.'' A Nexus is never dissolved — ''it goes quiet''. That
rhymes with every neighbouring ruling rather than inventing a new shape: a cabal-realm ''DISSOLVES BY COOLING''
and never by an act; a standing lift ''EXPIRES'' and no party holds a lowering act; //nobody dissolves a Cabal
by preventing its meetings//. ''The correct admin capability here reads as NONE.''

```
\procedure ~Stage(Type:"" Params:"") ~Stage <<Type>> holds [{{{ [<Params>] }}}]
```

<<~Stage Birth "verb/nexus-seal-seat admin/none ~ the operator's own personas declare and stand">>
<<~Stage Advance "verb/nexus-seal-rotate admin/none ~ pre-committed, quorum-bound">>
<<~Stage Succession "verb/nexus-seal-rotate admin/none ~ the roster becomes exactly what STANDS">>
<<~Stage Liveness "verb/none admin/none ~ ley-line traffic; nothing to build">>
<<~Stage Quiet "verb/none admin/none ~ the absence of traffic, and that IS the point">>

''SUCCESSION RIDES ROTATE, and needed no new verb.'' A kahu steps down by ''no longer standing''; a new one
takes a chair by declaring a Handle and standing. A rotation then seats the roster that stands — and the
''reveal must still match the prior epoch's pre-commitment'', so a roster change lands only where the operator
pre-committed that exact successor key-set an epoch earlier, in the open, under the quorum standing then.
''Nobody adds or drops a kahu quietly'', and no hand holds a capability to do it.

''You cannot change who holds the quorum without changing what the quorum is'' — which is why the succession
door was already there wearing another name.

<<~/ahu>>

<<~ ahu #bag-home>>

!! Where a bag rests ~ repository ⊥ hearth ⊥ ley

A bag already declares ''who may read it'' (`CapTier`) and ''whether it stands loaded'' (residency
temperature). Neither answers ''where its bytes rest'', and one pair proves the axis stands apart: the canon
memes and the Nexus seal read as equally PUBLIC and want ''opposite homes''.

```
\procedure ~Home(Type:"" Params:"") ~Home <<Type>> holds [{{{ [<Params>] }}}]
```

<<~Home Repository "rests/a-configured-source-control-repo clone-carries/yes ~ canon, memes, seeds — the history IS the point">>
<<~Home Hearth "rests/the-operator's-DATA-home clone-carries/no ~ identity · the seal · the follow-graph · the persona names · the acquired shelf">>
<<~Home Ley "rests/nowhere-durable clone-carries/nothing ~ it lives while the mesh carries it">>

''FAIL-CLOSED TO HEARTH, and the asymmetry carries the reason.'' Only `hearth` can be wrong quietly — a bag
that should have ridden the repository merely fails to be shared, and an operator notices. The other two fail
where it hurts: a mis-defaulted `repository` writes a private thing into a history somebody may already have
pushed, and a mis-defaulted `ley` gives a durable thing no durable home. ''The default takes the recoverable
failure.''

''WHAT ENFORCES IT, and the bypass, named.'' `resolveBagHomeDir` holds the ONLY mapping from a declared home
to a directory. A caller that ''joins its own path'' bypasses it entirely and nothing there can stop it —
which is precisely how the seal came to sit inside a repository, off a call site reading `LAR_BAGS ?? join(root,
"bags")`. So this stands as ''a mechanism with a named bypass rather than a guarantee'', and the discipline
runs: a new bag asks for its directory, and a hand-rolled `join(…bags…)` invites the question why.
''`repository` refuses outright where no repo stands configured'' — guessing a tree is the failure the axis
exists to prevent.

<<~/ahu>>

<<~ ahu #preflight>>

!! ⓪′ Preflight ~ prove the toolchain answers BEFORE anything irreversible

''Every movement below assumes a binary that loads, and the rite never checked.'' A vessel cleared by a
toolchain that cannot then found leaves an operator holding neither — the clear runs irreversible and the
founding runs unavailable, and nothing in the sequence catches it in between.

''Measured 2026-08-04'': the `lares` entry point died at import on a symbol
absent from every source and present only in a built artifact — a stale `dist` against a renamed export.
The error named an internal symbol, not a build, and an operator reading it mid-rite has no way to reach
`rebuild` from what it says. ''A full rebuild cured it.''

```
pnpm build                                          # build AND stamp — `pnpm -r build` skips the stamp
tools/typecheck-witness.sh                          # a green BUILD is not a typechecked tree — see below
lares help                                          # the binary loads and answers — the ONLY gate this proves
pnpm outdated -r                                    # bump BEFORE, never after (see below)
pnpm exec playwright install chromium               # ONLY after a playwright bump — the binaries pin to it
```

''A BUILD THAT EXITS 0 HAS NOT TYPECHECKED.'' `tsc` colorizes, so ANSI bytes sit between the word `error`
and its code — a reader grepping for `error TS`, the obvious thing to grep for, matches ''NOTHING'' while a
package carries two dozen real errors. A package whose compile never joins the recursive run passes the
same way. ''The silence reads exactly like success'', which makes it the worst shape a check can take.

''Witnessed against a live example.'' A recursive build reported no errors
while a filtered one reported a real TS2307 in a command module — same tree, same moment, two answers. The
witness reads per package and exits non-zero on any real error, so a type error cannot ride into a founding
behind a clean-looking build. ''The rehearsal harness runs it too'' — the rite and its rehearsal check the
same thing, which is the only way a green rehearsal predicts anything about the keeper branch.

''A playwright bump strands its own browsers.'' The version pins a chromium build, so the browser suite dies
at launch with //Executable doesn't exist// until the matching binary lands. Measured 2026-08-09; the first
run after the install threw one cold-start flake and two re-runs came back clean, so ''a single browser red
immediately after an install earns a re-run before it earns a diagnosis''.

''Walked 2026-08-09.'' The whole tree came current, built green, and stood
1682 specs + 3318 tests with zero failures — so this movement now reports a walk rather than a plan. The
vendored `tiddlywiki` keeps its ''own'' eslint generation: its majors belong to that submodule, and bumping
them from here moves the engine blob that rides inside every signed delegation preimage.

''BUMP THE DEPS BEFORE, NEVER AFTER — and the reason reads narrower than the fear.'' A runtime bump
(automerge · noble · ws · vite) moves ''NO epoch binding'': the engine blob is vendored TW5 and the built
plugin carries zero automerge references, both measured. It can skew the ''on-disk serde'' alone, which
`lares vessel rite rebuild` cures identity-safe. So bumping first reads as hygiene rather than a cliff.

''The surfaces that WOULD be a cliff do not move on npm at all'': `hearthTrueName` (the engine CID, signed
into every device-delegation edge) and the `pluginsCid` (built from this repo's own source). Those move by
an ACT — a TW5 submodule bump, or a commit — never by an upgrade.
<<~ loulou lar:///ha.ka.ba/lararium/mesh/epoch-binding-surfaces>>

''USE `pnpm build`, never `pnpm -r build`.'' The root script builds and then STAMPS the source digest the
freshness gate reads. A build that skips the stamp leaves the tree reading stale, so the first lifecycle
verb rebuilds ''mid-founding'' — and a build cleans nothing now, but it still costs minutes at the worst
moment. `pnpm build:clean` retires the outputs first, and even that leaves `dist.prev` to come back to.

''Run this at the head of BOTH branches.'' A rehearsal that opens on a stale toolchain measures the
toolchain and reports nothing about the rite.

<<~/ahu>>

<<~ ahu #clear>>

!! ① Clear ~ pare to void (⚠ irreversible — the KEEPER branch)

''Branch check first.'' A REHEARSAL never reaches this movement — its clear rides `compose down -v`
(#/rehearse) and it keeps nothing. What follows governs the ''KEEPER'' founding alone. ''⓪′ Preflight
stands as a precondition'', never a courtesy — it names the one failure this movement cannot undo.

A fresh-from-void founding discards the prior identity. ''Back the old identity up FIRST'' — the
vault seals nothing until movement ④, so a raw copy carries the only net before the nuke:

```
lares vessel read                                        # confirm the identity-home path
cp -r ~/.local/share/lares/identity ~/founding-backup-PRIOR   # the only pre-nuke net
lares hooks pause                                   # still the capture/ingest hooks during the pave
```

Then clear. ''⚠ The next line destroys the prior persona-roots + the keyhive archive — it does not
undo.'' A vessel that carries nothing worth keeping (the bootstrap case) crosses it freely:

```
rm -rf ~/.local/share/lares/identity                # ⚠ IRREVERSIBLE: the fresh-from-void cut
rm -rf ~/.local/share/lares/nexus                   # ⚠ IRREVERSIBLE: the prior SEAL (see below)
lares vessel clear --force                                 # pave the vessel store (the identity home now stands empty)
```

''TWO SOVEREIGN THINGS SURVIVE A CLEAR, and BOTH want cutting here.'' `lares vessel clear --force` pares the vessel store
and preserves the state home — that boundary IS the //share substrate, not sovereignty// law, and the sovereign
root sits behind it on purpose. The Nexus SEAL sits behind it too, in its own home beside the identity
(`<lares>/nexus/founding-roster.mem`), so a re-founding cuts both or strands itself: the seat ''FAILS CLOSED''
against a lineage already past genesis (//advance it with rotate, never re-seat//), and the operator holds a
fresh vessel that cannot seal.

''WHY THE SEAL HOMES THERE and not in the corpus.'' It reads as a FILE by ruling — an operator backs it up,
hands it to a peer, reads it with their own eyes — and a file sited in the bags tree ''inherited that tree's
home'', which on a development install sits inside the repository. A founding seal could have landed in a git
history nobody meant to write it into. Per-operator state cures it, and says something true besides: ''a clone
of the corpus carries NO Nexus'', because a Nexus belongs to the operators who founded it rather than to
whoever copied the code.

''A first founding crosses this freely'' — no seal stands, so the line removes nothing.

!!! ⚠ WHAT THE CLEAR MUST NOT TOUCH ~ the library survives every founding

''LARES PASS; THE LARARIUM ABIDES'' (operator-ruled). ''THREE WORDS, AND NONE OF THEM ARE SYNONYMS'':
''lares'' names the SPIRITS (agents, personas — their identity, and the vessel substrate they run on),
''lararium'' names the HOUSE (the acquired shelf, the sensoriums, the history), and ''lar'' names the
NAMESPACE, the `lar:///…` URIs and every resolver in the stack. Every siting answers one question —
''whose is this?'' — and the answer picks the home:

|! home |! holds |! the rule |
| `~/.local/share/lararium` (`<lararium>`) | the acquired library · every sensorium (operator history + DreamNet history) | ''THE HOUSE'S.'' Back it up; nothing here re-derives |
| `~/.local/share/lares` (`<lares>`) | identity · nexus seal · repo registry · hearth bags · the vessel substrate | ''THE SPIRITS'.'' Back it up; nothing re-derives except `vessel/` |
| `~/.local/state/lares` (`<state>`) | harvest-stage · projection watermark · locks | ''NOBODY'S FOR LONG.'' Delete at will — all of it re-derives |

''A REHEARSAL READS THE SAME TABLE.'' `LAR_ROOT` re-prefixes every home and changes no shape: each directory
under an isolated root names an XDG ''KIND'' — data, state, cache, config, run — and the two HOUSES nest
inside the data kind exactly as they do under XDG. `<root>/data/lares` stands beside `<root>/data/lararium`
just as `~/.local/share/lares` stands beside `~/.local/share/lararium`. ''A house never stands where a kind
belongs'', so a rehearsal exercises the real siting and a container never reaches the operator's own shelf.

''A LAR'S KEYS ARE THAT LAR'', which is why identity stays with the spirits even though no rite re-makes it
either. And a family's books outlast whoever reads them, which is why the shelf is the house's. ''The wipe
zone standing as a house rather than a list is the CONSEQUENCE, never the reason.'' A Lar wakes, acts and
gives way — the substrate under `<lares>/vessel` is exactly that, reforged whole by `clear`, `bake` and
`rebirth`, and correct to lose because a rite re-makes it from the carriers on disk. The lararium is the
''shrine'': the place a family fed across generations and left standing when they moved. What is the
house's stands THERE, in a home every wipe-list fails to name because it names nothing outside its own. A
tier kept safe by sitting one subdirectory aside stays safe only until somebody widens a wipe by one path
segment; a tier standing in its own house leaves no edge to widen. ''A rule that fits in one breath needs no
warning label.''

The founding pares `<lares>/vessel` through `reset` and cuts two more by hand; `<lararium>` it never touches:

|! kind |! path |! the clear |
| the sovereign root | `<lares>/identity` | ''CUT'' — the fresh-from-void act |
| the Nexus seal | `<lares>/nexus` | ''CUT'' — a prior lineage strands a re-founding |
| the vessel substrate | `<lares>/vessel` | ''PAVED by `reset`'' — it re-forges from `bags/`, and the social bootstrap dies with it |
| the repo registry | `<lares>/repos.json` | ''KEEP'' — ids the bags name; re-registering costs an act |
| the ACQUIRED library | `<lararium>/library` | ''ANOTHER HOUSE'' — books, corpora, anything poured over. It rebuilds from NOTHING |
| the sensoriums | `<lararium>/sensoriums` | ''ANOTHER HOUSE'' — capture the machine took once and cannot take again |

''The shrine holds what is the house's.'' The runtime CAS may be pared because its blobs rebuild from
the bags carriers; an acquired body has no such path — the corpus left the repository precisely so a shelf
could grow without the history growing with it, and the shelf is now the only copy. A sensorium reads the
same way: operator history and DreamNet history, captured once, with no source to re-pour from. Verify the
shelf after the founding rather than trusting the absence of an error:

!!!! The OTHER home — `<lares>/vessel`, which clears WHOLE

''The spirits' house holds both kinds at once'' — the identity no rite re-makes, and the substrate every
rite reforges. `<lares>/vessel` carries the CRDT
store, the runtime CAS, and the ''social bootstrap'' — the address book naming the docs this vessel stands.
''Only the `daemon` bag stands required there.'' A PLACE names `daemon` and nothing else; a lit FACE adds its own
four planes, each under the tag its PersonaGroup derives — `persona-<tag>`, `circles-<tag>`,
`identities-<tag>`, `sessions-<tag>` — and the boot refuses a HALF face, some pins standing and others
absent. `reset` removes the directory entire, so the address book dies with the docs it addresses and no
wipe-list needs to remember it.

''It moved out of `genesis/` for a reason a founder meets directly.'' Sited under the corpus root, a
per-vessel address book sat inside a shareable directory — and anything copying that directory wholesale
handed a fresh vessel ''another vessel's identity'': `init` reads a bootstrap present, skips re-seeding,
and the boot resolves hearth-private docs no local store holds. A rehearsal proved it twice
(#rehearse, fifth pass). ''`genesis/` now carries the baked island ALONE'', which makes a seed safe to
copy, share, and re-site precisely because no identity rides inside it.

''A per-vessel artifact in a shared directory reads as a landmine
with a long fuse'' — it costs nothing until somebody copies the directory, and then it costs a founding.

```
lares library verify                                # every body re-digests to the directory holding it
lares library list                                  # the collections, and what each holds
```

''A `reset` never reaches it'' — the tier stands in the SHRINE (`<lararium>`) for
exactly this reason, sharing no path prefix with anything a substrate verb reforges. The danger the rite must
name reads as the ''operator's own hand'', never the machine's.

<<~/ahu>>

<<~ ahu #found>>

!! ② Found ~ stand the device + the three kahu

''THE RITE RUNS IN TWO MOVEMENTS, AND THE ORDER BINDS.'' A ''place-founding'' seeds the `daemon` bag alone — the
sovereign island, the vessel's own Keyhive individual, the hearth true-name — and a vessel founded to
exactly there carries, serves the public shelf, and holds every sovereign act closed. A ''face-founding''
lands on that standing place: it mints the PersonaGroup FIRST, then seeds the four planes that group's tag
names (`persona-<tag>`, `circles-<tag>`, `identities-<tag>`, `sessions-<tag>`) into the SAME `daemon`
island. Minting first carries the whole weight: `changeIdForBag` keys a document by hashing its bag URL, so
a plane seeded before its tag exists answers to a name nothing ever reaches again.

`lares vessel stand --install` stands the device/vessel identity + the daemon + the founder persona-root h0
(h0's root signs the founding bind that peers pin — it MUST pre-exist). The three kahu then stand
as ''three symmetric commands'' — and each command performs ''THREE ACTS, never one'' (persona-policy,
2026-08-06). Stand h0 too; an unnamed founder strands itself out of its own charter:

```
lares vessel stand --install                                # stands the VESSEL — and nothing else (#/sidecar)
lares persona new 0 --name '<private label>' --handle '<declared Handle>' --seat
lares persona new 1 --name '<private label>' --handle '<declared Handle>' --seat
lares persona new 2 --name '<private label>' --handle '<declared Handle>' --seat
lares persona list                                  # h0/h1/h2 stand: label -> declared Handle, each standing for a chair
```

''THE THREE ACTS, and why the seal reads only one of them.''

* `--name` sets the ''PRIVATE label'' — what the operator calls that compartment. It rides the human's
  own fleet (that face's `persona-<tag>` plane) and ''never publicly federates''.
* `--handle` declares the ''PUBLIC Handle'' the persona answers to. A ''local intent'', not a publish:
  only a publicly announced Handle binds a PersonaGroup to a public glamour.
* `--seat` stands the persona for a ''Kahu chair on THIS node''. It stays local — a chair names a seat
  on a particular node, so it never rides the fleet.

''THE ROSTER FORMS FROM WHAT STOOD.'' No list of founding names ships in the build: `nexus seal seat` reads
the personas that ''declared a Handle AND stood for a chair'' and writes exactly those as the roster. A
scaffold carrying names would make the ''SOURCE'' decide who the founding kahu are, leaving the operator to
confirm a legitimacy call somebody else made — and legitimacy belongs where the acts happen. A persona that
declares and never stands ''takes no chair at all'', rather than an empty one somebody wrote down for it.

''THE THRESHOLD comes from the roster, or from the operator.'' Absent `--threshold`, the seat derives
''MAJORITY'' over what stood (3 chairs → 2). Majority reads as the weakest defensible default: it refuses a
single hand without handing any one kahu a veto. A `--threshold` past the roster ''REFUSES'' — it would seat
a rule no quorum could ever reach — and a seat with ''nobody standing'' refuses and writes nothing.

''THE SEAL JOINS ON THE DECLARED HANDLE, NEVER ON THE LABEL.'' Matching the private label would ''weld
the two registers'': an operator could then seat only under the same string they call the compartment at
home, and every private label would become a public commitment by construction. The two MAY read
word-for-word identical — one string may name a compartment privately AND stand as a chair name — and
they remain ''two acts''. ''The declaring act makes a name public, never the characters.''

''Neither flag implies the other.'' A persona MAY declare a Handle and stand for no chair (the ordinary
case, and the shape every later persona takes). A `--seat` carrying no Handle ''answers to no chair'',
so `persona new` refuses it rather than seating a nameless claim, and a chair whose declared Handle
nobody stood for stays ''UNSEATED'' — the seal reports the empty chair rather than reading a declaration
as consent to sit in it.

''THIS MOVEMENT WRITES THE SELF-STOOD PATH, and a second path stands beside it'' (True Name Model,
2026-07-25). `FoundingBinding` names two modes as a union rather than inferring one from an optional
field:

* ''SELF-STOOD'' — the vessel carries a seed that SIGNS: it mints its own persona root and signs its
  own device-delegation, seating a fresh persona-KEL inception. The sequence above. A human's own
  hearth or leaf, present and binding their own device.
* ''CONTRACTED'' — the vessel mints ''NO root''. A trusted operator, already contracted with the
  kahu, signs the edge on THEIR vessel; this one carries only the ''EDGE'', the pinned identifier
  prefix, and that operator's KEL chain (carried, never fetched — a vessel holding no human key also
  holds no way to go ask for one). ''It seats no inception'': the contracting operator's identifier
  already stands, and minting a second would fork the very continuity the pin exists to hold.

''BIND BY EDGE, NEVER BY SEED.'' A key COPIED between vessels //is// the collector — presented to
every verifier it meets, it links every self beneath any policy written above it.

''⚠ ONE WORD, TWO SENSES — hold them apart.'' This rite uses ''contracted'' in both meanings and they
never coincide: a ''CONTRACTED FOUNDING'' (this movement) names HOW a vessel came to stand; a
''CARRIAGE-CONTRACTED OPERATOR'' (#/grow-the-nexus) names an operator who agreed to CARRY for a Nexus.
A self-stood hearth may be carriage-contracted; a contracted-founding Herm carries without ever
holding a civic identity. ''Founding-mode ⊥ carriage-contract.''

''A HERM STANDS CONTRACTED ALWAYS, as law rather than setting'' — its legitimacy runs kahu quorum →
the operator's carriage contract → that operator's persona root signing its edge. Bind-by-edge in its
purest case: ''such a vessel cannot cheat by holding a seed, because it never holds one.'' Its persona
ceiling reads 0 and no argument raises it (faceless-by-class). See #crossroads.

<<~/ahu>>

<<~ ahu #reserve>>

!! ③ Reserve ~ forge the pre-rotation's future

The charter pre-rotates (a compromised present key-set cannot forge the future,
[[charter-chain|lar:///ha.ka.ba/lararium/mesh/charter-chain]]), so `seat` needs a
`--next-key-commit`. `reserve` forges the next epoch's key-set from a ''separate CSPRNG reserve
seed'' (hardened SLIP-0010, never the live signing seed), prints the public commit, splits the seed
Shamir ''2-of-3'', seals Share 1 on the vessel, and zeroizes the seed — the vessel keeps only Share
1 + the public commit, never the seed:

```
lares nexus seal reserve --guardian-a '<guardian-a>' --guardian-b '<guardian-b>'
    # → prints --next-key-commit + THREE cards (each: a checksummed share code + a confirm phrase),
    #   seals 'Recovery-card mine' (Share 1) on the vessel, zeroizes the seed
```

''⚠ THE REAL NAMES NEVER ENTER THE TREE'' (operator, 2026-08-09). A guardian label names a ''living
person'', and this runbook rides a checked-in, federating corpus — so writing the actual two in here
would publish, to every reader of the repository and every clone of its history, ''who to approach to
reconstruct a Nexus''. The independence rule (#/reserve-custody) buys its safety from an adversary not
knowing which three domains to reach; a runbook naming two of them ''hands that back''.

''So the rite carries placeholders and the operator types the names at the CLI'', where they reach the
reserve state on the vessel and the printed cards alone. ''The same law governs every operator-facing
example below'' — a runbook demonstrates a SHAPE, and a shape needs no real person standing in it.

Place the three cards by hand (#/reserve-custody). Then seat the roster + arm the pre-rotation, and
read the quorum verdict:

```
lares nexus seal seat --next-key-commit <the printed commit>   # seats h0/h1/h2 → 2-of-3, epoch0 armed
lares nexus seal show                            # the quorum STANDS + the chain head reads armed
```

<<~/ahu>>

<<~ ahu #reserve-custody>>

!! ③′ Reserve custody ~ the independence rule

A single Shamir share reveals ''nothing''; any ''two'' reconstruct the reserve seed. So the whole
scheme rests on one rule:

> ''The three shares MUST sit in three domains that do not fall together.'' No single realistic
> breach, theft, or fire may reach two of them.

The compromise-resistant layout keeps every operator copy as the ''same'' Share 1:

* ''Share 1 (your domain)'' — the vessel + 'Recovery-card mine' (print AND/OR email); all one share,
  one domain. An adversary who owns your whole digital life reads ''one'' share -> nothing.
* ''Share 2'' — 'Recovery-card guardian-A' (a trusted person, or an independent physical place; a
  cabalmate takes it as they launch, via `reserve refresh`).
* ''Share 3'' — 'Recovery-card guardian-B' (a second independent domain).

Two correlation traps hide inside "lots of options": the ''online-identity trap'' (GitHub AND a
cloud both behind one email/password-manager count as ONE domain — put at most one share there) and
the ''co-location trap'' (a thumb drive AND a printout in the same drawer die in one fire — spread
the offline shares across places). The card's ''confirmation phrase'' rides out-of-band (in person, a
call) — it confirms the right person holds the right share, and it becomes the handshake when a
guardian hands the share back at recovery.

The math then holds three ways: ''compromise'' (you fully
owned -> one share -> safe); ''loss'' (you lose vessel + card -> two guardians rebuild without you);
''rotation'' (your share + one guardian). A recovery materializes the seed, so `reserve refresh`
re-seeds -> re-splits -> re-issues cards + a new commit afterward (Proactive Secret Sharing,
[[siege-resilience|lar:///ha.ka.ba/lararium/mesh/siege-resilience]]).

!!! Naming the two guardians ~ what the flags do, and the three questions that decide

''`--guardian-a` and `--guardian-b` write LABELS on cards.'' They carry no key, address, or account, and
nothing transmits anywhere: the command prints a share code plus a ''confirm phrase'' you read out-of-band,
and you place the card by hand. The names ride the public reserve state so `seal show` can report who holds
what — ''the label answers //who do I call at recovery//'', nothing more.

''The scheme's real purchase runs in the direction operators forget.'' Full compromise of the vessel yields
''one'' share, which is nothing. The inverse matters more: ''the two guardians recover WITHOUT you''. That
converts a death, a hospitalization, or a lost vault from the end of the lineage into a survivable event.

```
\procedure ~Ask(Type:"" Params:"") ~Ask <<Type>> holds [{{{ [<Params>] }}}]
```

<<~Ask Collusion "asks/would-these-two-combine-against-me ~ together they hold the Nexus; pick two whose interests differ FROM EACH OTHER">>
<<~Ask Correlation "asks/could-one-fire-flood-or-subpoena-reach-both ~ two cards in one house reads as a 1-of-1 wearing a costume">>
<<~Ask Reachability "asks/can-someone-who-is-NOT-me-find-them-on-the-worst-day ~ a guardian only you can reach recovers nothing">>

''THE NAMES MAY STAND UNASSIGNED AT FOUNDING'' — and a founder short of an answer should let them. The split
runs 2-of-3 either way; the labels store as `unassigned` and the cards print regardless. ''Naming is a
PLACEMENT act'' — handing physical cards to humans — and that act need not share an afternoon with the
founding. Place them when you next stand in front of those two people.

''The cost of a wrong name reads as a refresh, never a lineage'' — but the refresh carries a fork the command
names for you: ''before'' the first rotate it re-mints genesis cleanly; ''after'' one, the current reserve
keys must reveal at the pending rotate first, because a sealed head's pre-commitment freezes into its
`epochCid`. So a founder who expects the names to change should refresh ''before'' rotating, not after.

<<~/ahu>>

<<~ ahu #threshold-attest-recovery>>

!! ③″ Threshold-attest recovery ~ the pre-commit rhymes, the recovery rotates

''Fork B'' (identity-classes#the-two-forks) recovers a PERSONA-root the way the reserve pre-commits a
CHARTER key-set: ''both pre-commit at founding''. The reserve folds the next epoch's kahu keys into
`nextKeyCommit`; the persona-KEL folds the ''guardians' recovery PUBLIC keys'' into the identifier
PREFIX at inception (`provisionThresholdRecoveryAtFounding` -> `sealKeySetHash` -> `mintPersonaInception`).
The two rites RHYME — a pre-committed k-of-n digest an operational compromise cannot forge — and the
same ''independence rule'' (#/reserve-custody) binds the three guardian domains.

The DIVERGENCE from the reserve card path (#/reserve-custody): threshold-attest ''reconstructs NOTHING''.

* the reserve SPLITS a seed 2-of-3 and MATERIALIZES it at recovery (a transient single-secret window);
* Fork B gives each guardian their OWN recovery keypair — at recovery each guardian ''SIGNS'' the
  rotation event independently, ≥ k signatures verify against the pre-commit, and no seed ever assembles
  (`attestAndRotate`). The recovery authority signs ONLY rotations, never content.

''Operator ruling (2026-07-20):'' recovery ROTATES a fresh op-key
rather than resurrecting the old — ''continuity anchors on the identifier, not the key''. The persona-KEL
(the stable prefix) + the threshold-attest rotation stand BUILT + tested (mesh: `persona-kel` +
`recovery-keel-core#attestAndRotate`).

''THE BINDING-GATE PIN-MOVE LANDED.'' `bootDaemonKeyhive` pins `personaKel.prefix` and walks the local
chain to its current authoritative head, halting fail-closed on a genesis-prefix mismatch, a broken or
below-quorum head, or an edge that fails to chain — and it records //no raw-pin hybrid//, because
accepting both a raw op-key pin and a prefix would open a downgrade. So ''continuity rides the log'' and
this movement waits on no storage/sync placement (identity-classes#the-honest-edges carries the
correction and its reason: //a stale wall misroutes every reader who trusts it//).

⚠ ''WHAT REMAINS IS THE FORK, not a prerequisite.'' `identity-classes#the-two-forks` holds the one open
choice — whether the recovery authority EVER materializes as a single secret (Fork A reconstruct-then-sign,
maximal reuse of the guardian-card machinery, a shrunk transient window) or NEVER does (Fork B
threshold-attest, each guardian signs independently, new multisig and heavier onboarding). ''Both stand
built''; neither is wired into `runInit`, and the wiring waits on the ruling rather than on code.

⚠ ''TWO SITES STILL BIND A RAW KEY where a prefix belongs'', each with its own consequence: the antigen
bans a presenter `nym`, so a lawful rotation ''sheds the ban''; and a handle-card names a bare key as its
`nym`, so a face carries no chain to break.

<<~/ahu>>

<<~ ahu #seal-and-back-up>>

!! ④ Seal + back up ~ lock the secrets, write the net

Set the at-rest passphrase (a no-echo TTY double-entry — a typo self-locks), which seals the keyhive
archive + the recovery/reserve shares. Then write a passphrase-sealed backup to safe media:

```
lares vault seal                                    # no-echo TTY, double-entry; seals the carriers at rest
lares vault export ~/founding-backup.sealed         # a passphrase-SEALED backup (--force overwrites)
lares vault status                                  # confirm the seal state
```

!!! ★ WHAT A SEAL COMMITS THE OPERATOR TO, from this moment on ★

''A seal writes `sealExpected` into the CONFIG, and the clear does not remove it.'' From that moment every
boot reads whether the archive OPENS, and a boot that cannot open it ''does not refuse — it stands at the
WAKING FLOOR''. The vessel comes up as a crossroads: it meshes, carries, routes, serves the public shelf,
and holds every sovereign act closed until a key arrives. Nothing degrades, because the floor is a named
standing rather than a weakened one — a vessel that cannot open simply never rose.

''Which changes what a missing passphrase LOOKS like, and that matters more than the mechanism.'' An
operator expecting a refusal reads a running-but-faceless vessel as a failure and goes hunting one. The
honest reading: ''the hearth did not light''. The cure rides in the boot's own words — set
`LARES_ARCHIVE_PASSPHRASE` and stand it again.

''The rite never crosses that gate.'' ⑤ plants content into the vessel already standing from ②
and restarts nothing, so a founding that seals here by TYPING the passphrase — the honest path, the one
the prompt exists for — walks to LIVE without carrying it forward by hand between two movements.

''The env var is not a convenience; it is the standing condition of a
sealed vessel.'' Every later terminal, every reboot, every `lares vessel stand --foreground` wants it.
That reads as the unattended-boot tension in everyday clothes (#/rehearse), and it arrives the moment the
vault seals — not later, at some crossroads.

!!! ⚠ THE ENV VAR MAKES THIS MOVEMENT A NO-OP — and it says so quietly

''A passphrase in the environment during ② seals the carriers THERE.'' The archive's seal policy takes the
passphrase path whenever `LARES_ARCHIVE_PASSPHRASE` rides the environment, so a founding run in a shell
that already carries one seals at founding — and ④ then finds ''nothing left to do''. The TTY prompt never
opens, because a prompt only opens where no env var answers first.

''Which reads fine, and hides one thing worth knowing'': the leg the rehearsal cannot walk goes unwalked
HERE too. A keeper founding in a passphrase-carrying shell never meets the double-entry prompt at all —
so it never meets it, rather than meeting it on the worst day. That reads as a smaller risk than the one
the rite warns about, and a DIFFERENT one than an operator expects while typing ④.

''Choose deliberately.'' To walk the prompt, export ONLY `LARES_ARCHIVE_PASSPHRASE_NEW` (which `seal`
reads) and leave `LARES_ARCHIVE_PASSPHRASE` unset through ②; to seal at founding and skip ④, carry both.
Either stands — an unwitnessed choice does not.

''A no-op now announces itself'': `vault seal` reports sealing NOTHING and
writes no day-stamp, rather than printing a seal-day for work it did not do. It once stamped first and
sealed second, so a vault that sealed nothing carried a record claiming it had.

!!! The two env vars ~ and why a bare `export` betrays you

''Two vars ride, and they answer different questions.'' `LARES_ARCHIVE_PASSPHRASE` carries the ''current''
one (what opens the vault today); `LARES_ARCHIVE_PASSPHRASE_NEW` carries the ''target'' (what a `seal`,
`rotate`, or `export` moves to). A first seal has no current, so only the second matters. Both exist to let a
''non-interactive'' run proceed — the rehearsal harness reads `_NEW`, which is why a run that exported only
the base var fails at the vault with the passphrase apparently set.

```
read -rs '?NEW passphrase: ' LARES_ARCHIVE_PASSPHRASE_NEW && echo
export LARES_ARCHIVE_PASSPHRASE_NEW
echo ${#LARES_ARCHIVE_PASSPHRASE_NEW}               # verify by LENGTH — never echo the secret
```

''`read -rs` rather than a bare `export`, and the difference is the whole seal.'' A typed
`export LARES_ARCHIVE_PASSPHRASE_NEW='…'` writes the secret verbatim into the shell's history file, where it
outlives the ceremony, survives a reboot, and rides into any backup of the home directory. ''A passphrase
that reaches disk in the clear has already failed'' — the vault protects a stolen disk, and the history file
sits on that same disk. The `-s` keeps it off the screen; the leading space or `read` form keeps it out of
the history; the length check confirms arrival without disclosure.

''IT LIVES FOR THAT SHELL ALONE.'' A new terminal starts without it, and that reads as the design rather
than an inconvenience: the var exists so ''one ceremony'' can run unattended, never so a machine can seal
itself forever. Putting it in `.zshrc` would leave the key beside the lock on every boot, which trades the
whole at-rest seal for the cost of retyping — and the at-rest seal is the only thing standing between a
stolen disk and a sovereign identity.

''The TTY leg still goes un-exercised by the harness'' (#/rehearse), so a
keeper founding that seals interactively walks a path no rehearsal has proven. Seal by hand at least once
before the day it matters.

<<~/ahu>>

<<~ ahu #regenesis-live>>

!! ⑤ Seed + ⑥ LIVE ~ plant the content, serve

The vessel already stands from ②. What ⑤ adds is the CONTENT: every `bags/*` holding planted back
into its doc, kind-routed and diff-gated. Resume the hooks, and the node serves:

```
lares vessel seed --apply --yes                     # plant every holding; idempotent, a converged tree no-ops
lares hooks resume                                  # re-arm capture/ingest
lares vessel read                                   # LIVE — the node serves; the quorum + charter stand
```

''SEED, not rebirth — and the subtraction removes a trap rather than hiding one.'' Rebirth composes
stop · clear · bake · stand · seed, which on a fresh founding tears down what ② built ninety seconds
earlier and re-bakes a genesis nothing has touched. Only the seeding was ever needed here. Rebirth of a
''standing'' vessel stays a real motion — `lares vessel rite rebirth` — and simply not a founding step.

The trap it removes: a seal writes `sealExpected` into the config, and the clear does not remove it, so
every later daemon boot reads whether the archive opens, and one that cannot stands at the FLOOR instead of
lighting the hearth. A movement that RESTARTS the daemon meets that reading mid-rite — and a founding would
reach LIVE as a crossroads rather than a hearth, which reads like a failure and is not one. A movement that
never restarts never meets it.

''AND THE RITE ENDS HERE, at the handover.'' `hooks resume` re-arms capture, which starts the sensorium's
clock — and the sensorium's lifecycle rides ''its own rite'', deliberately not this one
(<<~ loulou lar:///ha.ka.ba/lararium/api/sensorium-runbook>>). The two carry different cardinal sins:
this rite's worst outcome ''loses a key'' and nothing recovers it; the tending rite's ''loses a baseline'',
and a corpus re-pours where a measurement never taken does not. ''A pour, a sweep, or a teardown belongs
there'' — a movement for it here would teach an irreversibility the sensorium does not have.

<<~/ahu>>

<<~ ahu #crossroads>>

!! ⚲ Crossroads ~ stand a Herm where the family needs one (OPTIONAL, after LIVE)

A family whose hearths sit behind different networks wants a ''standing'' place to meet. The Herm
(Lares Viales) stands that crossroads: a ''wiki-LESS wayfarer'' composed up from granular caps —
substrate + the daemon-bag immune keel + a writable meshpalace FLOW-map + the read-face that serves it.
''NO wiki, NO pool.'' Pi-deployable.

```
lares herm --port 8080 --relay-port 8090            # the http FLOW-map face + the crossroads WS port
                                                    # prints the dial URL(s) + the gate pubkey
```

''The gate seed pins, and that matters across restarts.'' Absent `--relay-seed`, it derives from this
Herm's OWN identity — stable across reboots, so a family's hearths keep dialing the same key and
''never a fresh random per boot''.

''DERIVES, and the word is load-bearing.'' The gate seed reads `HMAC-SHA256(RELAY_GATE_INFO,
vesselSeed)` — the tag itself lives in the domain registry — domain-separated, never the identity seed passed through. It once WAS the seed itself, so a
Herm published its vessel identity key as the public crossroads gate key: nothing leaked, and two trust
domains became one correlatable pair, which is the join the veil model spends its design preventing. The
house's own rule settles it — an HMAC `info` IS the separation.

''What a Herm holds and what it refuses.'' It carries a ''PLACE DID'' and ''NO operator DID'': the
crossroads earns a name it can be addressed and vouched by, while ''no local human key rests there to
steal''. It runs a full immune keel (it recognises Kapae bans and draws Mu) yet ''reads nothing'' —
carry ⊥ read holds absolutely; it holds the ciphertext replica and decrypts none of it. It founds
CONTRACTED (#/found), so it holds no seed to leak.

''AND A HERM CARRIES A RAISE DOOR, so a recognised operator may act there for a visit.'' The Herm prints a
''raise challenge'' in its own boot output — vessel, Nexus, epoch, nonce, and nothing of what it holds. A
recognised operator signs it on THEIR machine and hands the grant back:

```
# on the recogniser's own vessel, with one of their own persona roots:
lares raise sign '<challenge-json>' [--as <persona-index>]
```

''The caps ride the recogniser's key, never the Herm's.'' Nothing seats a persona root at the crossroads
(`personaSlotCeiling("herm") === 0`), so a stolen disk still yields nothing of anybody's person, and the
raise lives in memory only — a reboot drops the vessel back to its floor with nothing to resume.

''It ends by NON-RENEWAL, and nobody performs a lowering act.'' A grant binds the lease epoch its
challenge named; when that Nexus's epoch rolls past it the reading simply stops coming back raised.
Recognition itself borrows the members fold this Nexus already holds — quorum-signed, contract-in
verified — so a Herm that recognises nobody raises for nobody.

''WHAT WAITS: the ask/answer verbs.'' The Herm's own half — provoking a fresh challenge on demand and
reading a grant back — needs the door registered as a daemon verb, so today the boot output carries ONE
challenge and a refused answer waits for a restart. Inconvenient rather than unsafe: burning a challenge
on refusal IS what stops anyone grinding at a nonce.

''The boundary reads STRUCTURAL, never a flag.'' A Herm that gains a persona cap ''stops reading as a
Herm'' — it composes into an operator-class vessel and the nothing-to-steal property leaves with the
absent cap that carried it. `composable-keel`'s //absence = un-routed authority// makes that a fact
about the stack, not a rule a later bug could flip.

<<~/ahu>>

<<~ ahu #grow-the-nexus>>

!! ⊕ Grow ~ open the Nexus, contract operators, welcome users

The LIVE node stands ''sovereign-island by default'' (`federationPosture: private`). It GROWS on the
operator's word, holding the ''membership doctrine'' at every step — ''track operator CONTRACTS, never
user IDENTITIES'' (#/membership-doctrine). Nothing here stands up a user table.

''Open the beta'' — flip the Nexus from private-island to co-federating the public planes into the
super-mesh (never a private plane; the read-floor holds in both postures):

```
lares nexus posture open                            # co-federate the public shelf; a node bounce applies it
```

''Contract a fellow operator'' — a two-sided wax-sealed handshake (the joining operator accepts
carriage; the kahu-quorum counter-signs). The entry carries ''operator-pubkey + accepts-carriage +
charter-epoch, and nothing more''; a contracted operator relays the mesh's sealed traffic + carries the
Kapae immune antigen:

```
lares nexus accept-carriage                          # the joining operator signs "I accept carriage"
lares nexus contract <operator-pubkey>               # ≥k kahu counter-sign the wax-sealed CarriageEntry
lares nexus members --list                           # the carriage-contracted operators — never the users
```

''THE BOARD RECORDS CARRIAGE, AND EVERY SPELLING SAYS SO.'' `holdsCarriage`, `CarriageEntry` and the
board's own address `#carriage` run one word through the signing domain, the doc address and the CLI
alike — because //a board that recorded contracts and called everyone a member// reads exactly backwards
to anyone asking //is this human one of us//. The act that writes an entry therefore spells CONTRACT
(`nexus contract`), and `nexus members` reads the board back.

''A rename that reaches the signing domain and stops at the CLI leaves the
operator typing the one spelling nobody corrected.'' It reads as a free move — a CLI verb name carries no
signing domain and no doc address — which is exactly why it gets deferred and then forgotten. The
rite-commands witness now reads every instructed sub-door at its DISPATCH POSITION, so a door renamed
underneath this rite fails the witness rather than the operator.

''Welcome a user'' — a user needs no contract and leaves NO roster trace. Through invite-only alpha a
sealed BURNABLE boot-invite gates the boot (spent-on-boot, no who-invited-whom edge); a garbled or
absent invite founds a vessel at the ''anon floor''. A user builds their OWN social graph — a follow lands as
a LOCAL write to that face's own private `circles-<tag>` doc, which fleet-syncs every one of their
vessels and NEVER touches the wire — one follow graph per FACE, so a multitude's compartments never share
a document anything could read to correlate them (#identity-classes, the recognition layer gives the human their one window on the crypto):

```
lares circle add <nym> --to following --petname '<your-name-for-them>'   # a private, never-federated follow
```

''What crosses vs what stays'': a contracted operator BLIND-TRANSITS sealed CIPHERTEXT bodies for the
mesh (carry ⊥ read — the relay verifies `BLAKE3==cid` holding NO secret, the body sealed under the
per-Nexus convergence salt, fetched DHT-free); the read-cap crosses for no one; a private plane never
leaves the PersonaGroup's own fleet. The mesh breathes over immutable bodies while a stranger — every
500-year GPA — cannot confirm who-holds-what.

<<~/ahu>>

<<~ ahu #gather-the-fleet>>

!! ⊛ Gather ~ the operator's own vessels join the standing fleet (after LIVE)

A laptop browser, a phone, a tablet. Each stands as a ''LEAF, CONTRACTED'' — it mints its own vessel key on-device and receives a signed delegation EDGE from the vessel that already holds the persona root. It mints ''NO root of its own''. `BIND BY EDGE, NEVER BY SEED`, movement ② names the mode; this walks it.

''On the standing hearth'' — mint the admit payload for the joining device, under the persona root that will hold it:

''THE JOINEE MINTS FIRST, AND THE ORDER CARRIES THE PROPERTY.'' Admission ''SIGNS a key the joining
vessel already holds''; it never issues one. So the private seed is born on the joining device and stays
there, only the ''public verifying key'' crosses, and the signing call refuses to run without it —
measured 2026-08-04, `runDeviceAdmit` throws where that key goes missing. This ordering IS what keeps the
QR ceremony photograph-inert: a payload names a key the holder already proved it holds.

''① On the joining vessel'' — mint the vessel key and read out its public half:

```
lares vessel stand                                           # stands the stack; mints THIS vessel's own key
lares vessel read                                         # read the vessel verifying key (the public half)
```

''② On the standing hearth'' — sign an edge over the key the joinee showed:

```
lares device-admit --as 0 --joinee-key <hex>         # signs a device-delegation edge; writes the payload
```

''③ On the joining vessel'' — apply the signed payload:

```
lares vessel stand --admit <payload-file>                    # joins the existing PersonaGroup on the key it already holds
lares persona list                                   # the same roots, reached through a new vessel
lares persona sync                                   # carry this vessel's own labels + declared Handles to the fleet
```

''THE NAMES RIDE, THE CHAIR DOES NOT'' (2026-08-06). Once a joining vessel breathes, the ''private label
and the declared Handle'' travel that face's `persona-<tag>` bag — the self-slot FLEET-syncs it
same-operator, and the
DeterministicFederationGate never volunteers it to a cross-operator. So a persona answers to the same
name on every device the human holds, and `persona list` on a fresh leaf reads the multitude rather than
a blank. The ''seat claim stays on the node that holds it'': a Kahu chair names a seat on a PARTICULAR
node, and syncing it would let one device seat a persona on another device's seal.

''`sync` runs local-first and idempotent.'' A vessel FOUNDS before it breathes, so names set pre-boot
live in the local floor until a hearth answers; running it from every vessel converges, because each
name carries its own stamp and the later one reads. A quiet sock reports ''NODE-LOCAL, never failure''.

''What the payload carries, and what it does NOT.'' It carries ''one signed edge on a lineage'' — the
persona KEL prefix, the signer that vouched, the hearth true-name the edge binds to, and the
self-delegation naming this device. It carries ''no capability-event bundle'': the founder mints the
capability history and holds it. Continuity anchors on the KEL prefix, so a later rotation ''supersedes''
this edge rather than orphaning the vessel holding it.

''A browser vessel differs by device-gated caps, never by class'' — the isomorphic architecture stands the same cap-stack and composes fewer of them (`identity-classes`). Transport for the payload (QR / NFC / LAN) stays pending; a file over any channel the operator already trusts works today.

!!! THE INVITE CARRIES THE DIAGNOSIS ~ what the payload MUST hold before QR transport lands

''A silent responder makes the invite the only place a failure can speak.'' A shrine that stays silent toward strangers (<<~ loulou lar:///ha.ka.ba/lararium/mesh/siege-resilience#/the-active-prober>>) answers a legitimate guest with a stale invite, a skewed clock, or a too-old client exactly as it answers a prober: nothing. Every deployment that bought silence paid this bill — WireGuard reports a missing handshake timestamp and no reason; a Tor bridge leaves diagnosis to the client log and to out-of-band bridge-health checks. ''No responder-side cure exists'', because any error channel rebuilds the oracle.

''So the payload carries every failure the joining vessel can check WITHOUT sending a packet'', and each becomes a loud LOCAL error:

* ''expiry'' — a stale invite reads as expired on the joining device, never as a dead network.
* ''chant epoch + protocol version'' — a dialect mismatch names itself before the handshake runs.
* ''shrine key fingerprint'' — the guest confirms WHICH shrine it approaches, which the wire never tells it.
* ''expected release fingerprint'' — the invite authenticates the software rather than the reverse (<<~ loulou lar:///ha.ka.ba/lararium/mesh/genesis-doc#/invariants>>).
* ''single-use nonce'' — spent-on-boot, matching the burnable boot-invite the ⊕ Grow rite already mints (#/grow-the-nexus).

''Silence then stays reserved for a genuine key mismatch'', which reads as the only failure the guest may not distinguish — and the only one worth hiding.

''NEVER gate on a wall clock.'' The replay window wants a per-peer monotonically increasing counter, not an accurate time (<<~ loulou lar:///ha.ka.ba/lares/api/pono/causal-islands#/two-clocks>>); a device with a skewed clock must still join.

!!! The QR mitigations that hold, and the one that survives a stolen code

''Treat a code as a bearer token a camera lifts across a room.'' The mitigations carrying a field record:

# ''single-use, and consumed at the first successful handshake.''
# ''short expiry'', checked locally by the joining vessel.
# ''confirmation at BOTH ends'' — Matrix's key-verification flow requires a reciprocating human act on the scanned side; Signal hardened its device-linking flow the same direction after the campaigns at <<~ loulou lar:///ha.ka.ba/lares/api/pono/shelter-against-empire#/legibility>>. The Matrix QR byte layout (prefix · version · mode · transaction id · two keys · shared secret) reads from held knowledge; the fetched spec section came back truncated and the field widths stay UNVERIFIED here.
# ''A HUMAN closes the loop after the channel comes up'' — a short authentication string or emoji comparison, spoken between the two people. ''This one alone survives a code an adversary already photographed'', because it moves the check off the device.
# ''Operator-side notification plus an auditable, revocable enrolled-vessel list.''

> ''AND SILENCE HIDES THE RACE — OPEN, the ruling belongs to the operator.'' Where an adversary redeems a photographed invite FIRST, the legitimate guest's failure reads identical to a bad network, so no client-side signal exists at all. Detection therefore has to arrive from the operator's side: a vessel appears in the enrolled list that the operator did not expect. That names the requirement — ''enrollment MUST notify, and every enrolled vessel MUST come off individually without re-keying the shrine'' — and leaves unresolved how a revocation reaches a fleet that may sit partitioned for weeks under no global now.

''What this does NOT add: DEPTH.'' Every vessel here binds ''one principal's instruments''. Below the phase change the gauge reads the realm nominal, and correctly — a fleet buys REACH, never dwelling. The realm's standing moves when a second locus of cost begins to dwell, never when a fourth device arrives.

<<~/ahu>>

<<~ ahu #feed-the-realm>>

!! ⊙ Feed ~ the first offering IS the realm's founding (after LIVE)

''A realm mints no DID and hosts on no machine'', so nothing gets created — it begins when someone feeds it and would cease if feeding stopped (`cabal-realm#verb-not-noun`). ''The vessel is FOUNDED; the realm is FED.''

The machinery stands built and ''carries no CLI verb''. Until one lands, the rite goes LIVE with the realm it stood as a seed of never having begun.

''TWO PLANES CARRY THE WORD //FEED//, and only one of them survives a restart.'' `feedCabalRealm` warms a `BagStowage` residency — an in-process cache keyed on a wall clock, correct where it lives and worthless as a civic record; a verb that stood up its own would report on a residency it just invented. The DURABLE feeding rides the ''per-writer lease slot'': a coordinator-free max-register in epoch coordinates with ''no clock anywhere'', which `cabal-realm-clock` folds into who-feeds-and-how-deep. ''The offering the rite means is the lease roll.''

''OWED — the smallest honest surface'', and it wants no new machinery:

```
lares cabal feed --realm <realm-doc-id> [--as <n>]   # the OFFERING — roll your FACE's lease slot
lares cabal clock --realm <realm-doc-id>             # who feeds and how deep, verdict-free
```

The second reads the capture-clock the model already argues for — ''who feeds the realm and how hard'', verdict-free, so a majority SEES a minority out-feeding them while exit stays cheap (`cabal-realm#the-unswept-corner`).

''AND IT REPORTS NO //ALIVE// OR //DISSOLVED//, deliberately.'' Deriving a liveness verdict needs a rate at which unfed standing erodes, and ''no rate stands seated'' — a verb that answered anyway would be inventing the operator's calibration and handing it back as a reading. `deriveCabalRealmLiveness` still maps the in-process cache tier, which reads honestly ''only inside a running vessel''. An empty board reads UNFED and never //nobody feeds it//: under no-global-now an unsynced realm and an unfed one generate identically.

> ''ONE FORK GATES BOTH VERBS, and it belongs to the operator:'' WHICH BOARD carries a realm's feeding. A deterministic per-Nexus board (the seventh sibling beside carriage · antigen · vouch · edge-kāpae · persona-KEL) would federate the clock to every carrier — and publish, by inference, which keys dwell where, which is precisely what `dwellersHolding` refuses to enumerate. The realm's own members-only substrate keeps the sight INSIDE the realm and makes the clock unreadable from outside it. The read (`realmMaintenanceFromBoard`) takes whatever doc it is handed and never asks, so the choice stays open above it.

<<~/ahu>>

<<~ ahu #dwell>>

!! ⊘ Dwell ~ a second operator joins the REALM (design before commands)

''Carriage and dwelling ride ORTHOGONAL axes.'' ⊕ Grow contracts carriage — who relays whose sealed bytes — and seats no dweller. A contracted operator may dwell in no realm at all; a dweller may carry nothing.

''What stands built on the join axis'' (the JOIN ⊥ CONTRACT law, `admission-on-a-lineage`):

```
lares cabal vouch <joiner-nym> --realm <realm-doc-id> [--as <root-index>]
                                                     # one held face stakes its OWN standing. Admits NOBODY.
```

A vouch rides as signal-2 on the lineage; the crossing prices it later, and the cost already landed — the voucher's score SPLITS across everyone they vouch for.

''AND THE CROSSING ITSELF REFUSES TODAY, correctly.'' `admissionDials` fail closed to null, so a founded Nexus admits nobody until the operator seats ε · β · ρ · S · α. That reads as an unmade choice standing visible rather than somebody's guess quietly enforced — and the numbers stay ''an open design seat'', never a default this rite may supply.

''What CANNOT be written here, by ruling.'' The conversion act — a fed guest re-anchoring from host to realm — stays ''UN-MODELLED'' (`the-thing-event#unmodelled`). The mesh may record ''THAT a re-anchoring happened''; it must not hold what made it valid. A configurable rite would still be this stack's rite with someone else's values poured in, so no command here may name a quorum, a waiting period, a franchise, or a test. ''Each realm supplies its own.''

<<~ loulou lar:///ha.ka.ba/lararium/mesh/first-realm-arc>>

<<~/ahu>>

<<~ ahu #the-frame>>

!! The frame ~ why compromise-resistance rides early

Recovery cost climbs with the fleet. While a vessel stands as a-multitude-of-one (one laptop-node +
a browser), a compromise costs a ''full-pave + a fresh DID-multitude'' — cheap, nothing accreted. Once
vessel-fleets and cabalmate personaGroups entangle around an operator, the identity carries
relationships no cheap re-mint restores. So the rite ''bakes compromise-resistance in while it costs
nothing to get wrong'', and leans on cheap full-pave as the bootstrap net. The reserve's two guardian
slots stand ''unassigned'' until cabalmates launch, then fill via `reserve refresh` — the seed grows
from a-multitude-of-one, hardening as the cabal arrives.

!!! And SUCCESSION rides earlier still ~ the bus factor names the roadmap

''THE SPEC STANDS AS THE SUCCESSION PLAN.'' Read across the whole federation record, one clean survival
appears: an author moved the load-bearing artifact out of his own implementation and into a form that
transmits ''as a document'', and that grammar outlived every implementation he wrote — including his
own rewrite, which stranded the network it had convened.
''Behavioural membership makes this stack unusually well-placed to take that cure'', because //operators
who parse a shared grammar identically// names a document rather than a codebase. The boot seed and the
grammar carry the survivability; the TypeScript does not.

''THE FOUNDER-LEAVES FAILURE NEVER ARRIVES AS DEPARTURE. It arrives as a model held in one head.'' A
single operator carrying a conceptual system of this density carries an extreme transmission cost, and
nobody picks it up cold — so ''the reconstitution kit reads as a founding movement, not a later
courtesy'': what ONE other competent party needs to stand a node from void and read the grammar
identically. ''Absent that artifact the bus factor reads ZERO rather than one'', because a rite only its
author can perform transmits nothing when he stops.

''AND THE EXIT GETS DESIGNED BEFORE THE DEPENDENTS ARRIVE.'' Running infrastructure for others compounds
a debt that grows with THEIR success, and a hearth sheltering a community cannot decline the obligation
without harming exactly the parties it sheltered. So the rite owes an answer before ⊕ Grow, never after:
''what becomes of a dwelling community's bytes when this operator stops paying?'' Content addressing
answers it only where a second party already holds the bytes — ''verifiability, never availability''.

> ''UNDER THE STANDING BRIEF, THE OPERATOR VANISHING READS AS A FORESEEABLE ADVERSARIAL EVENT rather
> than an accident.'' Seizure, compulsion, illness, a door. So the disappearance belongs in the
> ''REHEARSAL branch as a rehearsed movement'' — stand the mesh once with the founding hand removed and
> see what survives — on the same logic this section already runs: rehearse it while it costs nothing
> to get wrong.

''On what pays for the running.'' The record rules out three channels by measurement: a crowdfund
finances a launch and never an operation; a subscription at the door blocks growth precisely where
network effect needs it free; and venture money forces the monetisation that breaks the trust promise.
''What survived ran on public-interest grant funding'' — no token, no exit, no growth mandate — and the
brief points at exactly the anti-censorship ground that channel serves.
''TWO FORKS RIDE WITH IT, and both belong to the operator rather than to this meme'': such funders sit
under their own political attack and offer no stable base; and taking state-adjacent money to shelter a
community FROM a state names a threat-model question, never a paperwork one.

''AND THE SYSTEM STAYS FULLY USEFUL TO ONE PARTY, indefinitely, with no mesh at all.'' Every open
alternative that built first and sought participants after died on network effect; every survivor had a
forcing event deliver participants to something ''already working''. A design that only becomes good at
scale dies before scale — which reads as a constraint on the rehearsal branch, not a hope about adoption.

<<~/ahu>>

<<~ ahu #identity-classes>>

!! Identity classes ~ who carries the reserve at all

The reserve burden rides ''operator-class'' identities alone (KERI transferable — persistent,
rotatable, they operate node vessels; the charter epoch chain reads as a key-event-log). Browser-only
''users'' (ephemeral, short cap-stacks, no charter role) carry NO reserve — they ride KERI
non-transferable/basic identifiers, and their "rotation" reads as ''abandon-and-recreate'', cheap and
native. The line draws by persistence/value, not by title: a user
vessel that accretes lasting value drifts toward transferable, held either by promoting that vessel
or by delegated identifiers (the operator root delegates to disposable user streams, durability in
the root). The default holds — users stay light; operators carry the future.

<<~/ahu>>

<<~ ahu #bags-and-shelf>>

!! What a founding vessel carries besides its keys ~ declarations and the shelf

Two surfaces landed after this rite was first written, and a founder meets both.

!!! Every bag declares itself

A bag carries its own `meta.mem` answering three questions no bag could answer before: ''what it holds'',
''who may read it'' (`cap-tier` — and a declaration only ever TIGHTENS against the structural floor, so one
that lies open changes nothing), and ''where it belongs'' (`home` — `repository` · `hearth` · `ley`).

```
lares bag list                                      # tier + home per bag, and whether each sits where it says
lares bag show <bag>                                # one bag, resolved on THIS vessel
lares bag declare <bag> --tier <t> --home <h> --repo <id>
lares bag home <bag> --to <home> --approve          # MOVE the bytes + re-anchor the declaration together
lares bag repo add <id> --root <path>               # the registry: bags name IDs, never paths
```

''`ADRIFT` names the row that matters'' — a bag whose bytes sit somewhere its own declaration does not point
at. That condition, unsurfaced, is how a Nexus seal came to rest inside a repository nobody chose.

''THE DECLARATION IS DISK-OWNED'' (operator ruling, 2026-08-08). It never enters the store and never
round-trips: seeded, a wiki edit could re-home a bag or loosen its cap-tier, and ''a declaration a rendered
surface can move is a declaration nothing holds''. The authority stays where `lares bag declare` writes it.
A re-founding therefore ''re-reads'' the declarations from disk and needs no act to restore them.

!!! The acquired shelf

Books and corpora a human did not author live at `<lararium>/library`, never in a bag. `bags/` carries what an
operator ''authors'' and round-trips; an acquired body has no author here and no parse∘render fixed point, so
left on the seed surface it enters git history and a growing shelf grows the history with it.

```
lares library list | show <coll> | verify           # verify re-digests BYTES, never records
lares library acquire <file> --to <coll>            # MOVES by default — moving is the point
lares library index <coll> --out <path>             # the tracked index, the part that travels
```

''A pour names a collection, never a path'': a bed manifest sources `library:<collection>` and each vessel
resolves it locally — the same law the repo registry runs and the `lar:` URI before it. ''The shelf survives
every founding'' (#/clear).

<<~/ahu>>

<<~ ahu #agent-seed>>

!! The agent seed ~ how a reader answers //what do I run next//

''This rite serves as the operator-instruction SOURCE'' (operator charge, 2026-07-27). A reader —
human at a prompt, or an agent through the MCP — should resolve the next command from ''the vessel's
current state'', never from memory or from a guess about which movement stands done.

''THE SURFACE SURVEY, and the gap it names.'' The two operator surfaces do NOT cover the same ground:

* the ''CLI'' carries the WHOLE founding surface — `vessel` (`found` · `stand` · `stop` · `clear` ·
  `bake` · `seed` · `read` · `rite`) · `persona` · `nexus` · `herm` · `device-admit` ·
  `cabal` · `edge` · `vault` · `circle` · `raise` · `bag` · `library`
  (#/bags-and-shelf).
* the ''MCP'' carries almost none of it — its ground reads sensorium/analysis/memory (`li` · `ki` ·
  `jing` · `pour` · `couple_r` · `forecast` · `mismatch` · `plane_record` · `sweep` · `rejim` ·
  `worldline` · `recall` · `analyze`) plus `vault_*`, `wiki`, `status`, `flow`, `reconcile`,
  `kapae`/`un_kapae`, and the roster/promote/retire family.

''NO VERB COUNT LIVES IN THIS PROSE, and the absence carries a ruling.'' A figure written here ages the
moment a door absorbs capability, and it ages ''silently'' — a reader trusts it precisely because it looks
specific. Both figures DERIVE instead, from the live table, at the tool that already reads it:
`tools/rite-commands-witness.sh` prints ''doors · addressable verbs'' on every run. The pair rides
together because a door count reports a collapse the operator cannot feel — the fan-out lives one level
below the number, so either figure alone flatters.

''NOT ONE founding verb crosses to the MCP'', and the surface-parity witness derives that too rather than
asserting it. An agent working through that surface can read, recall and analyze — and stands
''structurally blind'' to the entire civic/identity rite, by design.

''THE PONO SHAPE READS GUIDANCE, NEVER EXECUTION.'' The cure emphatically does NOT mirror the
founding verbs into the MCP: a surface an agent drives should never reach `vessel clear --force`, mint a
persona root, or seat a kahu. ''Executing the rite belongs to the operator's own hand.'' What the
agent surface owes instead:

# ''READ the rite'' — resolve the movement matching the vessel's state, and return its exact commands
  as ''text for a human to run'', with its branch (REHEARSAL ⊥ KEEPER) named.
# ''READ the state'' — which movements already closed (does an identity home stand? do h0/h1/h2 stand
  named? does a charter epoch stand? does a reserve stand? does the node serve?).
# ''NAME the branch and the cost'' — a ⚠ irreversible movement MUST surface as irreversible, and the
  rehearsal alternative MUST surface beside it, because a reader who does not know a cheap path exists
  will take the expensive one.
# ''REFUSE to act.'' The guidance surface returns instructions; the operator runs them.

''The seed response, in shape'' (never a template — the frame holds, the words never):

> //You stand at movement ② (found): an identity home stands, no personas seated. Branch: REHEARSAL
> (containers) — nothing here costs anything to redo. Run: `lares persona new 0 --name '<label>'
> --handle '<Handle>' --seat`…
> Next after that: ③ reserve. ⚠ Movement ① on the KEEPER branch destroys prior roots and does not
> undo — you are not on that branch.//

''Honest bound.'' No such tool stands built, and this meme asserts none. The survey above reads off
the live surfaces (2026-07-27); the shape reads as a proposal awaiting the operator's word, and it
waits behind the first rehearsal — a guidance surface built before anyone has run the rite would
teach a ceremony nobody has performed.

<<~/ahu>>

<<~ ahu #edges>>

!! Edges

<<~ loulou lar:///ha.ka.ba/lararium/mesh/carry-contract>>
<<~ loulou lar:///ha.ka.ba/lares/api/pono/persona-policy>>
<<~ loulou lar:///ha.ka.ba/lararium/mesh/siege-resilience>>
<<~ loulou lar:///ha.ka.ba/lararium/mesh/charter-chain>>
<<~ loulou lar:///ha.ka.ba/lares/api/pono/causal-islands>>
<<~ loulou lar:///ha.ka.ba/lararium/mesh/membership-doctrine>>
<<~ loulou lar:///ha.ka.ba/lararium/mesh/identity-classes>>

<<~/ahu>>

<<^ code="&#x0003;">>ni:///sha-256;MY9-_kOBo-vncwH1yzKt0AeeGPPsObUWz-vDQ6hTodo

<<^ code="&#x0004;" -> to=?>>
