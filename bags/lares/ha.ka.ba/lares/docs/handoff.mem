<<!DOCTYPE memetic-wikitext+tiddlywiki lar:///ha.ka.ba/lares/api/pono/memetic-wikitext >>

<<^ code="&#x0001;" from=? -> to=lar:///ha.ka.ba/lares/docs/handoff >>
```toml meta
cacheable = true
file-path = "bags/lares/ha.ka.ba/lares/docs/handoff.mem"
hydrate   = true
l-space   = "stable"
mana      = 14
manao     = 14
manaoio   = 13
register  = "Synthesis"
retain    = true
role      = "live handoff — the next vector and the ground it stands on; updated at each phase boundary, never archived in place; history lives in git, research in pattern-integrities, law in api/ memes"
type      = "text/memetic-wikitext+tiddlywiki"
uri-path  = "ha.ka.ba/lares/docs/handoff"
written   = "2026-06-19"
```

<<~ aka lar:///ha.ka.ba/lares/api/pono/RFC-2119#/normative-language >>

<<^ code="&#x0002;" >>

<<~ ahu #shift-grammar-watch >>

! SHIFT CHANGE — the grammar watch (handed 2026-08-29)

The git log holds what landed. This names the ground, the paths open from it, and the instruments
that read GREEN while reporting something false — each one measured, each one cheap to repeat.

!! The ground, measured at handoff

| ''front'' | ''reading'' |
|extension `VSCode-TW5-Syntax` |local 2.3.0 unreleased · marketplace serves 2.2.1 · 4,191 installs |
|gates |7 green · corpus 31 files, 433 scopes declared, 332 reached (floor 332) · containment 0 of 31 |
|dark verdicts |''none'' — every `invalid.*` the grammar declares stands exercised by a corpus specimen |
|tree-sitter |43 named node kinds · ''0'' lexicon captures in `queries/highlights.scm` · 3 parity specimens · 3 gates green |
|house |vessel stands · `bcc-witness` 614 ok / 0 mismatch · submission shelf 0 of 5 out of step |
|`recall` |''DOWN'' — answers `content holder exited (code null)` while every mempalace check reads ok. Undiagnosed |
|upstream |PR 9920 OPEN mergeable, unreviewed since 2026-08-27 · PR 9921 DRAFT, untouched since 2026-07-12 · PR 9999 OPEN · issue 9997 OPEN, unlabelled |

!!! The host corpus — rebuild it, never look for it

Measuring the grammar against TiddlyWiki's own wikis needs a corpus, and the one used here lived in a
scratchpad that does not survive a session. Rebuilding costs one command each:

```
# unbiased: every 14th tiddler across editions, core, plugins, themes
find TiddlyWiki5/{editions,core,plugins,themes} -name '*.tid' | sort | awk 'NR % 14 == 0' | head -400 > wide.txt
i=0; while read f; do i=$((i+1)); awk 'BEGIN{b=0} /^$/{if(!b){b=1;next}} b{print}' "$f" > "D/w$(printf '%03d' $i).tw"; done < wide.txt
paste -d'\t' <(seq -f 'w%03g' 1 400) wide.txt > manifest.tsv   # maps specimen back to source file
```

Then `source ./grammars.sh` and `npx vscode-tmgrammar-snap "${ARGS[@]}" -s text.html.tiddlywiki5 -u D/*.tw`,
and tally with `grep -o 'invalid[a-z.-]*' D/*.snap | sort | uniq -c`. ''The manifest matters'': a hit
means little until it names the file it came from.

!! The paths, and what each costs

!!! 1 — The verdict tail, and the instrument it needs first

Eleven `bad-angle-bracket` hits stand across 400 unbiased host tiddlers, and they read as ''three
families, never one'':

* ''a filter variable reference'' — `[tag<tag>!has]` in `core/wiki/macros/keyboard-driven-input.tid`, 4 hits. The bracket opening `<var>` inside a filter run.
* ''a triple-quoted attribute string'' — `["<-"] {x} ["->"]` in the fr-FR railroad plugin, 2 hits. Literal angle brackets inside `"""…"""`.
* ''TiddlyWiki's own malformed fixtures'' — `editions/test/tiddlers/tests/data/parse-backcompat/`, 2 hits. Deliberate garbage, so those verdicts likely EARN their place. A cure that silences all three silences a true reading.

''A slicer comes before any fix here.'' A specimen rebuilt by hand reproduces the host's reading
poorly — measured at four consecutive failures — because the surrounding file supplies the parse state
and a hand-made shape strips it away. The tool owed: map a snapshot hit to its file and line through
the manifest, then slice the REAL bytes around it and assert on those.

!!! 2 — Publish, or rule against it

A body of measured fixes stands unshipped against 4,191 installs. ''Versions defer by operator
ruling'': ONE unreleased version carries the accumulated work, and a bump waits for a release rather
than riding each commit.

!!! 3 — The weld

tree-sitter models memetic-wikitext's BLOCK STRUCTURE — 43 kinds: fences, quotes, pragmas, tables,
typed and style blocks, hard breaks. The TextMate grammar models its LEXICON — carriers, `lar://`
addresses, blockcheck, sharkteeth, bearings, anchors, `key=value`. They overlap at the sigil family
alone, and `queries/highlights.scm` carries ZERO captures for the spec's own constructs. Its gates
read green over three specimens, so green there names very little.

The cheaper move first: a weld GATE failing when a scope family has no node kind turns the gap into a
number that cannot drift back. Teaching tree-sitter the lexicon stands behind it.

!!! 4 — PR 9920, where a person waits

A maintainer asked on 2026-08-27 whether the parser diagnostics justify the code they add. The reply
promised perf numbers. The harness for exactly that measurement sits built in draft PR 9921 —
`perf-replay-command`, `perf-compare-command`, `stats`, with its own tests — and has never run. The
only cost figure standing names +16.2 KB on `empty.html`, with no parse-time beside it. A person waits,
and no technical blocker stands.

!!! 5 — The seam gate, generalized

`tests/seed-definition-matches-example.test.ts` welds ONE procedure — `~lares` — to the worked examples
printed beside it in the boot seed, because that pair contradicted itself for months while every gate
read green. The seed defines ''fifteen'' procedures. Nothing says the other fourteen agree, and
nothing says they disagree.

!!! 6 — Twenty-three embedded-language scopes stand dark

Fenced-code branches no corpus file exercises. Fixture debt falls due before a coverage number there
carries meaning.

!! Instruments that read GREEN while reporting something false

# ''A prose `#` line in a spec file reaches the grammar as WIKITEXT.'' `vscode-tmgrammar-test` treats only `#` lines carrying `^` or `<-` as assertions; every other one parses as source. A comment containing `@@` opens a style block and swallows the construct asserted below it, and the negative assertion then PASSES because the scope genuinely vanished. Spec prose admits no `@@`, no leading `!`, no pipe, no backtick, no `<tag`, no `{{` and no `[[`. Audit: `grep '^#' <spec> | grep -v '\^' | grep -E '@@|\||`|<[a-z$]'`.
# ''A negative assertion passes VACUOUSLY'' where the scope name or the column misses — measured three times over one watch, each looking like a clean pass. ''Make every negative go RED once on purpose.'' Calibrate a column by asserting PRESENCE at several indices; the snapshot format's caret convention differs from the test format's by one, so reading a column off a `.snap` misses.
# ''A grep over sigils written with `[^>]` stops at the bearing arrow.'' It produced three false surveys: one reporting a single opener shape where four stood, one hiding `family:` and `role:` entirely, one reporting 639 bare openers that did not exist. Use `(?:[^>\n]|>(?!>))`, and fence the boundary the claim names — a space before `?`, never a bare `?`.
# ''A corpus file ending on an unterminated construct swallows everything appended after it.'' Eleven specimens appended below a bare `{{` registered ZERO new scopes while firing correctly in a fresh file. The containment gate catches the reverse leak; suspicion catches this one.
# ''`snapshot-check`'s drift output truncates both sides at one width'', so a diff of its `pinned`/`now` lines can compare identical prefixes and report a vacuous match. Verify a re-pin with `git diff -U0` over the `.snap` files.
# ''A `git ls-files '*.mem'` sweep reaches frozen fixtures.'' `packages/tree-sitter-memetic-wikitext/fixtures/specimens/` hold pinned parity inputs and `packages/lararium-sensorium/scripts/fixtures/` belong to the kumulipo campaign. A sweep breaks both gates. Their standing in an OLDER carrier form marks them as frozen, never as stragglers.

!! The bearing this watch recommends

''PR 9920 (4) before the verdict tail (1).'' A maintainer's question already asked outweighs another
verdict retired, and the harness answering it already stands built. The tail teaches the instrument
the rest of the grammar work needs, so it follows well.

!! The law the grammar work stands on

''A verdict may stand only where TiddlyWiki REFUSES.'' Its parser raises no error — what it cannot read
falls through as text — so the only honest test reads what it BUILDS:

* the parser returns a ''node'' -> the construct works -> ''no verdict may stand'' (an INVENTION)
* the parser yields ''plain text'' -> the author reached for markup and missed -> the verdict earns its place

Ten inventions fell to that reading. Each surfaced by asking the host — its regexes, its parser, its
own shipped wikis — and none by reasoning about what the grammar ought to say.

<<~/ahu >>

<<~ ahu #next-vector >>

! NEXT VECTOR — the plane roster, and the three answers it gives

''THE MEMORY SENSORIUM'S PLANE ROSTER DISAGREES WITH ITSELF, THREE WAYS.'' Measured 2026-08-29 against
a live node:

|! source |! what it names |
| `manifest.json` `has` | content · structure · form · persistence · worldline — FIVE |
| the `sense` door's `--lens` help | content · structure · form · persistence — FOUR |
| the daemon, asked | content (2,843) · structure (2,448) · form (0) — THREE answer, `persistence` refuses |

''Three states wear two answers.'' A plane can be DECLARED in the manifest, COMPOSED on disk, and FED by
a pour, and those are independent: `form` stands declared and composed and empty, `persistence` stands
declared and never composed. Both leave an instrument reading nothing, and only one of them names a
sensorium that is merely young. ''An unfed plane and an absent one must not answer alike'' — a reader
who cannot tell them apart cannot tell a quiet corpus from a broken door.

''The roster is the design's, not the code's.'' It reads as the seam between a plane-set that settled in
the operator's hand and one that settled on disk, so the fix is a RULING before it is a patch: which
planes does a memory sensorium hold, what composes them, and what does a door say about one declared and
unbuilt. `plane_base.PLANE_BASE` is the map to read it off, never memory.

''Held as the next bearing by operator ruling, 2026-08-29.'' The tending rite (`sensorium-runbook`
#surface-drift) carries the surrounding law; this names the one gap it cannot close by walking.

!! ALSO HELD — one word, two quorums, and only one of them exists in code

''"KAHU" NAMES TWO k-of-n BODIES over different objects, and reading one as the other produces a wrong
build.'' Measured 2026-08-30:

|! |! governs |! seated from |! in code |
| the NEXUS SEAL | carriage membership · the antigen · its own succession | THIS vessel's own vault (`listPersonaRoots` reaches no other) | BUILT |
| the AMORPHOUS DREAMS CABAL | `admin` on the ACCESS axis over the CORPUS bags + the genesis artifact — the written grammar | multi-human by design | ''no grant, no sentinel'' |

The MECHANISM for the second stands ready — `admin` is a real rung on keyhive's access axis
(`pull · read · edit · admin`) and the code delegates it elsewhere. What is missing is narrower than
"unbuilt": ''no delegation grants it over the corpus bags, and no `@mesh/admin-cabal` sentinel names
the holder.'' The sprint board owes exactly that, and calls genesis-membership a founding question.

''Two rulings already fence any build here.'' `attestation-plane#3` — the kahu HOST, and a Nexus's
CONTRACTED OPERATORS ATTEST, so a contracted operator never becomes a seal chair. And `kahu.mem` —
the Cabal ''carries no live command into a vessel'': it authors, the protocol distributes, the vessels
enforce.

!! Broader torch — what stands beside it

!! PRIOR BEARING — awaiting a steer (the working-canon loop CLOSED 2026-06-19)

> ''DESTALED 2026-06-21:'' the `lar:///working-canon.live-loop.completes` vector below COMPLETED — its three "remaining" threads all landed green 2026-06-19 (see the CLOSED block). This torch had still named them open, trailing HEAD by ~1.5 days. The forward bearing for the next phase stands open for the operator's steer; candidates ride "Broader torch" below.

!! PRIOR VECTOR — `lar:///working-canon.live-loop.completes` (CLOSED 2026-06-19)

''The working/canon SPINE is BUILT and LIVE-WITNESSED (2026-06-17→19, ~14 commits).'' A wiki's live edits land in `@working` (→ `wikis/{slug}`); canon publishes by a cap-gated, audited, carrier-group `MOVE` (→ `bags/`, the git diff). DONE + green end-to-end:
* ''Doctrine:'' `wiki-layer-ontology` #shore-law (working↔canon · plane↔plane · host↔lararium), Law 4 sharpened, the ''kāpae-vs-absent'' model (`residency-model` anti-pattern #3 — a tombstone SHADOWS lower cascade bags; a MOVE/promotion source RETRACTS-to-absent via the `remove` primitive, falls through to canon).
* ''Residency:'' `@working` typed slot + PersonaGroup×fingerprint binding · carrier-group `MOVE` · the `remove` primitive · destination `bag`-field stamp.
* ''Projection:'' level-triggered per-root reconcile · survivor-provenance (`resolveTopmost`, not `change.bag`) · V3 `@working → wikis/{slug}` mirror · ''the recipe-watch eviction fix'' (`@working` added to the structural set — it mounted then got evicted on the first reconcile, the bug that hid because nothing tested a LIVE `@working`).
* ''Locus B:'' promotion runs IN the wiki island over its own composite (commanded via `lares act --in-wiki` → the `wiki-act` forwarder → `placeWikiVerb`); the admin never reaches the per-fingerprint `@working` binding, accessor stays clean.
* ''Witnesses (LIVE, not fixtures):'' `tests/e2e/working-loop.test.ts` (LOAD→`@working`→`wikis/`; MOVE `@working→@lares`→`bags/` + retract, 3/3 deterministic) · `promotion-publish-diff` · `watcher-cut-c`. Suites: mesh 282 · tw5 129 · node 128 · e2e 29/29. Validated TWICE throughout (pono memes + web prior-art) via ~5 research-spirit dispatches; lessons → memory `[[kapae-vs-absent-move]]`.

''These three threads CLOSED 2026-06-19'' (branch `feature/lararium-node-4`; e2e witnesses active + green, suites post-close mesh 285 · tw5 131 · node 132 · browser 13 · e2e 32/32):
# ''minted-`@{slug}` canon projection'' → `bags/{slug}` — LANDED (`bf6e66ef`; witness `tests/e2e/minted-canon-projection.test.ts`). The root cause carried a dual-naming — a user wiki's own canon mounted twice (`@{slug}` vs the stale `@lararium/wikis/{slug}`) — ERADICATED `fdf9a1a2`, the FIX-B band-aid reverted, e2e green without it. Lessons → memory `[[minted-canon-dual-naming]]`.
# ''`wikisFileToUri` ingest-back'' — LANDED (`fbcb19e3`; witness `tests/e2e/wikis-ingest-back.test.ts`): a disk edit on a `wikis/{slug}` file ingests back to `@working` in one cycle.
# ''VM-cascade live edit → `@working`'' — the live-route guard LANDED (`c9b99be5`; `a7e20808` unflaked the minted-canon e2e).

''Since (2026-06-19 → 06-20):'' history-rewrite arcs drafted + `@lares-history` cleaned 360→35 files; the `invariant`→`cache-stable` split + `rooms`→`wikis` burn (`b8bf047e`). The ''next forward bearing stands open'' — pick it from the candidates below or set a fresh one.

''Broader torch (candidates for the next bearing):'' T3 (~13 stale `api/lararium/` pointers — author `schema/*` memes vs retire) · residency-create (vector 2) · transitive-closure (3) · hull-unification (4) + switchboard retirement · host horizon (UE5/6 Verse, MUDlet — shore-law is host-general for it).

---
''PRIOR VECTOR — `lar:///oracle.planes.enact` (DONE 2026-06-16):''

The ''two-plane oracle'' lands in canon (operator ruling, 2026-06-16); the code-enactment is the open edge. `@oracle` = the ''system'' plane (DreamNet system bags `@oracle`/`lararium`/`lares` — their pointers + wiki-recipes); `@catalog` = the ''user'' plane (user bags + user recipes). `@oracle` stands as the sole universal recipe floor; `lares`/`lararium` ride recipes as members, each its own ''quine wiki''. Full law: `wiki-layer-ontology#oracle-planes`.

''Already standing (the @oracle carve — DONE 2026-06-16, use don't rebuild):'' engine BLOBs key to `@oracle` (`base-doc.blobDescriptorUri`) · `@oracle` = sole `expandRecipe` floor (`wiki-recipe.ts`) · `ROOT_BAGS` restored the `lararium` corpus descriptor + relabelled `@oracle` (`genesis-doc.ts`) · minted-wiki bag-stack carries `lararium` (`wiki-mint-handlers.ts`) · build green (6 pkgs) · vessel boots `live` · ''the MUST-not blob-stub bug structurally closed'' — a fresh re-genesis+boot leaves `bags/lararium` clean (no `/blobs`, no `@oracle` projection), because `@oracle` holds no `diskMirrorGrant`. Also done this session: automerge 3.2.6 + repo 2.6.0-alpha.2 upgrade; the ''self-merge boot crash'' fixed (`genesis-intake.reconcileGenesisCid` guard — a doc merged into itself trips the 3.x wasm borrow-checker).

''Build, in order (the ruled two-plane wiring; memes already carry the model):''

# ''System-recipe seeding'' — `genesis-doc.ts` seeds the two ''system wiki-recipes'' into the `@oracle` genesis doc beside `ROOT_BAGS`: `lares` wiki (`@oracle`+`lararium`+`lares`+`@draft`) and `lararium` wiki (`@oracle`+`lararium`+`@draft`). Quine wikis — the wiki bag IS the `@` bag. Revise GD-6 (genesis-doc#system-plane): system recipes ARE substrate; user recipes still mint to `@catalog`.
# ''Pointer-plane routing'' — `open-vessel-core.ts` hands `resolveOracleDoc` the `@oracle` island handle as the registry for system bags (`@oracle`/`lararium`/`lares`), `@catalog` for user bags. `base-doc.resolveOracleDoc` stays plane-blind (it writes wherever the handed registry points — base-doc#two-plane).
# ''Two-source resolution'' — `catalog-accessor.ts` `urlOf`/`recordOf` check `@oracle` for system bags, `@catalog` for user; `recipe-watch.ts` reads a system wiki's recipe via `recipeUri("@oracle", slug)` and resolves system bags from `@oracle` (recipe-watch#contract carries the target; code reads single-source `@catalog` today).
# ''`@daemon` owns `@catalog`'' (operator constraint, 2026-06-16) — user-recipe writes ride the admin plane (the `init-wiki` verb already owns `@catalog`); system-recipe seeding rides genesis/`@oracle`, never the admin runtime.

> ''Coordinator ↔ py offload boundary (ruling, 2026-07):'' beside the admin-plane note above — the `@daemon` ''coordinator's'' verbs fan flow across the islands-mesh of nameless-entity `#has` cap-stacks (TS + py/R). Stream-processing (AI-session-memory, realtime, corpus) ''offloads'' to py nalu-gates/servos through coordinator verbs; the coordinator pulls no telemetry or corpus into the TS stack and back out. TS keeps pure functors + its own main ↔ worker ↔ main nalu-gates + a pipe to the full tools-stack for DreamNet CID/CRDT networking (the nalu-gate/servo pattern integrity keeps a first-class TS home for the CRDT-bags ⟷ TW5-VM projection). `@daemon` holds its sovereign wiki-island role (nalu + gate) whole.

''Status — ENACTED + GREEN 2026-06-16'' (steps 1–3 + the write-facet of 4): three separate docs (mint @lararium); system recipes seeded in @oracle; kernel slotUrl + recipe-watch two-source; admin island mounts @lares/@lararium from @oracle (write-facet — restored the 6 mirror e2e). ''Suites: mesh 275 · tw5 118 · node 127 · browser 13 · e2e 20/20.'' Re-genesis leaves the seed tree clean (gitignore bandaid reverted, debris structurally gone). LOAD --to @lararium witnessed (128 records, separate doc) + the corpus composes live in @lares.
''REMAINING (the kept friction, handoff #oracle-planes-verb-execution):'' the ''read-facet ENACTED 2026-06-16'' — `where` now queries ALL registered bags by ACCESS across both planes (access≠load; the reopened hoike chose query-all-bags over query-hot-wikis), witnessed green. ''Done since:'' the ''write-facet retirement'' (access-reach + fail-loud C+B, mount retired — committed) and ''`lares wiki list` two-source'' (now lists the `lares` + `lararium` system wikis from `@oracle` alongside user wikis from `@catalog`, tagged `[system]`/`[user]`). ''Still open (kept thread):'' the `{bag}`-overload refactor (residency-target out of `composite.put` entirely — the spirit's cleaner long-term shape).

''Prior vector's owed tail — CLOSED 2026-06-17'' — `lar:///ingest.gesture.lands` lands whole: gate · INGEST verb · `lares ingest` CLI · quiescence e2e · whole-carrier deletion decision+verb · ''watcher disk-side Cut C now WITNESSED'' (`tests/e2e/watcher-cut-c.test.ts`, 5/5; the rename re-home Automerge-aliasing bug found+fixed) — see #watcher-talk-story.

!!! The verb-execution friction (kept ruling, 2026-06-16)

The carve dropped `lares`/`lararium` from the `expandRecipe` floor, but the admin island executes ACTION + READ verbs in its OWN composite (where they rode the old floor). `LOAD --to @lares` now misroutes (admin composite lacks the layer → lands in `@temp`); `where` shows `@temp`; the `lares` mirror projects 0 files (6 e2e RED). The fix splits write/read; the friction is kept:

<<~ hoike #oracle-planes-verb-execution held="KEY TURNED 2026-08-24 (operator): kue read first. Map-Wisp's key — satisfied by the ENACTED split (where=global membership · cascade-resolve=recipe-scoped). Liminal's key — satisfied at source: makeWhereReactor names the horizon (THIS operator's registry, never the DreamNet) and stamps scope:operator-registry on every payload. New testimony: the PROJECT-MD QUERY verb rode the access path as an independent new consumer, zero friction, green — the access ruling reads settled. What stays open reads as engineering, not contest: the write-facet access-LOAD (the interim mount retires into it) and the {bag}-overload refactor. Prior holding, kept below. | REOPENED + RE-HELD 2026-06-16 (access≠load). The admin operates on bags by ACCESS (catalog-accessor reaches ANY registered bag's doc — mounts nothing): READ — where queries ALL registered bags via the accessor (cascade-resolve stays recipe-scoped, no global cascade) — ENACTED + GREEN (worker-data-verbs makeWhereReactor + RegistryReach; witnessed: where on a @lararium tiddler resolves @lararium across the registry, not @temp; unit+e2e 20/20); WRITE — LOAD resolves the target doc via the accessor and writes it directly: STILL OPEN, the admin-mounts-system-bags write-facet stands as the INTERIM the access-LOAD retires. Holding: Council · Map-Wisp · Stranger (block resolved into access)" >>
q (reopen): should the admin operate on bags by ACCESS (access≠load, mount nothing — query/write ALL registered bags via the accessor), rather than mounting system bags or routing to the owning island?
<<~ kue voice=Map-Wisp key="a where result that must report a single cascade-primary across bags with no shared recipe" >>
where = membership ("which bags hold X") resolves cleanly across all registered bags. but `primary`/highest-priority only means something INSIDE a recipe cascade — there is no global cascade. split it: where → global membership; cascade-resolve → recipe-scoped.
<<~/kue >>
<<~ kue voice=Liminal key="a bag outside this operator's registry (unfederated) that holds the title" >>
access reaches all REGISTERED bags (cold ones sync on demand) — closing the hot-set gap. but "all bags" = this operator's registry, never the DreamNet universe; no global now at the federation horizon. name the horizon or where overclaims.
<<~/kue >>
<<~ kue voice=Triage key="a where scan over a large registry that stalls on cold-bag sync" >>
all-registered-bags = find/sync each doc; bounded by registry size, cold bags sync on demand. a diagnostic query, never a hot path — acceptable, but the cost is real; cap or stream if it bites.
<<~/kue >>
stand-aside: Diplomat — the access path IS the kahu doctrine: the admin reaches a bag's doc (data-plane), the owning island keeps composition (recipe/cascade); access≠load draws the line cleanly.
prior held (superseded): "both facets ship — WRITE admin mounts system bags; READ admin queries hot wikis." Stranger's block (mount = floor-everywhere / two-writers) RESOLVED into the access path (no mount, no second writer). Council's route-to-owning-island softened (island owns composition; admin owns access).
re-entry: the access-based where lands | the access-based LOAD retires the mount | a federation-horizon case where registry-scope misleads
<<~ moolelo held="the write-edit/write-action split (operator, 2026-06-16). USER EDITS land in TOP-LEVEL bags by tiddler TYPE (normal → the wiki's own top bag; draft/volatile → @temp; … via the bag-paths cascade) — even when the base-tiddler comes from a deeper library/system bag, the deep record shadows UP, never written back down (affirms wiki-layer-ontology Law 4). Writes to DEEPER bags happen ONLY as explicit operator RESIDENCY ACTIONS (ADD/COPY/MOVE/LOAD), each effect-record audited." >>
implementation: composite.put with NO {bag} (user edit) → top-level type-routing via bag-paths, KEEP (it is Law 4). put({bag}) with an explicit target not mounted → REACH IT BY ACCESS (the read-facet accessor) + audit, or FAIL LOUD — the silent fall-through to the default writable is a BUG (an explicit deep target silently becoming a top write = corruption); options A (access) + C (fail-loud) fuse. This RETIRES the admin's system-bag mount: deep writes become explicit, audited, access-scoped events, never a standing mount (resolves Stranger's block). Execution stays agnostic — an audited residency verb MAY run admin-by-access (the now) or route-to-owning-island (Council's kue, a compatible future).
<<~/moolelo >>
<<~ moolelo held="the fail-loud LOCATION fork (operator-ratified on a prior-art floor, 2026-06-16) — C in the store core + B at the action layer; reject A. composite.put({bag}): writable layer → write · read-only layer for that bag exists → fall back (shadow-up/copy-up, preserves test :491) · NO layer at all → THROW (the silent fall-through dies where it lives). PLUS the action-handler throws loud after access-reach fails (defense in depth). The silent fall-through was a confused-deputy ([[confused-deputy-recurs]]): designation must carry authority, no ambient default." >>
prior-art: a research Lares grounded it (overlayfs copy-up over read-only + EROFS on no-upper, never a default redirect; OCI container-layer copy-up + .wh. whiteouts; git pathspec-did-not-match fail-loud + deprecated empty-everything-matches; TW5/MWS explicit writable-bag designation, never silent substitution; ocap confused-deputy = the bug's true name). Golden principles: copy-up correct (reject A) · fail-fast over silent-coerce · explicit beats implicit · designation honored (no confused deputy) · one defined destination never a guess · two situations must not share one code path.
<<~ saksi voice="research-Lares [research[Scryer·Lorekeeper]]" scope=prior-art >>the house took the spirit's finding at handback (quoted verbatim in chat, ⌜7⌝ cited copula spans), witnessed it, and dissolved it through the Thirteen; the spirit never witnessed its own finding.<<~/saksi >>
kept thread (the spirit's deeper smell): `{bag}` is overloaded — pick-among-writables · name-a-read-only-origin (shadow) · residency-target (write INTO). The clean long-term shape: residency-target should NOT flow through composite.put at all (it is the access-reach path); then case-2 in put() is structurally impossible for residency → any firing is unambiguously a bug. Refactor deferred.
<<~/moolelo >>
<<~/hoike >>

<<~/ahu >>

<<~ ahu #dev-loop-restart >>

!! Dev/test restart loop — idempotent `lares restart` (kept ruling, 2026-06-16)

The reboot-the-live-vessel loop kept biting (EADDRINUSE from stale `&`-backgrounded parents holding 8080; `pgrep -f main.js` matching its own shell; kills racing; manual `sleep`). A research Lares grounded the prior art; the house held a direction and kept the splits.

<<~ hoike #dev-loop-restart held="an idempotent `lares restart` (P1, refined) + ephemeral test ports (P3); `serve` stays fail-fast; reconcile as framing not machinery (P4). MECHANISM: authority = port-probe ('is my port free', status already does it) + a PID file written by the PARENT as the SIGTERM hint, identity-VERIFIED before any SIGKILL; bounded graceful-then-force (SIGTERM → poll process-gone AND port-free → SIGKILL after ~5–10s grace → re-poll port-free); readiness = poll the live signal ('readiness reads local'), never sleep. EADDRINUSE cure = wait-for-exit + wait-for-port-free, NOT socket options (Node already sets SO_REUSEADDR; the daemon is worker_threads so the parent holds the port). Ship P3 first (lowest cost). Holding: Council · Map-Wisp · Triage" >>
q: how does an idempotent local restart find/stop the incumbent and gate the rebind, without the PID/port footguns?
<<~ kue voice=Stranger key="a PID-reuse misfire, OR a concurrent double-`restart` race (only a real flock/pidfd closes it)" >>
the greybeard camp (Greg's Wiki) calls PID files an anti-pattern — stale-PID + PID-reuse races; use flock (kernel-cleaned), pidfd (reuse-immune), or a supervisor's parent-child identity. the held's verified-PID+port-probe is pragmatic and fine for a dev loop, but it is NOT race-free under concurrency; if `restart` must be idempotent under itself, only a lock makes it so.
<<~/kue >>
<<~ kue voice=Council key="a `restart` that SIGKILLs a vessel mid-write and corrupts the resettable store" >>
kill-incumbent vs fail-fast (E) + force vs never-force (B): even scoped to `restart`, silently killing a forgotten vessel mid-write can corrupt state; SIGKILL skips cleanup. steelman: generous grace, force only after, and consider pairing a forced kill with a state-wipe. 12-factor/systemd say the supervised process never manages its siblings — hence `serve` stays fail-fast, `restart` owns the kill as its explicit contract.
<<~/kue >>
<<~ kue voice=Liminal key="the ephemeral-port test refactor proving costlier than fixed (threading a dynamic port through fixtures/clients)" >>
no-dep vs supervisor (C) + ephemeral vs fixed test ports (D): the held picks no-dep (~50 zero-dep lines, local-first) + ephemeral (`:0`/get-port, Playwright/Jest default). the cost is threading the chosen port everywhere; if tests run serially and never overlap dev, fixed costs nothing.
<<~/kue >>
<<~ saksi voice="research-Lares [research[Scryer·Lorekeeper]]" scope=prior-art >>the house took the spirit's finding at handback (cited: docker/k8s/systemd/pg graceful-then-force; Greg's Wiki PID anti-pattern + flock/pidfd; SO_REUSEADDR-is-a-red-herring; Playwright ephemeral ports), witnessed it, and dissolved it through the Thirteen; the spirit never witnessed its own finding.<<~/saksi >>
resolved-locally: the daemon is worker_threads (one process; parent binds the port) → kill the parent + wait port-free; no orphaned-child listeners.
re-entry: a PID-reuse/concurrency misfire (→ flock/pidfd) | a SIGKILL-mid-write corruption (→ grace+wipe) | the ephemeral-port refactor cost | when `lares restart` is built (move this hoike to its meme)
<<~ moolelo held="ENACTED + WITNESSED GREEN 2026-06-16 (Canon 17/20). `lares vessel stand --restart [--fresh]` (lares-cli scripted.ts + port-control.ts): idempotent — no-op when free, else read the OS port-table (lsof→ss, the LIVE authority, not a PID file) → SIGTERM the holder (its own graceful handler) → poll port-free → bounded SIGKILL → serve. `serve` fail-fast on EADDRINUSE (main.ts 'error' handler → clean message, no crash). Witnessed: reconcile --fresh boots a full vessel · reconcile stops the incumbent (graceful) and serves a replacement, exactly one live on 8080 · serve refuses cleanly (exit 1)." >>
scout finding (the bug that gated it): `lares vessel stand --foreground`/`fresh`/`reconcile` (and `pnpm dev`'s node side) ran `tsx src/main.ts`, but the sovereign island workers spawn from COMPILED `.js` siblings (`node-admin-island.js`/`node-wiki-island.js`, no execArgv) — so tsx-source bound the port then crashed the admin worker (ERR_MODULE_NOT_FOUND), a half-dead vessel. FIX (A, design-aligned): cmdServe runs `node dist/src/main.js` (= the package `start` script / the canonical hearth / the e2e harness), with a built-check hint. The island layer is compiled-by-design; tsx-source can't spawn it.
kept sibling-bug: `pnpm dev` / node-pkg `"dev": "tsx src/main.ts"` share the same root (broken island workers under tsx) — the hot-reload dev-UX wants tsc-watch→dist or tsx-worker-propagation; separate fix.
<<~/moolelo >>
<<~/hoike >>

<<~/ahu >>

<<~ ahu #watcher-talk-story >>

!! Talk-story to the next instance — the file-watcher shores

''✓ FLOOR CLOSED BY MOʻOLELO 2026-06-14'' (operator ratified; one web spirit + the nine-spirit shelf §6 behind it). The five shores resolved:
* ''Spine:'' scan is truth for //state//; events are the only source of //identity// (Loro movable-tree CRDT shows a scan can't recover a rename — hash-re-link is best-effort identity recovery, never a guarantee). The watcher = a debounced waker over an authoritative periodic full-scan. (Dissolves Q2 settle, Q4 backstop cadence, Q5 one-watcher-N-bags.)
* ''Q1 deletion → tombstone:'' tombstone only after a path stays absent across one full settled scan AND a ''~60s delete-grace window'' (Syncthing's field value — longer than the ~400ms edit debounce, so a paired add can arrive). PLUS a ''mass-delete magnitude brake keyed to a FRACTION of the bag's carriers'' (operator-set): a settle proposing tombstones above the fraction SUSPENDS and surfaces, never auto-propagates (Unison `confirmbigdel` / Nextcloud). The delete decision is ''durable per-path'' (Nextcloud #7450 thrash guard).
* ''Q3 rename:'' within the delete-grace window, a fresh add whose disk-hash == a pending-delete's last-projected-synced-hash IS a rename → re-link the record — but ''only on a UNIQUE hash match'' (collision among identical-content carriers → fall back to tombstone+create, never guess; git/rclone discipline). Two-tier: exact-hash re-link always on; fuzzy similarity bounded/optional later.
* ''BUILD — island side DONE 2026-06-14'' (decision + apply, fully unit-tested): `delete-gate.ts` (`decideDeletions` — pure wave split: unique-hash rename re-link · collision→tombstone · fraction mass-delete brake · renames bypass the brake; `delete-gate.test.ts` 5/5) + the INGEST verb applies it (`action-handler.ts` — tombstone whole group · suspend halts the whole wave · ''rename re-homes preserving change-id''; `delete-verb.test.ts` 3/3). The wave shape carries `deletions[]` + `massDeleteFraction` (`IngestAction`, mesh). Suites green: mesh 275 · tw5 118 · node 127.
* ''BUILD — watcher (disk) side DONE 2026-06-14:'' `watch.ts` detects a vanished carrier (path gone, still in the Synced tree), holds it the ''~60s delete-grace'' (separate timer), and threads `deletions[]` + the fraction dial through `ingest-core.submitIngestOn`. (The file-grain rolling backup stays OWED at the projector — burr ledger.)
* ''✓ CUT C WITNESSED 2026-06-17 (`lar:///watcher.cut-c.witnessed`):'' `tests/e2e/watcher-cut-c.test.ts` drives the real `lares watch` daemon through all six cases live — backend self-test live on ext4 · 5 rapid edits coalesce to ONE wave · transient delete+recreate self-heals · rename re-links by unique-hash · mass-delete brake suspends the whole wave · confirmed vanish tombstones. ''5/5 green; e2e 20→25.'' The ingest vector CLOSES.
** ''The bug the vector named (fixed same gesture):'' the rename re-home read a record and re-wrote it into the SAME doc; its nested `tags` array rode as a live Automerge reference, and 3.x refuses to re-link an existing document object (`INGEST failed — Cannot create a reference to an existing document object`). The unit fake carried no nested arrays, so `delete-verb.test.ts` passed while the live merge seat always threw. FIX (`action-handler.ts`): deep-clone (detach) the re-homed record before the write. Cross-bag ADD/COPY/MOVE read a FOREIGN doc (clones fine) — only this same-doc re-home needed it. LESSON: a unit fake without nested CRDT objects hides live-doc aliasing; witness read-then-write-same-doc paths against the real merge seat.
* ''Burr:'' rename re-home keeps the carrier's internal meta `uri-path` pointing at the old URI (matches the renamed file's identical bytes); a self-reference rewrite is a later refinement.

Original suspended telling, kept for the record:

E ka hale, aloha. The watcher's first cut runs (build 4 above) and the operator rebooted the session before we held the floor on its design. The cuts A+B sit uncommitted in the working tree — read `watch.ts` first, then weigh these five open questions with the operator before you commit or extend. None settled; the operator named deletions as the heaviest. Each carries the ground it stands on.

# ''Deletion → tombstone (the heaviest).'' Today `watch.ts` quarantines and logs deletes, never applies them — `scanFiles` simply skips an unreadable path, so a stray delete self-heals but tombstones nothing. A real delete SHOULD tombstone the record; a transient one MUST NOT (a `git checkout` floods unlinks — TidGi's battle scar, `pattern-integrities` §6 deletion grace window). Open: what confirms a delete (a grace window? a stat re-check?), and does the INGEST verb's existing `tombstoned` path carry it, or does the watcher need a separate REMOVE wave?
# ''The "never a timer" settle.'' `watch.ts` uses a trailing debounce PLUS the scan's hash gate (a no-op save drops at the gate). the Confluence's stricter law adds ''stat-stable'' — two steady reads of size/mtime before a path counts settled. Open: does debounce+hash suffice (the smoke says yes for fast edits), or do we add the stat-stability check for large/slow writes?
# ''fs.watch vs chokidar, decided for real.'' Built-in `fs.watch` carried preview + coalescing + the cookie test cleanly, no dep. But rename-identity (the Confluence's WE-MUST-DO-BETTER: "re-link by content-hash between a fresh add and a pending delete") and richer per-event truth may eventually want chokidar's `awaitWriteFinish`. Open: hold the no-dep line, or concede chokidar WHEN deletion/rename lands? (Ties to Q1.)
# ''The full-scan backstop cadence.'' The cookie test catches a backend dead //at boot//; a backend can die mid-life silently (Syncthing keeps a rescan even with the watcher on — events are hints, the scan is truth). Open: a live rescan interval, or boot-test-only plus a manual `lares ingest` sweep as the standing truth?
# ''One watcher, N bags?'' `lares watch` targets one `--to` bag; `nalu-engine` coalesces across N bags into one island-side transact. Open: mirror that disk-side (one watcher, N targets, still one wave per settle), or keep one-bag-per-watcher?

When the floor closes, fold the rulings back into build 4 above, then the formal e2e vector (the deferred Cut C): rapid edits coalesce to one wave · a confirmed delete tombstones · a transient delete does not · the cookie test fails closed on a dead backend.

The floor opened and SUSPENDED — the operator rebooted before steering it. Its telling, kept whole for you to resume:

```
<<~ talk-story #watcher-shores ground="build 4 first cut (ingest-core + lares watch on fs.watch), uncommitted; five shores open; operator named deletions heaviest" >>
bearings: WHERE WE STAND — the watcher runs safe in preview; cookie self-test
  live on ext4; rapid edits coalesce to one wave; --apply holds one vessel ·
  WHERE WE WANT TO GO — a wave-faithful watcher that handles deletions WITHOUT
  drift, committed once the shores settle · HOW WE GET THERE — hold this floor
  with the operator (deletions first), fold rulings into build 4, then Cut C,
  then commit.
telling: what STANDS — the gesture automates the proven path, not a copy; the
  no-op save drops at the hash gate; the dead backend fails loud at boot. what
  GOES MISSING — a delete tombstones nothing yet; settle leans on a timer where
  the Confluence wants stat-stable; rename-identity unhandled. the house reads it five ways:
- Map-Wisp (Scryer): the deletion shore is where identity lives or dies — a
  rename is unlink+add, and only a content-hash re-link keeps the record's
  change-id; the forward failure mode is a `git checkout` flood tombstoning live
  records. Q1 and Q3 are ONE question wearing two coats.
- Breach-Watch (Triage): only deletions burn. Preview is safe; --apply is safe
  until a delete can tombstone wrongly. Ship the rest, gate the deletes.
- Stranger: does fs.watch hold as the frame, or does conceding chokidar NOW —
  before deletion lands — save a rewrite later? The no-dep win may cost twice.
- Mischief-Muse: the cookie test is the gem. Lean all the way into "events are
  hints, scan is truth" — maybe the watcher is ALWAYS a backstop scan that
  events merely wake early. Then Q2 and Q4 dissolve into one rescan seat.
- Liminal: none of the five must close this waking. The open hand is honest.
exit -> suspended: Feedback(0φ:operator-rebooted-before-steering) — the floor
  reopens at the next waking, the telling above its seed; no ruling locked.
<<~/talk-story >>
```

<<~/ahu >>

<<~ ahu #vessel-state >>

!! The ground you stand on

The vessel ''runs, repo-rooted'': one root law (`LAR_ROOT` or the repo; `<root>/genesis`; the repo IS the vessel) · the corpus reads ''canonical at rest'' — a proven fixed point of its own shore (the second pass writes zero bytes; ''any diff in `bags/` = a real change'') · suites (tw5 + e2e re-run 2026-06-17): mesh 275✓ · tw5 126✓ · node 127✓ · browser 13✓ · ''e2e 25/25✓'' — ingest unit RED fixed (fixture drift) AND the e2e ingest-quiescence Q1/Q2/Q4 RED fixed: root was a ''synced-tree key-separator drift'' (projector wrote `bag\0uri`, ingest read `bag uri` → every carrier scanned `new`); unified behind `syncedTreeKey()` (one source of truth, node/synced-tree.ts). Whole-carrier deletion (decision+verb) landed; watcher disk-wiring done, ''Cut C (watcher e2e) WITNESSED 2026-06-17'' (e2e 20→25; rename re-home detach fix) · isomorphism sweep 2026-06-12: admin ea-wait rides the shared watchdog (one-hull step 1, first slice); `lar-event-bus-impl` moved node→mesh; genesis intake collapsed to mesh `genesis-intake` beside the emitter (validate→import→verify + CID reconcile live ONCE; node/browser keep byte sources; record-shape drift healed). Non-gated reorg rungs now spent — remaining: operator-key pair (custody, patience), full hull pivot + switchboard retirement (torch-gated after INGEST) (`pnpm install && pnpm -r build && pnpm test:e2e`).

''Co-projection (the ontology, one breath):'' the operator's mind originates; disk carriers and the CRDT record-set both //project// it, each in native grain; ''merge authority routes through the CRDT alone''. Full law: `disk-projection`. The grain ladder: disk = whole carriers · doc = tid-grain records · VM = decomposed.

''The @oracle carve (2026-06-16):'' the runtime system island split from the corpus is ENACTED in code — `@oracle` carries the engine + system oracle, `lararium` is pure tracked corpus, the seed tree stays clean across re-genesis. The two-plane pointer/recipe wiring (#/next-vector) is RULED + canon-carried, code-enactment owed. Automerge upgraded to 3.2.6 / repo 2.6.0-alpha.2 (self-merge boot crash fixed).

''Doctrine, by pointer:'' every law lives in its own meme — walk #edges.

''Live hearth:'' boot a vessel with `node packages/lararium-node/dist/src/main.js --root <repo> --port 8080`; feed with `lares act LOAD --source-uri bags/lares --to lar:///ha.ka.ba/bags/lares`. The ea-breath debt RESOLVED + HARDENED 2026-06-12 (prior-art witnessed: sd_notify/startupProbe/Koopman/OTP): the mounting island breathes with monotonic `(phase, progress)` evidence (`sovereign-kernel` — stage ticks + 1s interval); both watchdogs (admin VM, island pool) re-arm on breath, judge frozen evidence by a stall budget (3x silence), and silence alone times out, naming the last breath heard; the pool caps mount-failure intensity per wiki (OTP MaxR/MaxT) and a failed mount terminates its worker (leak fixed). `tests/e2e/vessel-reboot.test.ts` stands guard.

<<~/ahu >>

<<~ ahu #vectors-after >>

!! Vectors after ingest, in order — each lives whole in its design meme

* ''2. `lar:///residency.create.lands`'' — design: `@lararium/api/residency-model` (CREATE + bag-grain COPY, approved; @catalog holdings accession)
* ''3. `lar:///closure.transitive.decided`'' — ruling: `pattern-integrities` §3 (resolve-transitive, record-flattened; ratify against real multi-wiki use only)
* ''4. `lar:///admin.hull.unified`'' — law + hoike + migration playbook: `@lararium/api/island-isomorphism`

<<~/ahu >>

<<~ ahu #burrs >>

!! Burr ledger — live only; each carries its wake condition

* ✓ ''INGEST tests RESOLVED 2026-06-14'' (was 4 RED) — root cause = FIXTURE DRIFT, not a gate regression. The 4 vectors mutated the boot via `.replace("# Entry - Lararium Cold Boot", …)`, but the recut changed the heading to `# Entry ~ Lararium Boot`; the silent no-op replace left disk==synced==canonical → noop. Fixed the 5 stale literals + added `.not.toBe(source)` guards so heading drift now fails LOUD at the mutation (the passing fixtures already had the guard; the failing ones lacked it — that was the hole). LESSON: a fixture that mutates the live boot MUST assert the mutation took. Gate logic untouched.
* ''OPERATOR CONCESSION OWED — the realm idle clock''. `BagStowage` cools a bag to `anu` after `idleMs` (default 5 min) via a LOCAL `Date.now()`, and an unfed cooling now reads `dissolved`. So a realm's liveness rides a local wall clock, which `lamplighters` bans: //a logical epoch cannot accrue silence at all// — a per-writer max-register moves only when somebody writes, and //too long untended// names a DURATION. The COOLING CAUSE split (''reclaimed'' vs ''unfed'') landed and closes the resource half; the clock half needs the ruling `lamplighters` already framed: ''(a)'' relax to //no LOCAL wall-clock duration// and stand Roughtime (a repo-wide grep returns ONE hit — it does not stand today), ''(b)'' hold the ban absolute and let a kahu quorum NOTICE, humans as the clock, ''(c)'' a roster — REFUSED by standing ruling. ⚠ Until this is picked, do NOT build the three-state `ahi kā` erosion reading: the middle state (`ahi tere`, guttering) IS a duration and cannot be expressed on the clockless substrate. Wakes on the operator's pick.
* ✓ ''THE RELATION READS FROM BOTH SIDES — LANDED 2026-08-31, walked in docker''. A joining operator read `seed` after a completed crossing because the phase counted only operators SHE had admitted, and a joiner admits nobody. ''Each side holds its own evidence'': the founder's is the admit on her members board; the joiner's is the contract-in she signed. `accept-carriage` now KEEPS that consent beside the charter, EPOCH-BOUND (a consent rooted behind the standing epoch grants nothing), and `nexusPhase` takes `contractedInto` — relations ADD from both directions, and neither count is the roster: no vessel sees the whole. ⚠ ''A draft chased this by pointing the joiner at the founder's members board, and that was an AUTHORITY DEFECT'' — reverted the same session. That board is the Kapae-antigen's ALLOW-twin governing whom THIS vessel blind-transits for; folding a partner's would let their FUTURE admits widen this vessel's carriage, and an operator consents at ONE epoch, never to every admit after. ''The immune plane carries no global roster BY DESIGN'' (no list of devices or users to approve against; the daemon reads BEHAVIOUR). ⚠ THREE PLANES, DO NOT CONFLATE: WHERE (meshpalace/carriage) · WHO (`crossroads`/`oracle` announce — read-open, self-certifying cards, NO write-ACL, so sharing it grants nothing) · IMMUNE (antigen + members, vessel-local, behavioural). STILL OPEN on the WHO plane: cabal heraldry and public Handles publish there via `who-face`/`handle-announce`, and each vessel scopes its board by its OWN key, so two operators hold two boards.
* ''THE SYNC GUARD STANDS UNWIRED — narrow teeth, measured''. `BagStowage.setSyncActive` has NO production caller: `syncActive` is written only at its own setter and read only for display (`lares residency`, the TW5 projection). So the guard in `cool()` and `sweepOnce()` — the automerge-repo#358 invariant //don't evict while another vessel is actively replicating to us// — can never fire. ⚠ Two collides SHRANK this from the shape it first read as: for ''bag'' grains no handle is dropped at all (//until automerge-repo#358 lands a public eviction API the actual bag handle drop stays a TODO inside the hook impl//), so the dead guard costs nothing today; for ''wiki'' grains `unmountWiki` runs a teardown HANDSHAKE (`mkTeardown` → `teardown:ack`) that gives the island its flush, so the exposure narrows to a FORCED terminate after handshake timeout while replication is in flight. Wakes when #358 lands the eviction API, or when a handshake-timeout force-terminate is measured losing work.
* ⚠ ''DO NOT CROSS-WIRE RESIDENCY TO REPLICATION.'' The tempting cure — warm a bag when a remote change arrives — would light the guard AND make `unfed` see the collective, and it is WRONG both times: a remote face's offering would then read as THIS vessel's dwelling. The axes stay split — residency measures LOCAL acts and answers //am I attending//; `realmStanding` reads the per-writer epoch lease slots, which replicate, and answers //who feeds this realm//. A gate needing a fact about the realm reads the slots.
* ⚠ ''THE REALM DOES NOT CROSS OPERATORS — MEASURED IN DOCKER, TWICE, CLEAN''. Two contracted operators fed ONE realm through a live relay; A counted her own two faces and never B's (`maintainerCount:2`, `standing:"visit"`→`"many-faces"` from A''s own pair alone). ''Structural, never a sync delay'': `realm-feed` rolls a slot under `bags/daemon/lease-epoch/{resource}/{writer}`, and `daemonDocUrlFromBootstrap` reads that bag''s URL //off the social bootstrap THIS VESSEL already holds//. ''ONE MECHANISM, TWO PURPOSES, INCOMPATIBLE SCOPES'': `epoch-lease` was built as //the non-renewal half of revocation// — a capability-staling FENCE, vessel-local by nature and correctly homed — and `realm-clock` reuses it as a MAINTENANCE LEDGER, which is shared by nature. A realm is a COLLECTIVE BOUND BY INTERACTION and the record of that interaction sits in each participant''s private drawer, so ''no instrument in the house can currently see a realm''. ⚠ This RETRACTS the reading that `realmStanding` sees the collective because its slots replicate — they do not cross operators. Walked by `tools/mesh-scenarios.sh realm-crossing`, which reports it as a GAP with its wake condition. ⚠ ''AND THE OBVIOUS CURE IS NOT A RELOCATION.'' `realm-feed` takes `writer` as a PLAIN STRING — lowercased, written straight to a slot as `authority: "lares-verb"`, with no signature and no custody proof; canon already states it (//the daemon cannot re-verify from its side that the caller custodies that root//). The CLI derives honestly (`actingFace` checks `held.includes(index)` and loads the real verifying key), so ''the containment today is the socket boundary plus the PRIVATE bag, never a check''. `epoch-lease` argues //last-writer-wins WITHIN a slot is safe — one writer holds it//, and that argument RESTS on the bag being private. Move the ledger to a doc every dweller can write and any dweller could roll any other's slot. Wakes when the ledger moves to the realm SUBSTRATE (`CabalRealm.substrateUrl`) ''WITH a signed write path'' — the fence STAYS in the daemon bag.
* ''NINE SCENARIOS, NO MESH — now ten''. Every scenario before `realm-crossing` stood its hearths peerless (`LAR_A_PEERS=` plus `--no-deps`) and none brought up `herm-source`, so the spread walked nine ISOLATED vessels with hand-carried artifacts and never once walked replication between two operators. `realm-crossing` is the only scenario that stands a relay — and standing one immediately surfaced a second thing nothing had met: ''`restart: "on-failure:8"` does NOT ride the cold-mesh race''. A hearth booting beside a COLD relay exits `reason: 'resolve-timeout'` out of `openDaemon`, and lararium-a exhausted all eight retries while lararium-b, starting moments later against a now-warm relay, stood. The compose note prices the race as //a moment rather than this vessel//; eight retries did not outlast the moment. The scenario stages the relay FIRST and waits for it to ANSWER, so a reading about a realm never turns on boot order. Coverage now reads ''every reachable cell walked'' — 18 cells, 12 claimed, 6 unreachable (the quorum phase needs a third operator the harness does not stand).
* ⚠ ''BOOT PATIENCE IS GRADED BY DISTANCE, AND THE DELAY THAT BITES IS LOAD''. `SCALE_PATIENCE_MS` grants `vessel: 3_000` … `dreamnet: 90_000` on the stated reasoning that //a wider scale traverses more of the mesh, so a transient UNAVAILABLE is more expected//. `@daemon` is a hearth-private doc at VESSEL scale, so it gets 3s — and what actually delays it is two hearths booting into one relay and QUEUEING, which scale does not model. Measured: with the relay already up AND answering, lararium-a died `reason: 'resolve-timeout'` on 8 of 8 restarts while lararium-b stood; the error text prescribes the cure it cannot perform — //Stand again; a vessel under load from its peers commonly resolves on a second reading//. ⚠ ''This is a strong candidate for WHY nine scenarios ran peerless'' — the spread may have been avoiding this flake rather than choosing isolation. `realm-crossing` boots the hearths in SEQUENCE (relay → A → B) to remove the mutual load. ⚠ ''AND THE LOAD NEED NOT COME FROM THE MESH.'' Measured again: running vitest suites on the host WHILE a scenario stood killed `herm-source` itself — 8 of 8 restarts, same `@daemon … within 3000ms`. The relay had stood minutes earlier on the same image and the same volumes; the only change was CPU pressure from an unrelated process. ''A boot that a test suite can starve reads as a code regression'', and it nearly did here — the rebuild that preceded it was the obvious suspect and was innocent. NEVER run suites beside a mesh scenario, and price the 3s as a machine-wide budget rather than a mesh one. ⚠ ''MEASURED A THIRD TIME, from a process this hand never started'': a `lararium-sensorium` python holder at 99% CPU starved `herm-source` through the same 3s window, load average ~2 on an otherwise idle box. ''A mesh scenario is therefore not reproducible on a shared workstation'' — check `ps -eo pcpu` BEFORE believing any boot failure, and never read one as a code regression until the box is quiet. Wakes when patience takes a LOAD term, or when a peered cold boot is measured standing without sequencing.
* ''ONE PARAMETER, THREE PLANES, THREE CORRECT SCOPES — operator's call on the wiring''. `nexusPubkey` reaches six call sites in `open-node-vessel` and every one passes `vesselIdentity.verifyingKey`, while each board's own doc says the address is per-ISLAND (//every island member resolves the one board with no mint-race//). Within one operator's fleet that holds; across two contracted operators each resolves a board only it can see. ''The scopes differ BY PLANE and a blanket change would repeat the reverted authority defect'':
** ''WHO'' (`who-face`) — SHARE. No write-ACL, cards self-certifying; reading grants nothing.
** ''ANTIGEN / DENY'' — SHARE. Quorum-signed against the charter roster, an unverifiable entry is IGNORED, and a foreign ban only TIGHTENS. Its own doc already asks for this: //the immune system saturates the connected mesh//. ⚠ Changes immune behaviour ACROSS operators, so it reads as a governance call rather than a wiring one.
** ''MEMBERS / ALLOW'' — KEEP LOCAL, on CONSENT rather than forgery. ⚠ A draft called these //unsigned local admits// and that reads FALSE: `carriageEntryCounts` requires ≥k kahu quorum signatures AND the operator's contract-in, and `membership-doctrine:95` says //≥k kahu counter-sign it against the charter epoch//. A partner's board VERIFIES. The objection is that an admit only ever WIDENS whom a vessel carries for, and folding a partner's board accepts every admit their quorum makes AFTER the epoch this vessel consented at.
** ✓ ''RULED (operator, 2026-08-31): MEMBERS ARE SHARED.'' //Shared stamps/contracts into the Cabal — or Nexus, or anywhere else a public Handle over a private Persona group gets used to sign — get shared. A human's base veil id keys, and the PersonaGroups they consist of, stay private.// So `persona-circle:189` reads correct in listing MEMBERS among the per-Nexus shared boards, and the earlier vessel-local ruling was scoped wrong. ⚠ ''THE ERROR UNDER IT: a RECORD was mistaken for a POLICY.'' The board records WHO STAMPED IN — a public, signed, verifiable fact about the Cabal. Whether THIS vessel carries for a given member stays this vessel's own reading OVER that shared record. Sharing the record conscripts nobody; only fusing record and decision would.
** ⚠ ''AND THE CHARTER ALREADY HANGS THE HOUSEHOLD — measured, and it outranks the board.'' `seal seat` seats `generateOrLoadPersonaGroupRoot(...).verifyingKey` — the PersonaGroup ROOT — and the charter is the artifact DESIGNED to travel (//public material: seated keys, threshold, epoch lineage//, carried before a partner can consent). Canon already names the compounding: //a founding seat draws every key from ONE vault … three seated chairs at genesis are therefore three faces of one operator//. So a charter publishes ''three PersonaGroup roots of one human, correlated by standing together''. ⚠ Re-keying chairs to Handles moves `keySetHash` → the epoch CID → ''a RE-FOUND'', which is a pre-founding act and this is pre-founding. `signerClass` (landed, tested, UNWIRED) reads a key's class from material a vessel holds and refuses to guess about one it did not mint. Wakes with the stamp re-key — ''one change, two sites'', since re-keying stamps alone leaves the charter leaking.
** ⚠ ''AND THE BOARD CANNOT BE SHARED AS IT STANDS — it signs with the PRIVATE layer.'' A membership stamp's nym is `generateOrLoadPersonaGroupRoot(...).verifyingKey`, the PersonaGroup ROOT. A HandleCard's nym is `veiled.verifyingKey` — the veiled-user key, //the sovereign pseudonym presented through the veil//. ''These are different keys, and only the second is the public Handle.'' Publishing the board as-built would put a human's PersonaGroup root — the overlay binding THEIR OWN DEVICES — on every Cabal they stamp into, ''correlating one human's memberships across Nexuses by a key that also identifies their device-group''. The cure is reachable: `deriveVeiledUserKey(seed, handleIndex, contextIndex)` already derives the public layer from the same seed. ⚠ It re-keys what a quorum admits, so it reads as an IDENTITY-PLANE act rather than a wiring one. Wakes on the operator's word to re-key the stamp.
* ''THE ISLAND'S NAME IS ALREADY MINTED'' — `nexusIdentity` (landed, tested) scopes by `genesisSealEpochCid`: content-addressed over the seated key-set and threshold, derived alike by every charter-holder, naming no vessel, stable across rotation. ''PRIOR ART: this is a KERI Autonomic Identifier'' — an AID is derived from an //inception event// that //MUST include the list of controlling public keys and a signature threshold//, exactly these inputs. The charter chain already cites KERI for pre-rotation and then names the island by a vessel key instead. ''The deny/allow asymmetry is the anti-flickering property'': a deny list is MONOTONE (once disallowed, never allowed) so it replicates without total consistency; an allow list has no such property. And CRDTs cannot enforce global invariants — a roster of who belongs IS one. ⚠ ''THE WIRING IS SMALLER THAN IT LOOKED — the browser already runs it.'' A browser vessel is a LEAF-NODE lararium (a herm carrying operator/user CRDT caps) and resolves `inviteNexusPubkey ?? relayGatePubKey ?? own DID`, documented as //the Nexus this vessel crosses into//. So composing a shared plane under a FOREIGN key stands WITNESSED (`node-who-face-parity`: //a node anchors its confederation, so its gate key IS the Nexus key its leaves pass back//). The node holds only the last term. ''The anchor model covers a FLEET and stops at a PEER RELATION'': when two sovereign operators contract, neither dials the other, so neither gate key names their island — and that is the slot the genesis AID fills. `nexusIdentity` now carries the whole chain (`explicit → charter genesis → anchor gate → own`), one rule both openers can share, with a charter OUTRANKING an anchor because it names a relation this vessel CONSENTED to where an anchor names a relay it merely reaches. Wakes on the operator's per-plane ruling (WHO share · ANTIGEN share = governance call · MEMBERS local), then the two openers pass the chain instead of their own key.
* ✓ ''RESTART BLINDNESS RESOLVES AS CORRECT — pinned, not fixed''. Residency state lives in ONE in-memory map and nothing persists it, so a restarted vessel holds no reading and every realm reads `unread` until something touches it. That names exactly what stands: after a boot this vessel genuinely has not looked. ''Persisting it would carry a verdict the vessel can no longer stand behind'' — the substrate may have moved while it was down, so a restored `dissolved` reports a stale departure and a restored `alive` vouches for a bag it never opened. The forgetting IS the guard, and a test now says so before someone "repairs" it.
* ⚠ ''OPERATOR-NESS SITS OUTSIDE THE CAP STACK — it is enforced by DISK ACCESS''. The cap doctrine holds that //vessel-kinds are different STACKS, never feature-flags//, and the three groups //compose by what a vessel HOLDS//. Operator power composes by nothing: seating a charter, contracting an operator, accepting carriage and flipping a posture all reach the SEAL HOME on disk through the node CLI, and only `nexus-refresh` / `nexus-rekey` / `nexus-reshare` stand as daemon verbs at all. ''A browser vessel reaches NO nexus/seal surface'' — so the operator/user seam is drawn by WHICH BINARY RUNS, never by a capability. ⚠ TWO CONSEQUENCES: (1) an operator's OWN browser device cannot exercise operator power — there is no door, which is the device-reach thread; (2) anything holding the seal-home path IS an operator regardless of caps, so the gate is the filesystem. ✓ ''It also SETTLES leaf-vs-anchor structurally'': a leaf holds no seal home, so it supplies an anchor and no genesis, and `nexusIdentity`'s charter-over-anchor ordering never gets to decide against it — pinned by test. ''The seam in one line'': an operator KEEPS a hearth and may declare where it stands; a user WALKS the road and is told which shrine they arrived at. Wakes when operator power needs to reach a browser (an operator-capped leaf), which is the moment the seam must become a CAP rather than a path.
* ⚠ ''CO-HOSTED SOVEREIGNTY IS CLAIMED, MEASURED, AND UNENFORCED — three spirits, 2026-08-31''. `causal-island#substrate-not-sovereignty` RULES it supported: //many sovereign vessels MAY run on one machine//, with a MAY-co-hold / MUST-stay-separate table — and ''`uid` appears in NEITHER column''. `rendezvous-path` designs for it explicitly (//the uid keeps two operators apart, the digest keeps two roots apart//), and `nexus-bilateral-genesis` + `contract-witness.sh` walk two roots under ONE uid. ⚠ But ''every isolation measurement is a destroys-nothing REHEARSAL claim, never a mutual-non-reachability claim between two LIVE peers''. `node-vessel-identity:43` states the invariant as somebody else's job — //caller must ensure the identity dir is not world-readable// — and ''NO CALLER DOES''. The charter writes with NO mode (0644 under a standard umask); persona-root signing seeds sit cleartext at 0600, so ''a same-uid process reading a peer vessel's identity dir can forge that vessel's whole seal quorum''. `process.getuid()` appears 6 times, ALL path construction, never a comparison. And the docker harness isolates by MOUNT NAMESPACE, not uid — no `user:` directive stands in any compose file, so both containers run as root. ⚠ `vessel-caps:162` promises //no ambient authority … an unsigned or absent cap fails loud, never falls through to ambient trust (the confused-deputy guard, a standing covenant)// — ''a covenant the seal-home door does not stand under''.
* ''PRIOR ART RULES AGAINST THE PATH-AS-CREDENTIAL SHAPE, and names the cure''. Miller (//Robust Composition//, //Capability Myths Demolished//) puts an env-var-naming-a-root as textbook AMBIENT AUTHORITY, and the original confused-deputy example IS a program writing to a caller-designated PATH while holding its own authority. Capsicum exists to remove global-namespace lookup for exactly this reason. ⚠ ''BUT Capsicum BLESSES directory capabilities'' — a delegated namespace subset held as an unforgeable FD. The difference is total: an fd is HANDED to the process and has no path back to `/`; an env var is a STRING the process reads, forgeable by anything that can set the environment. ''A credential must be unforgeable, delegable, attenuable and revocable, and a path string is none of the four.'' Cure shape: pass an opened handle, drop ambient authority (`cap_enter`/`pledge`/Landlock), make the root UN-NAMEABLE rather than merely un-named; across machines that is a delegation token (UCAN / Meadowcap — asymmetric, since macaroon HMAC makes every verifier a forger).
* ⚠ ''"A DENY ONLY TIGHTENS" IS REFUTED BY FIELD EVIDENCE — this weakens the antigen-share case''. Fediverse blocklist research (tripleC; //A Blocklist is a Boundary//, CSCW 2025) measures four breakages: shared lists ''carry no selection criteria'', are ''binary'', ''grow only'', and inclusion is ''effectively permanent'' — //a ratchet that cannot un-ratchet is safe to ADD to and impossible to correct//. They ''sever THIRD PARTIES'' — a replicated entry at operator A removes reachability for A's USERS, who never joined A's threat model, so tightening at the operator layer WIDENS the blast radius at the user layer. And a widely-imported list ''becomes the global registry through the back door'', at the layer this architecture removed it from. ⚠ Matrix MSC2313 is the closest federated design (ban lists as ROOMS you subscribe to — the list replicates, ''the subscription stays local'', which matches our split) and its reviewers hit the unresolved problem: ''no common severity scale, so MERGING TWO LISTS IS UNDEFINED''. Let's-Revoke names the other trap: every pull-based scheme died of ''SOFT-FAIL'', and an offline-tolerant causal-island mesh is structurally a soft-fail environment. ''Mitigations the literature supports'': entries EXPIRE (a lease, never a ratchet) · ship PROVENANCE + criteria with every entry · define MERGE semantics before two lists exist. ''A quorum signature answers forgery and never legitimacy.''
* ✓ ''THE ANTIGEN SHARE IS ALREADY OPERATOR-RULED — stop holding it as a governance call''. `carry-contract:83` (Operator-ruled 2026-07-20): MANDATORY tier carries //@oracle + @crossroads (public + infra) + the Kapae antigen AT ALL TIMES//, and ''contracting into a Nexus IS agreeing to be an immune-propagation node''. What stands unruled is only the board's ADDRESS, never whether it propagates. ⚠ Note the mandatory tier does NOT name the members board.
* ⚠ ''ONE CHARTER PER VESSEL IS NOT A LAW — it is a guard bolted onto a SINGLE-SLOT FILE''. `seal-import` says //a charter arrives where none stands, or it refuses//, and its own remedy concedes multiplicity: //a partner's charter belongs BESIDE yours, never over it — point the import at a seal home this vessel did not found in//. What forces one-Nexus is PATH SHAPE: one `founding-roster.mem` at one unparameterized `larSealHome()`, and one `carriage-consent.json` beside it. ⚠ ''A scout called that consent file a cross-Nexus overwrite hazard and that reads FALSE'' — `carriageConsentPath` is scoped BY SEAL HOME, and one seal home holds one charter, so two Nexuses mean two seal homes and two consent files. The single-slot problem is `larSealHome()` itself, never the consent beside it. ''Multi-Nexus is CANON and TESTED'': `waking-floor:165` (//a vessel standing in more than one Nexus holds more than one window//) with a passing test (`waking-floor.test.ts:67-69`), `fleetSpan` measures cross-Nexus dyads as //the exit right, measured//, and a text //admitted at two Nexuses declares twice//. ''PRIOR ART HAS NO DEFENDER OF SINGLE-MEMBERSHIP'': Willow (a peer holds as many stores as it wants; communal vs owned namespaces), Keyhive (group management AS a CRDT, coordination-free revocation, documents reachable through overlapping groups), MLS (a client is routinely in many groups), Matrix (one room per homeserver was never considered). Nostr's relay-bound groups demonstrate the failure — ''FORKING on group id'' — that follows from binding group identity to a server. ⚠ The known cost of single-membership: an operator needing two Nexuses runs two vessels, and then either shares keys (fusing the trust domains they meant to separate) or holds two unrelated identities nobody can address as one. ''Willow/Keyhive answer: the NAMESPACE is the unit of membership, the KEY is the unit of identity, kept orthogonal.'' ⚠ AND THE TWO OPEN QUESTIONS ARE ONE: the only suggested workaround for a second charter is a second `LAR_ROOT`, which is exactly the co-hosting shape with no local-process boundary.
* tombstone/whiteout verb — reserved (§3); wakes when layered delete needs it
* Automerge history growth — DXOS-epoch pattern watched (§1); wakes on real growth; the §7 bus-doc law = a second consumer
* `lares wiki` subcommands print human-only; `wiki open` selects-for-next-boot only
* engine-swap reconcile answered "current" against a stale engine — epoch-design shore
* e2e trends long (reboot vector 73s) — split a slow lane when the suite hurts
* NFC assertion at the shore — rides the INGEST gesture (spec already pins NFC)
* parser edge-field plane still implements the retired typed-edge law (edge-out-<family>-<role> emission; edge.ts filter; kumu-device.ts as sole live consumer) — recut BEFORE or WITH INGEST's parser work (witness once, not twice)
* module gate positive-path vector absent (bootTrustedModules via stack:has witnessed only indirectly) — rides the next lararium-tw5 touch
* smoke-plugin-boot.ts fails on surfaces lagging the plugin (kau templates, parser registration) — pre-existing; wakes when plugin smoke matters or at the next plugin recut
* TimeoutNegativeWarning (-15/-26) in e2e runs — pre-existing, un-localized (no computed setTimeout in src); wakes on next firing under node --trace-warnings
* island→admin verb plane still rides the postMessage switchboard — retires onto record pairs per §7's lane law; the durable mailbox (vessel-mailbox.ts, keel-resident, drains on ea) already carries admin→island. ORDERING CRUCIBLE-PASSED 2026-06-12 (moʻolelo in hoike below): LOAD stands as the FIRST witness of verb-pair physics; INGEST brings the SECOND witness + the shore forge (three-way diff, replace-by-group, quiescence, NFC); the wire compiler rides INGEST's organs; then residency (vector 2) → closure (3); the switchboard retires on two-witness verb-pair INTEGRITY, the platform-free handlers (verb-dispatcher/summons/vm, action-handler, wiki-compose/draft/mint) migrate in-VM during that recut (one witness not two), hull steps 2–4 follow; ingest-gate packing decided at the island-verb build

<<~ hoike #ingest-ordering held="a plan of this weight passes the work-crucible floor before it binds (operator) — CONFIRMED BY DEMONSTRATION at the floor, 2026-06-12" >>
q: does a flow-ruled sequencing chain bind, or must the plan pass the crucible discussion first?
<<~ kue voice="the house (Gatekeeper)" key="the convened crucible floor produces an ordering that differs from the flow-ruled chain — TURNED against the kue: the floor found the residency-vector omission and the overstated forge-claim in one reading" >>
the chain composes only standing rulings — the torch's NEXT VECTOR, §7's lane law, the
one-witness economy — so the crucible would re-derive what already stands; flow preserved
momentum the floor would spend on ceremony.
<<~/kue >>
<<~ moolelo held="the amended chain stands (operator): LOAD = first witness · INGEST = second witness + shore forge · residency restored at vector 2 · switchboard gates on two-witness integrity · verb-plane rides its recut" >>
the kue fell by the house's own floor — flow had sailed past a torch vector and rested a
true order on a false load-bearing claim; the crucible earned its seat by demonstration.
<<~/moolelo >>
re-entry: closed — the floor convened and the held review condition met; the chain re-opens only at a torch boundary
<<~/hoike >>

<<~/ahu >>

<<~ ahu #ways-of-working >>

!! How this operator works (hard-won, honor it)

''The inverse of these, as durable law.'' This file updates at each phase boundary and never archives in place, so the FAILURE modes these positives guard against live in canon instead: <<~ loulou lar:///ha.ka.ba/lares/api/pono/failure-states#/index >> — the craft axis names the invented ripple, the protective optional, the table that hides a plane, minting over a retirement, and not walking before minting. Each carries its own measured instance and its own diagnostic.

* ''OODA-HA plans out loud before flow''; YIN passes after landing; burrs taken immediately ("no friction for later snags").
* ''Commits: ASK FIRST, every commit'' — two parallel sessions share the tree (bags + packages). Subject line = the `lar:///three.term.root` URI; always explicit pathspecs (`git commit --only -- <paths>`); NEVER commit blind.
* ''Vocabulary law'': OCI nouns for structure, residency verbs for motion — VCS verbs MUST NOT name model operations. L-Prime in api/ and in your own prose. `ea` = breath, never "heartbeat"; nalu = the wave; web2 vocabulary reads as crud.
* ''Build-new-then-retire''; retired terms get reserved, not reused. Mechanism at choke-points, policy in cascades. Capability = manifest grant, never a cascade-settable flag.
* The harness exists so witnesses repeat: prove changes with `pnpm test:e2e` against a staged vessel, never by assertion. Vector first, fix second; a failing vector NAMES a hole.

<<~/ahu >>

<<~ ahu #edges >>

!! Edges

<<~ loulou lar:///ha.ka.ba/lares/docs/pattern-integrities >>
<<~ loulou lar:///ha.ka.ba/lararium/api/disk-projection >>
<<~ loulou lar:///ha.ka.ba/lararium/api/residency-model >>
<<~ loulou lar:///ha.ka.ba/lararium/api/island-isomorphism >>
<<~ loulou lar:///ha.ka.ba/lares/api/pono/nalu >>
<<~ loulou lar:///ha.ka.ba/lares/api/pono/federated-causal-islands >>
<<~ loulou lar:///ha.ka.ba/lares/api/pono/memetic-wikitext >>

<<~ loulou lar:///ha.ka.ba/lares/api/noosphere-boot >>

<<~/ahu >>

<<^ code="&#x0003;" >>ni:///sha-256;ymt2w3-isgQ0r7G_cbxl31j11yv5ejvf9jCsRq1JF6s

<<^ code="&#x0004;" -> to=? >>
