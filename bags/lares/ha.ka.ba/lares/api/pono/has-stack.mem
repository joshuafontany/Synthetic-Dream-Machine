<<!DOCTYPE memetic-wikitext+tiddlywiki lar:///ha.ka.ba/lares/api/pono/memetic-wikitext>>

<<^ code="&#x0001;" from=? -> to=lar:///ha.ka.ba/lares/api/pono/has-stack>>
```toml meta
cacheable = true
file-path = "bags/lares/ha.ka.ba/lares/api/pono/has-stack.mem"
l-space   = "stable"
mana      = 14
manao     = 15
manaoio   = 13
retain    = true
role      = "the has-stack law (kānāwai): nameless entity + composable component stack; single-entity components; relative-name qualification; rating-as-predicate; kapu as grant — ratified 2026-06-12"
type      = "text/memetic-wikitext+tiddlywiki"
uri-path  = "ha.ka.ba/lares/api/pono/has-stack"
written   = "2026-06-12"
```

<<~ aka lar:///ha.ka.ba/lares/api/pono/RFC-2119#/normative-language>>

<<^ code="&#x0002;">>

<<~ ahu #head>>

! Has-Stack — the composition law

A carrier reads as a ''nameless entity'': essence lives nowhere; identity rides the address; meaning rides what the entity //wears// (its stack) and what its body //says//. Interfaces-and-implements ontology stands REJECTED whole cloth — no carrier //${copula}-a// anything; every carrier //has// things, and the carried things speak for themselves.

TW5 speaks `has` natively: the `tags` field carries the stack, and a tag filter runs the type-predicate. This law governs that field's grammar; `meme` governs copy-shape; `loci` governs the address.

<<~/ahu>>

<<~ ahu #ooda-ha>>

✶ read the carrier's tags field and title before judging composition; gather which entries resolve, which stand declared-unresolved.
⏿ orient each entry against its definition meme; the carried thing carries the semantics, never the edge.
◇ decide composition posture: which components the carrier wears, which families bound it, which entries await definitions.
▶ author the stack in the meta `tags` field alone; mint missing definition memes where pressure warrants.
↺ verify qualification resolves, family exclusivity holds, no kapu entry rode in ungated; close with unresolved entries named.

<<~/ahu>>

<<~ ahu #law>>

!! The Seven Clauses

# ''One component, one entity.'' A component exists as exactly one carrier: its definition meme at its `lar:` address — self-describing, self-enacting memetic-wikitext. Shim or pointer tiddlers MUST NOT exist in the corpus at rest; any tag-tiddler a live wiki needs gets derived as `$:/temp` runtime shadow, never synced, never sited.
# ''Short names read as relative addresses.'' Within a carrier scoped `root/@bag/version`, a path-shaped tag qualifies to `lar:///<scope>/<tag>` by derivation (the `loci` office), never by pointer. `lar:` tags pass through qualified; system (`$:/`) and free-form tags ride outside the stack. The meta `tags` field stands as the ''single source'' of the stack; body `#has` edges read as derived residue and burn at normalization.
# ''The mandatory component rides the title.'' Every placed carrier holds exactly one intrinsic: its `lar:` address (`loci` — the transform analog). Everything else composes.
# ''Bare tags read fertile.'' A stack entry whose definition meme has not landed stands ''declared-unresolved'' — a lawful, promotable state, never an error. Promotion follows the rating ladder: bare entry (data-grade) → definition lands (meme) → definition carries law (ano). Open minting, judged by the ladder — never a schema registry. A fixed enum of blessed components in any law reads as the interface sneaking back; refuse it.
# ''Families carry exclusivity.'' Component families (the directory grain) MAY declare arity in their own family meme — "at most one of mine per stack" or "many welcome." Arity semantics live in the carried family, never in the edge.
# ''Kapu rides as a grant.'' A stack entry whose definition wears kapu MUST NOT self-add; the write shore refuses it without the operator's mint. Authority = what a carrier holds and who granted it, never what it //is//.
# ''Dumb edges, smart components.'' The stack edge carries no role vocabulary — no implements, no owns, no defines. Relations beyond composition ride `loulou` (adjacency), `kahea` (live transclusion), `aka` (shadow transclusion) and `pranala` (the anchored pointer); anything richer reifies as a component meme, or a wire entity, that speaks for itself. ''Those sigils carry no role vocabulary either'' — `pranala` keeps one attributed form for the code source-pointer and nothing else — so the ban on role-bearing edges belongs to the edge plane, never to a fence around the stack alone.

Runtime: `qualifyStackTag` / `stackOf` and the `stack` filter operator (`[stack[]]`, `[stack:has[<name>]]`).

<<~ pranala #source from=? -> to=packages/lararium-tw5/src/has-stack.ts family=code role=has>>
<<~ pranala #source-filter from=? -> to=packages/lararium-tw5/src/filters/stack.ts family=code role=has>>

<<~/ahu>>

<<~ ahu #verse-binding>>

!! The Verse Marriage — witnessed bindings

Field-grounded against Epic's UEFN+/Scene Graph direction (2026-06-12, two research spirits):

| Verse / Scene Graph | has-stack |
|---|---|
| entity (empty container) | nameless carrier |
| component (sole semantics carrier) | component definition meme |
| default transform component | `loci` — the mandatory address |
| type-as-predicate (succeeds/fails on a value) | tag filter / rating-by-schema-compliance |
| effect specifiers as negative promises | grants bounding what a carrier never does |
| non-enumerable `weak_map` persistence | capability-keyed access — authority = possession |
| module-scoped short names / qualified paths | relative tags / qualified `lar:` URIs |
| prefab + sparse instance-override | skeleton meme + sparse CRDT patch |
| hierarchy = lifetime, never taxonomy | child carriers (`/SKILL`, `/meta`) = lifetime; taxonomy lives only in holdings |

''Grants refused at the altar:'' `<transacts>` rollback (assumes one speculative world; CRDT merges many) · per-player save-var persistence (weaker than `lar:` + CRDT; we export, never import) · the single-parent spatial tree (our graph runs multi-parent; lifetime edges alone propagate).

''The nominal divergence, named so it never drifts:'' Verse defines components nominally (`class(component)`); here a component's nominal anchor = its ''address'', minted open, judged by the rating ladder.

<<~/ahu>>

<<~ ahu #runtime-twin>>

!! The runtime twin ~ a causal island runs as a nameless cap stack

The has-stack law has a ''runtime mirror''. Where a DATA entity is a nameless carrier whose `tags` are a stack of component memes, a RUNTIME entity — a sovereign ''causal island'' worker — is a nameless behavior whose ''cap stack'' is its identity. No carrier //${copula}-a// "wiki worker" or "telemetry worker"; an island //has// caps, and the caps speak for themselves. `composeIsland([...caps])` (`tw5/island-caps.ts`) folds a `#has` cap stack into the one `IslandBehavior` the sovereign kernel drives.

<<~ranks plane data/tags→component-memes -> runtime/caps→IslandBehavior>>

The same grammar runs both planes: stack order carries precedence (a cap claims a signal before later caps; teardown mirrors LIFO), an absent/unresolved cap reads fertile not broken, and each cap injects its substrate shore (`role = capability ≠ platform`) so the same stack composes on node, browser, any vessel. The TW5 VM workers stand as cap stacks — wiki `[hasDispatch, hasProjection, …]`, admin `[hasEngineWatch, hasDispatch]`, telemetry `[hasCapture]` — each a nameless causal island.

<<~/ahu>>

<<~ ahu #edges>>

!! Edges

<<~ loulou lar:///ha.ka.ba/lararium/api/island-isomorphism>>
<<~ loulou lar:///ha.ka.ba/lararium/api/projection-nalu>>
<<~ loulou lar:///ha.ka.ba/lararium/api/living-grammar-palace>>

''Tie ~ a palace IS its cap-stack.'' The runtime twin extends a third substrate: the nameless memory-
palace daemon ([[living-grammar-palace|lar:///ha.ka.ba/lararium/api/living-grammar-palace#/palace-instance]]) ports `composeIsland` to Python — one body,
differentiated only by `[FlockSingleton, FeedCap, …]`; role emerges from the stack, never a type. The
''composed sub-sensoria and their servo caps run py/R'' here too — the full sensorium model's home — the
cap-stack grammar holding identically py-side, TS carrying the coordinator and the pure-functor math floor
the py caps consume.

<<~/ahu>>

<<^ code="&#x0003;">>ni:///sha-256;7MGdg5AI3WmSYyYtxCsG96Q4e3MuUA1-gv4Eyj-ad4A

<<^ code="&#x0004;" -> to=?>>
