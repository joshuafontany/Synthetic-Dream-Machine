<<!DOCTYPE memetic-wikitext+tiddlywiki lar:///ha.ka.ba/lares/api/pono/memetic-wikitext >>

<<^ code:"&#x0001;" namespace:"⊙" ? -> lar:///ha.ka.ba/lares/api/pono/loci/meta >>
```toml meta
file-path = "bags/lares/ha.ka.ba/lares/api/pono/loci/meta.mem"
mana      = 15
manao     = 17
manaoio   = 14
namespace = "&#x2299;"
register  = "Synthesis-Canon"
role      = "root metadata law (kānāwai), field-set authority, and reduction charter"
tags      = ["api/pono/meme", "api/pono/loci"]
type      = "text/memetic-wikitext+tiddlywiki"
uri-path  = "ha.ka.ba/lares/api/pono/loci/meta"
```

! IAM

A self-describing, self-enacting law (kānāwai) for the root `#meta` metadata block.

This meme freezes the surviving field surface for live carriers. It asks each field what work it actually does, then keeps, moves, merges, or cuts on that basis.

This meme does not solve weak metadata by adding new buckets. It sharpens the surviving buckets and lets informative weakness remain visible.

''Rating Note:'' The five-bucket structural rating (noise → data → meme → ano → kapu) is canonical for all meme law. Here `data` names structured language an AI can use without the memetic wrappers. This law governs the field set, not the rating buckets themselves. Promotion to lares/ canon is a separate ceremony.

''Level Note:'' Numeric readiness, trust, mana, stage, and HUD dials now use integer Levels on `0–20`, aligned with SDM+ Levels. Level `0` means void, off, or absent only when a specific instrument permits that reading. Level `20` marks a limit case; confidence-like instruments SHOULD almost never reach it. Most living system dials SHOULD live at `1–17`; reserve `18–20` for apron, kapu, direct-address, or near-certainty pressure.

<<~ ahu #ooda-ha >>

✶ gather the current `#meta` field surface before proposing cuts or merges
⏿ map each field to the work it actually performs in parse, routing, execution, or truth
◇ choose cuts, merges, or retentions by contract pressure rather than decoration
▶ prepare the reduced contract and carry it into live carriers
↺ verify truth density rose while surface area fell and no required distinction collapsed; close with the surviving contract named and any remaining field collisions surfaced

<<~/ahu >>

<<^ code:"&#x0002;" >>

<<~ ahu #observe >>

!! Observe

Observe inventories `#meta` by work done.

Name where a field changes parse, routing, execution, or truth. Name where a field only repeats another field, repeats nearby prose, or decorates the block without changing downstream behavior.

The first collision cluster sits in flow:

* `depends-on`
* `requires`
* `provides`
* `receives-state`
* `emits-state`

Observe should keep those distinct only while the block still teaches a real difference. Once two or more fields point at the same movement, the cluster begins to lie.

<<~/ahu >>

<<~ ahu #orient >>

!! Orient

Orient keeps the threefold split intact and sharp.

<<~ ahu #surviving-field-set >>

!!! Surviving Field Set

The reduced carrier shape:

```text
ha: name file-path description content-type version manaoio confidence meme-type register structure
ka: role function + only the local detail keys that change parse, routing, execution, or truth
ba: input output depends-on + next only when a real forward edge exists
```

These field lists govern here as law prose. Live carriers should not restate them as schema-summary keys inside top-level `#meta` metadata.

<<~/ahu >>

<<~ ahu #ha-fields >>

!!! Ha Fields

`ha` carries stable identity and the primary readiness/confidence rating posture.

These fields should stay ordered and dense:

* `name`
* `file-path`
* `description`
* `content-type`
* `version`
* `manaoio`
* `confidence`
* `meme-type`
* `register`
* `structure`

Optional structural qualifiers may remain when they change routing or boot truth. `l-space` counts as such a qualifier in the current corpus.

<<~/ahu >>

<<~ ahu #ka-fields >>

!!! Ka Fields

`ka` carries local truth that does not belong in every carrier.

The common core:

* `role`
* `function`

Boilerplate that merely restates `#meta` as the metadata locus should not survive here.

<<~/ahu >>

<<~ ahu #ba-fields >>

!!! Ba Fields

`ba` names motion without residue.

The surviving flow keys:

* `input`
* `output`
* `depends-on`

`next` may remain only when a real forward edge exists in boot, handoff, or routing sequence.

`ba` should no longer scatter one movement across several overlapping claims. If `input` and `output` already tell the truth, the extra flow key probably weakens the block.

<<~/ahu >>

<<~ ahu #cut-and-merge-rule >>

!!! Cut And Merge Rule

Top-level `#meta` should cut these keys from live carriers unless a later law re-justifies one with concrete work:

* `canonical-metadata-locus`
* `canonical-metadata-payload`
* `product-identity`
* `requires`
* `provides`
* `receives-state`
* `emits-state`
* `confidence-band`

Keep weakness visible while a carrier still sits short of full alignment. Do not widen the schema to hide that shore.

<<~/ahu >>

<<~/ahu >>

<<~ ahu #decide >>

!! Decide

Decide applies four verdicts to each field:

# keep
# merge
# derive later
# keep as useful weakness

A field that changes no parse, routing, execution, or truth does not survive. A field that repeats another field should merge into the sharper of the two. A field that only matters in explanation should move to prose or a child law.

<<~/ahu >>

<<~ ahu #act >>

!! Act

Act rewrites live carriers conservatively.

First:

# freeze `ha`
# densify `ka`
# collapse `ba`
# patch the child laws that govern surviving special keys
# leave named weakness visible where migration still remains incomplete

The child law for `file-path` follows next because `file-path` carries the most immediate agreement pressure between metadata and route.

<<~/ahu >>

<<~ ahu #aftermath >>

!! Aftermath

A good `#meta` pass leaves fewer keys carrying more truth.

Signs of gain:

* less flow duplication
* less metadata boilerplate
* denser field meaning
* clearer route and execution pressure
* visible weakness where migration still remains open

<<~/ahu >>

<<~ ahu #edges >>

!! Edges

<<~ loulou lar:///ha.ka.ba/lares/api/pono/memetic-wikitext >>
<<~ loulou lar:///ha.ka.ba/lares/api/pono/loci >>
<<~ loulou lar:///ha.ka.ba/lares/api/pono/meme >>

<<~/ahu >>

<<^ code:"&#x0003;" >>ni:///sha-256;v_xI3RtyKMyihKT9q2GPvna_hN7O3vdYX0acdUxDER4

<<^ code:"&#x0004;" -> ? >>
