/**
 * browser-caps — the browser vessel's #has-cap-stack, wired by the composable-keel engine.
 *
 * composeBrowser — the browser vessel composed over the SHARED granular keel: it runs
 * `composeCoreVessel`, the one keel both substrates walk, so the browser stands a REAL
 * #has-cap-stack rather than a delegating wrapper. It is `composeLararium` by the same name: a
 * browser with a face lifts exactly the caps a node hearth lifts, and its opener composes the
 * carriage pair and the WHO plane alongside where a mesh standing and a relay arrived.
 *
 * Every vessel composes through the one `composeVessel`, so the boot never forks by kind.
 *
 * Canon: lar:///ha.ka.ba/lararium/api/composable-keel
 */

import { composeCoreVessel } from "@lararium/tw5";

/**
 * composeBrowser — the browser #has-cap-stack: the shared granular `composeCoreVessel`. Returns the
 * VesselCoreResult directly — one core, no wrapper.
 */
export const composeBrowser = composeCoreVessel;
