{
  "name": "@lararium/app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser-lararium app shell — boots a sovereign browser vessel and (when reachable) reads the public @oracle read-face. Location-agnostic: served from localhost / LAN / elyncia.app, the vessel always runs local; the origin is a static host, never an authority.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@automerge/automerge": "3.4.1",
    "@automerge/automerge-repo": "2.6.0-alpha.3",
    "@automerge/automerge-repo-storage-indexeddb": "2.6.0-alpha.3",
    "@lararium/browser": "workspace:*",
    "@lararium/mesh": "workspace:*",
    "idiomorph": "0.7.4"
  },
  "devDependencies": {
    "@keyhive/keyhive": "0.1.0-alpha.8",
    "@lararium/keyhive": "workspace:*",
    "vite": "8.2.2",
    "vite-plugin-arraybuffer": "0.1.4",
    "vite-plugin-wasm": "3.6.0"
  }
}
