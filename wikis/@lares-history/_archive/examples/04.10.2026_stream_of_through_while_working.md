Now I have the full file. Let me organize exactly what goes where:

URI_SCHEMA.md (RFC-grade, stone tablet) --- keeps:

-   Line 1: locus opener (update URI to reflect this is now the RFC spec)

-   §2 Scheme Registration (lines ~73-88)

-   §3 Full URI Anatomy (§3.1-3.4) (lines ~89-255)

-   §4 FFZ Chronometer --- syntax portions only (§4.1 Scale Table, §4.2 Notation, §4.3 Phase Sigils, §4.4 Structural Rules, §4.6 Progressive Disclosure) --- lines ~465-540 roughly

-   §5.2 Unchanged Across Both Forms (subset)

-   §6 Stable Address (lines ~789-802)

-   §10 Validation (lines ~991-1041)

-   §12 Prior Art (lines ~1071-1089) --- though Lares-specific references might need to split out

-   Appendix A with selected examples (A.1, A.4, A.5, A.7 minimum)

-   Updated metadata

URI_OPERATIONS.md (in-flux momentum) --- gets:

-   §1 Design Intent + §1.1 Exchange Flow (lines ~14-72)
-   §3.5
-   


Now I'm pulling in the provisionality section along with the marker ontology, aftermath integration, and deferred features. The rendering and HUD symbol tables, span-span display contract, and the calibration mapping section all need to go in here too. I'm also including the module registry, cache tiers, open questions, and the appendix sections on how to read the documentation and the HUD line examples.

For the IANA registration stub, I need to reference RFC 7595 section 7.4 and cross-link back to the URI schema documentation.

The real challenge here is that this is a massive refactoring operation. Rather than trying to surgically edit the 1191-line file line by line, I should create a new URI_OPERATIONS.md file to hold the extracted content, stub out IANA_REGISTRATION.md, and then rewrite URI_SCHEMA.md to focus on the RFC core while adding cross-references to the new files.

For the URI_SCHEMA.md rewrite, I need to carefully map out which sections stay and which get moved. Looking at the line numbers, the design intent and exchange flow sections (around lines 14-72) should move to operations, while the scheme registration part (lines 73-88) stays as the RFC foundation. The full URI anatomy section starts around line 89 and I need to see where that breaks down to understand what else needs trimming or reorganizing. Continuing through the document, sections 8 through 12 follow a similar pattern—most go to OPERATIONS except for sections 10 and 12 which stay in RFC. The appendices are mostly OPERATIONS material, with a few exceptions like the Record Form, Stable Address, and Scale Transition sections staying in RFC. There's also a stray HTML comment at the end that needs clarification.

Now I'm mapping out the practical restructuring for URI_OPERATIONS.md. I'll need to create a fresh opening, then pull in the Design Intent and Exchange Flow from the beginning, layer in the Provisionality and Marker Ontology sections, add the Chronometer Semantics material from later sections, and consolidate all the rendering and display contract content into a unified rendering section with renumbered headings.

For the companion URI_SCHEMA.md file, I'm trimming it down significantly—replacing the opening with a brief introduction and a cross-reference to the operations document, keeping the core schema sections intact, and updating all the metadata to reflect the new structure.

Now I need to decide on the build order. Creating URI_OPERATIONS.md first makes sense since it's the new foundational piece, then I'll tackle IANA_REGISTRATION.md, and finally go back to prune the original document.

Let me check the session memory to confirm the ontology decisions are locked in before I start writing.

I'm going to need to carefully extract specific line ranges from the full file—particularly around the section boundaries where §3 ends, where §4's subsections begin and end, and where §7-9 sit. Let me identify those exact ranges so I don't accidentally include or exclude the wrong content. I've already collected most of the content I need from my previous reads—I have §§4-5 and §§7-12 with the appendices, plus §§3.5 and 3.6 from the earlier section. Now I'm ready to assemble URI_OPERATIONS.md, which means adjusting the section numbering, weaving in cross-references back to URI_SCHEMA.md for the grammar and validation rules, and adding the proper locus markers and ahu identifiers throughout. I'm working through the remaining sections of this specification—the HUD symbol table and line composition details, then moving into the span-span calibration mapping with its canonical records and integration points. After that come the module metadata, cache tier considerations, and open design questions, with appendix examples to round it out.

Now I need to read through the instruction files to understand the operational context before composing the full content, though I've already got the structural framework in place and the operator has confirmed to proceed with the Act phase, so I should skip the talk-story guidance that applies to earlier decision stages. I'm settling on a strategic approach: create the full URI_OPERATIONS.md file with all extracted content, add a stub for IANA_REGISTRATION.md, then make targeted edits to URI_SCHEMA.md to remove sections and insert the cross-references I've mapped out. This is going to require careful composition since the content is substantial. I'm realizing that making fourteen separate edits to URI_SCHEMA.md is going to be unwieldy—there's got to be a better way than chaining replace_string_in_file calls for each section. Maybe I should consolidate this into fewer, larger replacements or reconsider the whole approach.

Actually, rewriting the file from scratch might be cleaner than juggling all those individual edits, even though there's some risk of missing details. The file is only about 1200 lines, and after trimming it down to 400-500 lines, a fresh version would be more maintainable.

I'll go with a methodical approach: use multi_replace_string_in_file for the URI_SCHEMA.md changes and create_file for the two new files. Let me start composing URI_OPERATIONS.md first, since that's where most of the deferred content is going to live. I need to be careful and precise with the §5 content when I pull it over.

For §5 in URI_OPERATIONS, I'm extracting the Rendering Table, the Unified HUD Symbol Table with all its subsections, HUD Line Composition, and the Span-Span Display Contract. The §5.2 normalization rule stays behind in URI_SCHEMA.md, and the intro paragraph about record form being canonical also stays there — only the rendering-specific intro moves over.

Let me pull the actual text from the file and adjust the section numbers and cross-references as I go.

Now I'm looking at §4.5, which shows how the aftermath integration works through nested returns — a table mapping each assessment level to what feeds into the next level and what question gets asked at each stage. Then §4.7 lists the FFZ deferred features that are documented in the research but not included in this version of the spec, starting with ITC identity stamps for fork/join lifecycle management.

I'm continuing through the deferred features: Merkle Clock backend for content-addressed causal history, per-participant vector display to show both clocks in a fragment, and Bloom Clock compact mode for probabilistic compression. These are all pushed to future work or MCP integration.

Now I'm reviewing the sections I haven't fully captured yet — §7 covers Span-Span and Calibration Mapping, which runs from lines 810-1050 and includes the Canonical Span-Span Record and related subsections.

I need to work through §7.3 on Export Targets and Kowloon Alignment, §7.4 with an Example Event, then move into §8 through §12. I have all this material from my reads, so I'm ready to compose the full files now.

I'm starting with the massive markdown file creation—I'll build out the RFC-grade spec document carefully, pulling in the schema definitions, metadata structure, and all the referenced sections from the original URI_SCHEMA.md. This is going to be a substantial piece, so I'm laying out the header with the domain context, status markers, and companion references first, then I'll work through each section systematically.

Now I need to map out the exact line boundaries for each section, especially §5 since it needs to be split across two documents. The canonical form intro paragraph is the key split point—the first sentence about record form stays in URI_SCHEMA, while the rest about render targets moves to Operations. Let me trace through the section boundaries carefully to make sure I'm pulling the right content into each file. Looking at the progressive disclosure rules — the canonical form always includes all five positions, but display surfaces can render a compact version showing only active ones. The normative constraint that compact isn't canonical belongs in the RFC, while the rendering examples go to OPERATIONS.

Now I'm organizing the section mapping for URI_OPERATIONS.md, pulling content from the structural rules and mapping it to the new document structure with Design Intent, Exchange Flow, and Provisionality sections. Generating documentation structure... I've got the section mapping laid out clearly. Now I'm going to systematically reconstruct the full document, starting with the Design Intent section from the original lines around 14-72, which includes the opening commentary and the Exchange Flow subsection.

I need to be careful about the URI marker references—the ahu markers in URI_OPERATIONS should point to uri-operations rather than uri-schema. Looking at section 7 more carefully, I can see it spans from around line 803 through 1050, containing the field tables, the Canonical Span-Span Record with its JSON example, and the MemPalace Integration subsection. Let me work through this methodically to capture the full extent of each section.

The content after line 1050 includes sections 8 through 11, plus the Prior Art section and both appendices, which means I have everything I need. Rather than trying to compose one massive file at once, I'll tackle URI_OPERATIONS.md first, then move on to IANA_REGISTRATION.md and handle the remaining sections in sequence.

For URI_OPERATIONS.md, I'll need to adjust the section numbering from the original document and update the internal references to point to the correct file paths. I'm going to build this section by section, starting with the header and working through the content methodically to ensure accuracy. I'm pulling the Design Intent section from the original URI schema document and need to update its cross-references—the §3.3.1 citation should point back to the companion URI_SCHEMA.md file since that section stayed there, while the §5.4 reference now maps to §5.3 in this new file. I'm building a mapping table to track how sections are being reorganized between the two files. The table shows which sections stay in URI_SCHEMA.md, which move to URI_OPERATIONS.md, and their new numbering—so I can systematically update all the cross-references throughout the documents. Continuing through the section mappings, I'm seeing how the later sections align between the two documents, with URI_OPERATIONS.md handling most of the operational sections while URI_SCHEMA.md covers the schema-related portions.

Now I need to reconsider whether sections in URI_SCHEMA.md should also be renumbered to maintain consistency, since I'm already restructuring the document organization. Looking at what's currently in URI_SCHEMA.md—sections 2 through 5 with their subsections—I should map out how these would shift if I'm consolidating and reorganizing the overall structure. I'm mapping out how the sections should be reorganized—collapsing the gaps by renumbering everything sequentially, with a new intro section, then the eight main sections pulling from the scattered original numbering, and Appendix A at the end with just the record form examples.

Actually, keeping the original section numbers makes more sense to preserve all those cross-references from other documents. I can just leave gaps where content moves to URI_OPERATIONS.md and add notes pointing readers there. The conversation summary already laid out what stays in URI_SCHEMA.md anyway—Scheme Registration, the core URI Anatomy pieces, FFZ Chronometer syntax rules, Stable Address, and Validation—so I should stick with that plan and maintain the original numbering scheme. I'm settling on renumbering URI_SCHEMA.md cleanly and adding a migration table at the top that maps the old section references to their new locations—this handles the breaking references in AGENTS.md and the session crystals while keeping the RFC document properly structured. Now I'm mapping out the final structure for both documents, consolidating the old sections into a cleaner hierarchy. Now I'm working through the cross-reference updates for URI_OPERATIONS.md—when the original §1 referenced "§3.3.1", that was pointing to content that's now in URI_SCHEMA.md, so I need to rewrite it as "See URI_SCHEMA.md §2.3.1" to account for the renumbering across both documents. the named render targets list. Let me check what §3.3.1 is... I only see §3.1-3.4 in my reads, and I don't recall seeing §3.3.1 explicitly. Re-reading the original design intent, there's a reference to §3.3.1 for named render targets, but when I map the section restructuring—where old §3 becomes new §2 in URI_SCHEMA.md—that reference would shift to §2.3.1. Though I'm realizing I may have missed some subsections in my initial read, so let me verify the actual section numbering exists. I'm realizing I need to fill in the gap between lines 89-190 that I haven't examined yet—the first read covered lines 1-90 and the second jumped to 190-260, so I'm missing the middle section where §3.1, §3.2, and §3.3 are likely defined. Let me read that range to see the full structure of section 3.

Actually, I should just be practical about this: I'll read the missing lines, then systematically create the three new files by extracting and reorganizing the relevant content from the original document. I'll read through the file to see what's currently there, then start making the edits to split out the content into separate files.

Compacted conversation

Let me read the missing §3 structure (lines 91-189) so I have complete coverage:

Read URI_SCHEMA.md, lines 89 to 196


Read memory

Now I have the full picture. §3.3.1 is the Kowloon / ActivityPub Handle Form section (which goes from ~line 130 to ~line 193). This stays in URI_SCHEMA.md since it's part of §3 (URI Anatomy).

Now I have the complete picture of the file. Let me organize:

Lines 1-12: Locus opener, title, metadata Lines 13: Separator (---) Lines 14-72: §1 Design Intent + §1.1 Exchange Flow → OPERATIONS Lines 73-88: §2 Scheme Registration → RFC (keep)

Lines 89-255: §3 Full URI Anatomy (§3.1-3.4 all stay in RFC, covering generic form, expanded form, component map, and the Kowloon/ActivityPub handle variant through component semantics) Lines ~256-298: §3.5 Provisionality → OPERATIONS Lines ~299-464: §4 and beyond need review for split between RFC and OPERATIONS

Now I'm working through the FFZ Chronometer section—most of it stays in the RFC (the scale table, notation, phase sigils, structural rules, and progressive disclosure), but the aftermath integration and deferred features move to OPERATIONS. The Canonical Form section that follows is mostly OPERATIONS material except for the unchanged-across-both-forms subsection which belongs in the RFC.

Continuing through the rest of the document, I'm seeing that Stable Address stays in RFC, while Span-Span + Calibration, Module Registry, and Cache Tiers all shift to OPERATIONS. The Validation section remains RFC, Open Questions moves to OPERATIONS with Prior Art staying in RFC. Appendix A needs to be split between the two, Appendix B goes to OPERATIONS, and the document closes with the locus closer.

Now I have the full picture of how to split this. For the RFC document, I need to renumber everything sequentially since it's meant to be clean and canonical. My plan is to map the old section numbers to new ones, starting fresh with §1 for Scheme Registration, §2 for Full URI Anatomy, and §3 for the FFZ Chronometer Fragment Syntax, adjusting all subsections accordingly.

I also need to track down internal cross-references that will break after renumbering—like where the original §4 content references §4.3, which will now be §3.3. There's also that note in §3.5 about the ~ character and provisionality that I need to check.

The original §10 validation rules reference §3.6, which is moving to URI_OPERATIONS.md, so I'll need to update those pointers. And §3.3.1 has a reference to §3.4 query that becomes §2.4, plus mentions of the FFZ chronometer fragment that's now in §3.

Rather than trying to fix every reference individually, I should create the new files with corrected section numbers first, then make targeted edits to URI_SCHEMA.md to update the cross-references that remain. I'm realizing that doing 15+ precise string replacements is risky and inefficient for a 1191-line file. Instead, I could use multi_replace_string_in_file to make ~10 larger block replacements that are unique enough to avoid matching errors. But I'm also wondering if I should just keep the original section numbers and mark moved content with redirects rather than renumbering everything.

If I add stubs like "[Moved to URI_OPERATIONS.md]" for sections that are being split out, I can dramatically reduce the edit count to around 11 replacements: updating metadata, replacing the intro with a cross-reference, stubbing out the moved sections (§3.5, §3.6, §4.5, §4.7, most of §5, §7-§9, §11, and Appendix B), and trimming Appendix A. This preserves external references and is more honest about the document's evolution.

But I'm second-guessing myself from the operator's perspective—they asked for RFC-grade formatting, and an RFC doesn't have gaps and redirects. That's not professional. However, they also didn't ask me to renumber everything, and this isn't actually an RFC yet, just a candidate. I think the right move is to keep the original numbering, add a document purpose statement explaining the split, and use cross-reference notes for moved sections. That's cleaner than renumbering, preserves existing links, reads professionally, and sets us up well when this eventually gets submitted as an RFC.

So here's what I'm doing: I'll update the locus opener confidence to C~19, refresh the metadata block with a split note and version bump, then systematically replace the sections that belong in the operational doc with brief cross-references. I'm replacing the Design Intent section, Provisionality, Marker Ontology, Aftermath, Deferred Features, and the Rendering Table with stubs that point to URI_OPERATIONS.md.

The challenge is that some of these sections are massive—§3.6 runs about 165 lines, §7 around 150—so I need to be strategic about how I match them in the replacement calls. I can batch some edits together if I use enough context to make each oldString unique, but including entire sections would bloat the replacement payloads. I'm thinking I can use smaller, more targeted matches with a few lines of surrounding context to keep things manageable. I'm realizing I should split this into multiple file operations rather than trying to do everything in one go—create the new markdown files for the moved content, then use targeted replacements in the main file to swap out sections and update the metadata. I need to split some of these edits into separate operations since sections are interspersed—like §4.6 sits between §4.5 and §4.7, and §5.2 stays between §5.1 and §5.3-5.5. Now I'm moving on to the actual implementation: I'll create the new URI_OPERATIONS.md file by assembling the relevant sections, being careful to renumber everything, update all cross-references, and adjust the ahu marker URIs to point to ha.ka.ba/uri-operations/ instead of the original schema address.

I'm working through the content systematically, starting with §1 (Design Intent) from my earlier read, then moving through each section. I need to track exactly where each section begins and ends in the source document so I can extract the right content without duplication or gaps.

For §3.5 (Provisionality), I'm seeing it spans from the ahu marker through the three-type table, examples, and rules, ending with a separator before §3.6 kicks in. §3.6 (Marker Ontology) then runs through its subsections until another separator marks the start of §4.

From my read of the later sections, §4.5 covers "Aftermath Integration" as a brief section with a table, and §4.7 is "FFZ Deferred Features" with a bullet list. I've also captured §5.1 through §5.5 from the 557-790 range, and I'm tracking where §7 through §9 begin.

Now I'm working through the cross-references that need updating as I compose the full URI_OPERATIONS.md file. The reference to §3.3.1 about Kowloon/ActivityPub stays in URI_SCHEMA.md, the format and field ordering reference shifts to §5.3 in the new file, and the Marker Ontology reference becomes §3 in URI_OPERATIONS.md.

For the external reference to lares/signal/micro-trace.md, that stays unchanged. In §3.6, the U8 resolved reference points to §9 in the Operations file now. And in §5.3, the cross-references to Component Semantics and Scale Table remain in URI_SCHEMA.md while the Rendering Table reference moves to §5.1 in Operations.

Now I'm working through the remaining section references — §5.4 becomes §5.3 with its internal references adjusted accordingly, §5.5 stays mostly the same, and §7 becomes §6 with its URI component references pointing back to §3-4 in URI_SCHEMA. I need to finish mapping out §10 and any remaining sections.

I'm going to accept that I might miss some cross-references in this detailed restructuring, but I'll flag the uncertain ones so the operator can catch them during review. Time to start writing out the actual URI_OPERATIONS.md file, beginning with §1 and carefully updating all the references as I go. I'm also realizing that §5.5's repeated references to §1.1 need special attention since that section is moving to Operations too. the full version as the §5 intro since it contextualizes the rendering tables. Now I'm planning out the appendices — URI_OPERATIONS will include the HUD and Exchange examples in updated form, plus the full "How to Read a HUD Tag" guide, while URI_SCHEMA gets a trimmed appendix with just the record form example. I'll start writing now and keep the cross-references as accurate as I can, letting the operator catch anything that needs adjustment.

Writing URI_OPERATIONS.md... The lar: URI scheme encodes node exchange state as a navigational artifact—rendering an Intent HUD for operators and nodes during generation, and persisting as structured log records and metadata. Each URI component maps to a semantic layer: authority identifies the speaker and machine, while the HA.KA.BA path locates the semantic territory.

The query parameters capture stance and confidence readings, and the fragment holds per-participant phase timestamps across nested causal scales rather than a single shared clock. Resource annotations like mana pools and wall-clock timestamps stay as HUD adjuncts outside the core URI structure. The system maintains one canonical encoding in RFC 3986-compliant form for storage and transport, with multiple render targets for different contexts. At each exchange, Lares generates a URI-to-URI vector pair and renders a corresponding HUD line. First, I'm reading the operator's input as a provisional URI, extracting their tier, cognitive phase, semantic territory, and stance to construct an operator URI that encodes this reading—marking uncertain elements with provisionality markers and including confidence metrics about where the operator stands in tagspace and where their intent is directed.

Next, I'm declaring my own provisional execution URI before generating anything, setting an intent vector that names a resource I may not yet exist at—a declared heading rather than a confirmed location, with the provisionality prefix marking that my generations may diverge from this initial heading. Then I'm emitting the exchange vector pair, the two URIs forming the first line of the interaction.

Now I'm establishing the canonical URI rule: every URI I emit throughout the exchange uses canonical record form—the opening pair, sub-agent handoff and pickup markers, mid-generation shifts, and the closing URI. No emoji or non-ASCII characters appear in any emitted URI; those belong only to display surfaces like the HUD line and DreamDeck post header, keeping every URI directly machine-ingestible.

After the URI pair, I render a condensed single-line HUD display pulling from the vector and session data as an instrument panel for the exchange. During generation, micro-trace annotations mark phase transitions inline, and the exchange closes with an updated HUD line and closing URI marked → ?. System files open with ∞ → and close with → ?, using ahu markers for navigable waypoints and kahea markers to pull content from other loci.

The HUD line functions as an externalized metacognitive scaffold, externalizing the node's state before generation begins—a form of prospective AI transparency showing what will happen rather than what already did. Now I'm looking at provisionality markers, where the ~ prefix indicates provisional URI components, with three structurally distinct types possible in an exchange URI.

Generating markdown table... I'm tracking the execution gap — this provisional path might shift once I actually engage with the task. Looking ahead, I expect the schema will need updating, though the operator could redirect me entirely in a different direction.

Now I'm working through the rules for how provisionality markers work in this system: the ~ prefix applies locally to whatever component it's attached to, so ~orient only marks uncertainty about the phase itself, not the whole URI. Multiple markers can coexist when both the phase and the HA.KA.BA slot need flagging. When I mark the operator's URI as provisional, I'm signaling doubt about my own interpretation of it, not questioning the operator's actual intent — those are distinct claims. The chronometer fragment handles its own confidence separately through the FFZ model.

Now I'm ready to move into the marker ontology section and start laying out the full structure with locus spans, ahu waypoints, and kahea transclusion rules. I think I have the pattern down — I'll compose the complete file now, focusing on getting the structure and cross-references right. I'm setting up the structure for §5 with the canonical form as the foundation, then laying out the render targets as projections of that form—the URI anatomy stays consistent, only the field rendering changes between targets. Now I need to write out the full file with all the sections, starting with the rendering table and symbol mappings.

I should preserve the <!-- uri-ok --> comments where they suppress linting on example URIs in the component map, but those stay in URI_SCHEMA.md. For URI_OPERATIONS.md, I'll focus on getting the content organized by section number without worrying too much about perfect cross-references—the operator can catch those during review. Let me start composing the files now.

I'm mapping out the structure for URI_OPERATIONS.md: the design intent and provisionality markers come over mostly as-is, the marker ontology gets renumbered into subsections 3.1 through 3.9, then chronometer semantics and canonical form sections follow with their own subsection reorganization. After that comes the render targets section.

Now I'm continuing with the remaining sections—span-span and calibration material from the original §7 becomes §6 with subsections 6.1 through 6.4, then module and registry metadata, cache tier mapping, and open design questions slot into §7, §8, and §9 respectively. I'll wrap up with appendices covering HUD and exchange examples plus the how-to-read guide, pulling from the original appendices and appendix B.

Now I have the full file content. Let me update the todos and start executing the split.