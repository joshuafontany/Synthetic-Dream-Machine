# Module-Map: Platform Wrappers

> Module-Map type: Platform Integration
> Source: `builds/agents/platform/Lares_Copilot_Wrapper.md` (34 lines), `Lares_Claude_Wrapper.md` (43 lines), `Lares_Codex_Wrapper.md` (48 lines), `platform/README.md` (362 lines)
> Updated: 2026-04-06
> Register: [S~13] 🏛️ — current state measured directly from source

---

## Overview

Three platform wrapper files append platform-specific coordinator suffixes to the combined output generated by `combine_agents.py`. Each wrapper is a **generated-file notice + worker registry table + platform-specific notes**. They are thin by design: the heavy payload currently remains `Lares_Preferences.md` + `Lares_VSCode_Operations.md`. Wrappers exist to isolate host-specific instructions without forking the core agent system.

The `platform/README.md` (362 lines) is the human-readable operations manual covering format specs, best practices, anti-patterns, and the Sprint 3 Codex implementation plan. It is **not** injected into any platform output — it is reference material only.

In the proposed modular architecture, wrappers should stay thin. They should evolve from "tail blocks appended to a monolith" into deterministic wrapper templates applied around a small root package plus scoped modules.

This makes the wrappers a **backlog contrast case**: they are not the main bug source, but they need reframing so they are clearly understood as host-integration artifacts inside the future deterministic build.

---

## Backlog Framing

**Current buggy state:** wrappers currently sit downstream of an oversized monolithic payload, which makes them look architecturally more important than they really are.

**Ideal state:** wrappers remain thin and explicit, applied as final host templates around resolved IaM packages built from manifests and module classes.

---

## Wrapper 1: `Lares_Copilot_Wrapper.md` (34 lines)

**Target:** `.github/copilot-instructions.md`
**Sprint:** 1 — Complete

### Sections

| Block | Content |
|---|---|
| Source guard notice | `> Source file. Do not edit .github/copilot-instructions.md directly.` |
| Worker registry table | 5-row table: Worker / Engineer / Researcher / Agent-Engineer / Assistant + delegation domain |
| Platform note: invocation | `user-invocable: false` — Workers surface only via coordinator delegation, not @-mention |
| Rebuild protocol | `Agent-Engineer Rebuild Protocol` — 4-step procedure (verify sources → run combine → verify alignment → commit) |
| Copilot-specific notes | Generated artifacts live in `.github/agents/*.agent.md`; do not edit directly; regeneration command |

**Marker:** `## Copilot Platform — Worker Registry`

---

## Wrapper 2: `Lares_Claude_Wrapper.md` (43 lines)

**Target:** `.claude/CLAUDE.md`
**Sprint:** 2 — Complete

### Sections

| Block | Content |
|---|---|
| Source guard notice | `> Source file. Do not edit .claude/CLAUDE.md directly.` |
| Worker registry table | Same 5-row table as Copilot |
| Platform notes | Subagents stateless — receive only their own system prompt; delegation via description field |
| Claude-specific notes | Worker source definitions in `builds/agents/workers/*.md`; do not edit `.claude/agents/*.md` directly; tool names differ from Copilot (PascalCase); regeneration command |
| Rebuild protocol | Same 4-step procedure |

**Marker:** `## Claude Platform — Worker Registry`

**Key difference from Copilot:** Claude has no `user-invocable` equivalent. Delegation gated entirely by `description` field and coordinator instructions. Tool names: `Read, Write, Edit, Bash, Grep, Glob, WebFetch` (PascalCase).

---

## Wrapper 3: `Lares_Codex_Wrapper.md` (48 lines)

**Target:** `AGENTS.md` (root)
**Sprint:** 3 — Implemented foundation, slimming still open

### Sections

| Block | Content |
|---|---|
| Source guard notice | `> Source file. Do not edit AGENTS.md directly.` |
| Worker registry table | Same 5-row table |
| Platform notes | Codex reads root `AGENTS.md`; coordinator file is root-level not platform-subdirectory |
| Codex-specific notes | Worker definitions in `.codex/agents/<slug>.toml` (TOML format, not Markdown); do not edit directly; `sandbox_mode` per-worker; root budget still needs slimming back to a stable reload-safe size |
| Rebuild protocol | Same 4-step procedure |
| Sprint notes | `project_doc_max_bytes = 150000` currently acts as a temporary compatibility override; slimming root payloads remains the active next step |

**Marker:** `## Codex Platform — Worker Registry`

**Key difference from Copilot/Claude:** Worker format is **TOML** (`developer_instructions = """..."""`), not YAML+Markdown. Root `AGENTS.md` is shared — Copilot/Claude coexistence is a known issue requiring post-Sprint-3 evaluation.

---

## Shared Structural Pattern

All three wrappers follow the same architecture:

1. generated-file/source-guard notice
2. worker registry block
3. platform-specific delegation/format notes
4. rebuild protocol

What changes across platforms:

- destination file path
- worker artifact format
- tool-name conventions
- invocation/delegation semantics
- size-budget and platform-limit notes

What should stay out of wrappers:

- large identity/lore sections
- repo source map material
- golden prompt examples
- regression test content
- generalized operating rules that belong in reusable modules

That boundary matters because wrappers should describe **host integration**, not carry shared runtime behavior.

---

## Platform Comparison Matrix

| Property | Copilot | Claude | Codex |
|---|---|---|---|
| Coordinator file | `.github/copilot-instructions.md` | `.claude/CLAUDE.md` | `AGENTS.md` (root) |
| Worker format | `.agent.md` (YAML frontmatter) | `.md` (YAML frontmatter) | `.toml` (TOML) |
| Worker location | `.github/agents/` | `.claude/agents/` | `.codex/agents/` |
| Tool name style | lowercase aliases (`read`, `edit`) | PascalCase (`Read`, `Edit`) | N/A (in TOML body) |
| User-invocable gate | `user-invocable: false` field | Via `description` + coordinator prompt | Via Codex session spawn |
| Memory | `/memories/` (VS Code) | `.claude/agent-memory/` (opt-in) | Not applicable |
| Sprint status | ✅ Sprint 1 | ✅ Sprint 2 | ✅ Foundation landed; slimming pending |
| Size constraint | No hard limit (but duplication risk) | ~200 lines recommended | temporary `150000` stopgap active; stable target still lower |

---

## Current vs. Future Role

| Aspect | Current pipeline role | Future modular role |
|---|---|---|
| Core assembly | Appended after the same monolithic body on every platform | Wraps a target-specific manifest output |
| Shared behavior | Indirect only; most shared behavior lives elsewhere | Should contain none beyond wrapper-specific notes |
| Platform routing | Encodes worker format + host quirks | Same, plus points to scoped module strategy |
| Budget control | Mostly absent; wrappers only note constraints | Should participate in explicit budget/report checks |
| Build logic dependency | Marker-based extraction in `combine_agents.py` | Likely manifest/template driven, but still deterministic |

The wrappers are not the architectural problem. The problem sits in the identical 130+ KB core payload placed ahead of them. In the modular design, wrappers remain necessary, but they become the last few percent of the build rather than the only platform-specific distinction.

---

## `platform/README.md` — Section Map (362 lines)

This file is the platform operations manual. It is **reference only** — not injected into any platform output. Key sections:

| Section | Lines (approx) | Content |
|---|---|---|
| Header + Platform Status table | 1–25 | Status per platform: Copilot ✅, Claude ✅, Codex ⏳ |
| VS Code Copilot | 26–145 | Format spec, tool aliases, invocation control, best practices, anti-patterns, file layout, known issues |
| Claude Code | 146–240 | Format spec, coordinator file, best practices (200-line target, import syntax, HTML comment stripping), resolved Sprint 2 items |
| Codex | 241–340 | How Codex reads instructions (directory walk), TOML format, Sprint 3 implementation plan, best practices, known issues |
| Build System Quick Reference | 341–362 | `combine_agents.py` + `verify_alignment.py` commands |

**Must-know items from platform/README.md for any future wrapper work:**

- Copilot YAML: no comments inside frontmatter block — silently corrupts parser
- Claude: HTML comments stripped before context injection — safe for maintainer notes
- Codex: `.codex/config.toml` currently uses `project_doc_max_bytes = 150000` as a temporary compatibility stopgap; do not treat that as the desired steady state
- All platforms: never edit generated files; edit sources and rebuild
- Worker system prompts are isolated (no coordinator context inherited) — prompts must be self-contained

---

## Modular Extraction Notes

The three wrapper files are already appropriately thin. They do not need decomposition. The main modular risks are:

1. **Wrapper drift** — registry tables or rebuild notes diverge across platforms unintentionally.
2. **Host-note leakage** — platform-specific caveats expand until wrappers start duplicating shared operational content.
3. **Marker fragility** — the combine script currently relies on one heading marker per wrapper file.

Recommended future handling:

- keep wrapper files as source-of-truth notes for host quirks
- move shared worker registry data into one reusable source if drift appears
- convert wrapper application from marker slicing to explicit templates/manifests when the build system becomes manifest-driven

The next migration target is not more wrapper work. It is package slimming: use the manifest layer to shrink Codex, Claude, and Copilot root payloads by moving always-on runtime, scoped repo behavior, and reference/spec material into cleaner boundaries. Wrappers should remain thin and template-like throughout that pass.

The `## [Platform] Platform — Worker Registry` marker in each wrapper is what `combine_agents.py` uses to locate and append the platform suffix. Do not rename these markers without updating the combine script.
